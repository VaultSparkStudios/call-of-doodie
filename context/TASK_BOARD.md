# Task Board

## Now
- Playtest session 6 additions: ricochet feel, wall layouts, enemy-wall collision, boss ability ramp
- Balance wall count / size if layouts feel too open or too cluttered

## Next
- Supabase Auth (anonymous) to lock callsign names per browser — prevents impersonation
- Enemy pathfinding around obstacles (currently push-out slide only — no A*)
- End-of-run summary screen (wave, kills, damage, perks taken, missions completed)

## Later
- Capacitor wrapper for iOS App Store submission
- PWA setup (manifest + service worker) for "Add to Home Screen"
- Gamepad / controller support
- Background music (procedural loop via Web Audio API)
- App.jsx game loop extraction to a custom hook (`useGameLoop`)
- Run seed visible + shareable on death screen
- More weapon types (e.g. shotgun spread, boomerang)
- More daily mission types
- Colorblind mode
- Leaderboard pagination (currently top 100 only)
