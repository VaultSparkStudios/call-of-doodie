# Task Board

Public-safe launch roadmap summary.

## Human Action Required
- [ ] [S60] Rotate or narrow the broad Cloudflare `cloudflare-studio-access.txt` token after domain stabilization — current token has account/user/zone-wide powers and should be replaced by narrower domain-migration tokens
- [ ] Physical launch QA — verify PWA install prompt/accept flow on a real mobile/browser combination
- [ ] Physical launch QA — verify one real gamepad/browser combo end-to-end
- [ ] Create Itch.io listing and publish the prepared launch copy package from `docs/LAUNCH_EXECUTION.md`
- [ ] Add `VITE_POSTHOG_KEY` to GitHub repo Settings → Secrets → Actions (workflow already wired in deploy.yml)
- [ ] Add `VITE_SENTRY_DSN` to GitHub repo Settings → Secrets → Actions (workflow already wired in deploy.yml) — S112 probe: the studio-ops `secrets/sentry.env` file does carry a `SENTRY_DSN` key, but the declared `sentry.api` capability in `CAPABILITY_MAP.json` only covers `SENTRY_AUTH_TOKEN` (org/user auth token for releases), not a per-project DSN grant. Wiring an unverified DSN into this public game's error stream risks misattributing errors to the wrong Sentry project. Founder should confirm that DSN is actually scoped to call-of-doodie before it's set as a GitHub secret.
- [x] Apply `supabase/migrations/2026-04-14_kofi_webhook.sql` in the Supabase SQL editor — applied 2026-04-21 via `supabase db query --linked --file` against project ref `fjnpzjjyhnpmunfoycrp`; `kofi_events` table verified in `pg_tables`
- [x] Set `KOFI_VERIFICATION_TOKEN` as a Supabase function secret and paste the webhook URL into Ko-fi → More → Settings → API & Webhooks — secret set 2026-04-21 via `supabase secrets set`; webhook URL `https://fjnpzjjyhnpmunfoycrp.supabase.co/functions/v1/kofi-webhook` pasted into Ko-fi; end-to-end verified with a simulated POST returning `HTTP 200 {"ok":true,"supporterUpdated":true}` + audit row written + supporter flag flipped on `callsign_claims`
- [x] Ko-fi webhook `callsign_claims.uid` NOT NULL gotcha — the Edge Function runs as service role where `auth.uid()` is NULL, so the upsert failed with a silent 500. Fixed 2026-04-21 via migration `2026-04-21_callsign_claims_uid_nullable.sql` (`ALTER TABLE callsign_claims ALTER COLUMN uid DROP NOT NULL;`). Supporters who tip before they log in are now recorded as `{ name, supporter: true, uid: NULL }`; `uid` fills in on first login

## Now
- [x] [SIL:2] **DONE S113** validate-replay edge deterministic slice receipts — generated `docs/AUDIT_2026-07-02.*`, wired Deno-compatible deterministic contract/stepper/combat/contact-enemy receipts into `supabase/functions/validate-replay/pressure.js` under the existing advisory pressure-estimate gate, typed the receipt in `validate-replay/index.ts`, and extended `npm run replay:edge-fixtures` to compare edge summary fields against browser `runResim()`. Validation: edge fixtures 4/4, focused replay 17/17, replay state-stepper 4/4, lint clean, full `npm test` 595/595, build passing, Deno check passing.
- [x] [SIL:3] **DONE S112** Determinism arc: seeded enemy RNG + REMATCH drill + replay contact-enemy parity slice — generated `docs/AUDIT_2026-07-01_6.*`, shipped `createWaveRng()`/`getWaveSpawnRng()` in `gameHelpers.js` threading a per-wave-derived stream through `spawnEnemy`/`spawnBoss`/App.jsx spawn call sites (type, side/position, elite rolls, wobble, shootTimer, cluster jitter, wave-director event roll), a REMATCH-at-death-wave practice drill (`src/systems/rematchDrill.js` + DeathScreen button, leaderboard/achievement/career-record excluded), and `runDeterministicContactEnemySlice()` extending Phase 2B replay trust with a seed-derived single contact-enemy step (advisory gate unchanged). Also closed the stale S82/S61 credential-gate blockers with live CI/OPTIONS evidence, surfaced the local balance-lab top insight to players (was debug-only), removed per-frame allocations in the drawGame hot loop (filter(Boolean) + gradient caching + hoisted shape tables), and deleted 4 dead non-spatial sound exports. Validation: 61 new/updated focused tests across gameHelpers/rematchDrill/replayResim/HomeV2, lint clean, full `npm test` 593/593, build passing, replay state-stepper + edge fixtures green.
- [x] [SIL:2] **DONE S111** DeathScreen event-source extraction + debrief receipt dedupe — generated `docs/AUDIT_2026-07-01_5.*`, moved debrief/drill/weekly-contract/rivalry event planning and score-submit fallback payloads into tested `deathFlow` builders, and added a stable debrief event key so rerenders do not duplicate local Studio receipts. Validation: focused death-flow/HomeV2 16/16, lint clean, full `npm test` 561/561, build passing, replay/media/live gates green.
- [x] [SIL:2] **DONE S110** PWA install QA receipt surface + saturation arc — generated `docs/AUDIT_2026-07-01_4.*`, shipped `buildPwaInstallReceipt()` / PWA install attempt helpers in `src/utils/pwaInstallReadiness.js`, persisted real browser prompt accepted/dismissed outcomes from App, and rendered a truthful HomeV2 PWA receipt chip without claiming physical install completion. Validation: focused PWA/HomeV2 16/16, lint clean, full `npm test` 559/559, build passing, replay state-stepper 4/4, edge replay fixtures 4/4, launch media check passing.
- [x] [SIL:2] **DONE S109** Input QA receipt surface + saturation arc — generated `docs/AUDIT_2026-07-01_3.*`, shipped `buildInputQaReceipt()` in `src/utils/inputCalibration.js`, wired HomeV2 to render the tested local input QA receipt, and preserved honest replay/media/device deferrals instead of force-changing trust labels or screenshot provenance. Validation: focused input/HomeV2 15/15, lint clean, full `npm test` 552/552, build passing, replay state-stepper 4/4, edge replay fixtures 4/4, launch media check passing.
- [x] [SIL:1] **DONE S108** Launch-confidence verification and deploy readiness arc — generated `docs/AUDIT_2026-07-01_2.*` / `audits/2026-07-01-session108.json`, verified no unblocked product-code item beyond maintaining launch confidence, preserved credential/manual/data deferrals, and completed local/live deploy evidence. Validation: lint clean, `npm test` 550/550, build passing, replay state-stepper 4/4, edge replay fixtures 4/4, launch media check passing, live site 5/5, post-cutover smoke 5/5, and Wrangler authenticated for Cloudflare Pages deploy.
- [x] [SIL:3] **DONE S106** Deterministic replay combat slice + protocol green-gate repair — generated `docs/AUDIT_2026-07-01.*`, added trace-driven `runDeterministicReplayCombatSlice()` with movement/dash/fire/ammo/reload/grenade/blocked-action receipts, exposed `deterministicCombatSlice` through `runResim()` without changing advisory replay labeling, expanded `npm run replay:state-stepper`, and restored model-router Unicode safety, compact-handoff smoke, and Codex plan-mode truth. Validation: focused replay 11/11, state-stepper fixtures 4/4, protocol focused 4/4, lint clean, full `npm test` 550/550, build passing, edge fixtures 4/4, launch media check passing.
- [x] [SIL:4] **DONE S105** Deterministic replay stepper + game-loop protocol source + verified launch media contract — generated `docs/AUDIT_2026-06-29_3.*`, added `context/GAME_LOOP.md`, added movement/aim-only `runDeterministicReplayStateStepper()` plus `npm run replay:state-stepper`, and moved manifest combat/mobile screenshots to verified browser-capture PNGs with production-ready provenance enforcement. Validation: lint clean, full `npm test` 547/547, build passing, replay state-stepper 4/4, edge replay fixtures 4/4, launch media check passing, protocol drift 24/24.
- [x] [SIL:5] **DONE S104** Death/debrief telemetry truth + replay contract readiness — generated `docs/AUDIT_2026-06-29_2.*`, extracted visible DeathScreen coaching telemetry into `buildDeathCoachTelemetry()`, extracted score-submit analytics payloads into `buildScoreSubmitAnalyticsPayload()`, added deterministic replay input-contract readiness via `buildDeterministicResimInputContract()` without overclaiming full resimulation, and preserved coaching flags in local `debrief_intelligence` Studio events. Validation: affected tests 22/22, full `npm test` 545/545, lint clean, build passing, replay edge fixtures 4/4, launch media check passing.
- [x] [SIL:5] **DONE S103** Arc protocol reliability + second-order hardening — restored canonical startup brief GENIUS/HUMAN PRESSURE boxes, Unicode-safe model-router transport and compact-handoff smoke, Codex plan-mode truth, hidden closeout shell spawns, and protocol drift coverage for Windows-hide enforcement. Generated `docs/AUDIT_2026-06-29.*`, `docs/IMPLEMENT_PLAN.md`, and updated the innovation pack with honest credential/product deferrals. Validation: focused protocol tests 6/6, startup brief validator, compact-handoff smoke, check-windows-hide, verify-plan-mode JSON, and protocol drift 24/24.
- [x] [SIL:3] **DONE S102** Leaderboard submission repair + protocol tooling sync — fixed `submit-score` HMAC signature mismatch (PostgREST returns `+00:00` vs JS `.toISOString()` `Z`; normalized via `new Date(tokenRow.expires_at).toISOString()`); fixed `sw.js` navigation-handler Response clone race (clone synchronously before detached `caches.open()`; bumped to `cod-v6`); synced `insight-voice-linter.mjs`, `skill-brief.mjs`, and `render-brief-delta.mjs` from studio-ops. Validation: `npm test` 540/540, build passing.
- [x] [SIL:2] **DONE S100** Compact-handoff smoke + explicit Obelisk routes — added `node scripts/compact-handoff.mjs --smoke-unicode` and `tests/compact-handoff-unicode-smoke.test.js` to prove malformed handoff Unicode cannot break startup transport again. Routed the generated Obelisk login surface explicitly through `/login` and `/auth/callback` only (`src/obeliskRoutes.js`, `src/ObeliskCallback.jsx`, `src/main.jsx`), with tests proving `/`, `/daily`, and unknown paths still render gameplay. Validation: direct smoke, focused tests, lint, `npm test` 510/510, build, and staged secret scans all clean; commits pushed (`a1d12f0`, `520a4d8`).
- [x] [SIL:2] **DONE S99** Protocol transport + Codex plan-mode truth — `scripts/lib/model-router.mjs` now sanitizes malformed Unicode scalar fragments before Anthropic API JSON payloads leave the process, preserving valid emoji while replacing lone surrogate code units. `scripts/verify-plan-mode.mjs` again stamps Codex sessions as `not_required` instead of falsely prompting for Claude Code `/model opusplan`. Validation: focused protocol tests 3/3, live compact-handoff force run succeeds, live plan-mode JSON reports `not_required`, game medium gate passes, protocol drift 20/20, lint clean, `npm test` 508/508, build passing.
- [x] [SIL:6] **DONE S98** Signature asset pack + security/lint/build hygiene — generated the first proprietary signature runtime pack (`assets/source/signature-pack/` + `public/visual-assets/`), added `npm run assets:generate`, expanded `assets/visual-assets.json` to 10 tracked proprietary assets, and surfaced the pack in HomeV2/Codex. Fixed five npm/GitHub Dependabot alerts with exact overrides, confirmed GitHub alerts moved to `fixed`, cleared all lint warnings, and split Sentry/Supabase vendor chunks so the main build chunk dropped from ~804 kB to ~620 kB with no chunk warning. Validation: asset checks, launch media check, focused asset tests 6/6, `npm audit` 0 vulnerabilities, lint clean/no warnings, `npm test` 505/505, build clean, e2e 2/2.
- [x] [SIL:3] **DONE S97** Proprietary visual asset library + pseudo-3D pipeline — `assets/source/README.md` and `assets/visual-assets.json` now establish Call of Doodie's internal proprietary source-art/provenance library; `npm run assets:check` and `npm run launch:media-check` validate manifest/source/runtime and launch PNG coverage; `src/utils/visualPrimitives.js` gives drawGame reusable pseudo-3D material primitives for enemy/player/weapon rendering; Playwright e2e is isolated to strict port 53173 and browser specs only. Ark `canon-update` cargo queued to `vaultspark-studio-ops` for studio-wide propagation of per-game proprietary asset libraries. Validation: 503/503 tests, e2e 2/2, lint 0 errors / 7 existing warnings, build passing.
- [x] [SIL:10] **DONE S96** Combat audio × career depth × coaching sprint — `docs/AUDIT_2026-06-15_3.json` shipped 10 waves: `soundBossGrudge(tier)` + combo urgency sounds; respite pickup beacon ring + vignette gradient cache; `moments[]` in share-card worker; `peakMomentRef` → PEAK MOMENT DeathScreen row; `gs._waveScoreLog[]` → SVG sparkline; `career.enemyKillBests{}` + `updateEnemyCareerStatsBatch()` + MostWantedPanel stats; `buildRunBrain(chokeWaves)` → `chokeWarning` coach pill; `buildWeaponDeathCoach()` "Mismatch:" line; `ammoPulseYellow`/`ammoPulseRed` HUD urgency animation. Validation: 499/499 (+10), lint clean, build clean.
- [x] [SIL:1] **DONE continuation 2026-06-15** Edge validate-replay pressure parity — `supabase/functions/validate-replay/pressure.js` now owns Edge pressure/evidence helpers, `validate-replay/index.ts` consumes the shared module, and `npm run replay:edge-fixtures` compares Edge pressure output against shared browser replay fixtures. Validation: Edge parity 4/4, browser fixtures 4/4, focused replay tests 17/17, full `npm test` 489/489, lint 0 errors / 7 existing warnings, build passing, and Deno Edge type-check passing via `C:\tmp\deno-2.8.2\deno.exe`.
- [x] [SIL:2] **DONE S94** Startup brief canonical box repair — `docs/AUDIT_2026-06-15.md` / `.json` shipped both protocol-integrity items: `scripts/render-startup-brief.mjs` now normalizes raw `generate-genius-list.mjs --brief` stdout into a canonical `GENIUS HIT LIST` tile and always emits `HUMAN PRESSURE` with an honest empty state when no compiled pressure queue exists; `tests/startup-brief-boxes.test.js` covers plain/boxed/empty genius output plus pressure item/empty-state paths. Validation: focused 5/5, startup brief render passed, brief validator passed, full `npm test` 489/489, lint 0 errors / 7 existing warnings, build passing.
- [x] [SIL:2] **DONE S93** Mission progress truth + telemetry single-writer sprint — `docs/AUDIT_2026-06-14_5.md` / `.json` shipped both items: mission completion lookup now treats legacy index keys and mission id keys as compatible across HomeV2, MenuScreen, and MissionsPanel; death/submit Studio events now have single owners, with generic death facts in `createDeathStudioEvents()`, weekly contract progress in DeathScreen's contract-specific de-duped writer, and successful score submission events in App.submitScore. Validation: focused storage 45/45, focused runSession 5/5, full `npm test` 484/484, lint 0 errors / 7 existing warnings, build passing.
- [x] [SIL:3] **DONE S92** Replay/share-card truth + contract progress sprint — `docs/AUDIT_2026-06-14_4.md` / `.json` shipped all 3 items: Run DNA share-card worker payload now comes from tested `src/utils/runDnaShareCard.js` and carries actual replay proof tier; DeathScreen persists de-duplicated `weekly_contract_progress` events through `buildWeeklyContractProgressPayload()`; replay trace fixtures now pin pressure-profile values and `scripts/validate-replay-trace-fixtures.mjs` enforces class/count/finalWave/finalScore parity. Validation: focused 13/13, fixture validator 4/4, full `npm test` 482/482, lint 0 errors / 7 existing warnings, build passing.
- [x] [SIL:17] **DONE S91** Depth sprint × L3 ladder climb — `docs/AUDIT_2026-06-14_3.md` / `.json` shipped all 10 audit items + 7 ladder climbs: adaptive-boss-dialogue (interpolateBossQuote + session-escalation + tone/bossKills tokens + getBossTone); precision-streak audio (soundPrecisionClick rising pitch + soundPrecisionLock); wave death social proof (deathCount + ⚠ CHOKE POINT chip via getCommunityChokePoints ≥3×median); combo fullscreen card (RAMPAGE/GODLIKE/UNSTOPPABLE cinematic overlay); beat-precision window (cyan vuln ring + +2💩 on-beat; window scales with streak; trackRhythmMasteryHit career stat; rhythm mastery milestones); run-arc (gs._runAct spawn rate modifier + atmospheric vignette per act); proximity cluster spawning (±60px same-type offset); weapon evolution on legend (WEAPON_EVOLVED_NAMES + ×1.05 dmg at 1000 kills); run-DNA share card (OffscreenCanvas PNG worker + community wave percentile); weekly world theme (WEEKLY_THEMES ×5 + statOverrides at gauntlet start); soundChainEscalate(1|2) at _chainEnrageLevel crossings. Validation: 478/478 tests (+17 new), lint 0 errors / 7 warnings (−1 vs S90), build passing.
- [x] [SIL:2] **DONE S90** Replay proof presenter + local innovation-pack command — `docs/AUDIT_2026-06-14_2.md` / `.json` shipped L3 presenter extraction (`src/utils/replayProofPresenter.js`, DeathScreen proof card/share-card/submission feedback proof readouts through one tested helper) plus `node scripts/ops.mjs innovation-pack` and `docs/INNOVATION_PACK.md` so `/implement` saturation loops have an executable local expansion path. Validation: focused 27/27, full `npm test` 453/453, fixture validator 4/4, protocol drift 20/20, lint 0 errors / 8 existing warnings, build passing.
- [x] [SIL:3] **DONE S89** Replay proof receipt + pressure-profile trust sprint — `docs/AUDIT_2026-06-14.md` / `.json` shipped all 3 items: `buildReplayProofReceipt()` turns trace evidence into a player-facing score/status/proof-lines/next-action receipt; `buildSessionSubmission()` now includes `traceReceipt`; DeathScreen renders a standalone REPLAY PROOF card for weak/basic/rich traces; `buildReplayPressureProfile()` extracts advisory pressure math for `runResim()`; `replayTraceFixtures.js` provides rich/basic/weak/malformed fixtures. Validation: focused replay/submission 21/21, full `npm test` 448/448, lint 0 errors / 8 existing warnings, build passing.
- [x] [SIL:8] **DONE S88** Combat personality + run identity sprint — `docs/AUDIT_2026-06-13_4.md` / `.json` shipped all 8 items: wave-respite-breath (120f loot lull after threat≥4 waves, _waveTransitDone/_respiteLock dual-flag arch); flow-state-time-dilation (precision streak≥10 triggers 90-frame bullet-time, 300f cooldown); boss-grudge-dialogue (BOSS_GRUDGE_QUOTES: intro/taunt/grudge/nemesis variants per boss type, dynamicQuote replaces static quote); enemy-combat-taunts (8 enemy types taunt player mid-combat, per-enemy 180f + global 60f cooldown); run-narrative-arc (runNarrative.js: act classification + up to 3 turning points, RUN ARC card on DeathScreen); weapon-legend-milestones (weaponLegendKills[] in career storage, Recruit/Veteran/Elite/Legend tiers, DeathScreen chip on threshold cross); run-dna-fingerprint (proportional weapon kill-distribution bar below BUILD GRADE); enemy-chain-fear-enrage-spawn (enrage≥1 spawns flash tint + ⚡ ENRAGED label). Validation: 444/444 tests, lint 0 errors, build passing.
- [x] [SIL:1] **DONE S87 continuation 3** Boss phase-two readable counterplay — `docs/AUDIT_2026-06-13_3.md` / `.json` shipped the exhausted-queue follow-on: `getBossPhaseTwoWarning()` in `src/systems/bossPhases.js` covers every guided boss plus fallback, and `triggerBossPhaseTwoTransition()` now shows a concrete counterplay line under the PHASE 2 banner. Validation: focused bossPhases 4/4, full `npm test` 444/444, lint 0 errors / 1 pre-existing warning, audit JSON parse clean, build passing.
- [x] [SIL:3] **DONE S87 continuation** Studio OS loop parity + nemesis dossier truth — `docs/AUDIT_2026-06-13_2.md` / `.json` shipped all 3 items: repo-local helpers for `sample-codebase`, `audit-sidecar`, `render-audit-md`, `session-floor`, `cache-genius-list`, `generate-genius-list`, and `record-skill-cost`; `ops.mjs genius-list` now executes; protocol drift coverage now checks 19 helpers; nemesis weapon recommendations moved to tested `waveDirector.js` helper and cover every guided boss. Validation: script smokes pass, focused waveDirector 19/19, full `npm test` 442/442, lint 0 errors / 1 pre-existing warning, build passing.
- [x] [SIL:8] **DONE S87** Combat depth × social rivalry sprint — `docs/AUDIT_2026-06-13.md` shipped all 8 items: wave-threat-rating (1-5 💀 danger score on wave preview card); heat-formation-seeding (heatBiasedFormation promotes flanks→pincer at heat 1, forces pincer at overdrive); formation-lore-card (per-formation toast explains tactics once per wave); kill-chain-ai-escalation (combo≥15 enemies +10% speed +15% fire rate, ≥35 +20%/+20%); certified-run-badge (⭐ VERIFIED RUN chip when trace evidence is rich); precision-best-shot-replay (🎯 BEST SHOT button scrubs ghost path to peak streak frame); nemesis-intelligence-brief (red-bordered dossier with weapon counter + evasion tip on boss card); ghost-rivalry-proximity-graph (getProximityRivals ±10% personal best → DeathScreen RIVALRY LADDER card). Validation: 440/440 tests (+8 new), lint 0 errors, build passing.
- [x] [SIL:2] **DONE S86 follow-on** Replay trust honesty + death-readout sprint — `docs/AUDIT_2026-06-12_2.md` / `.json` shipped all three items: replay resim receipts now label the gate as `heuristic_pressure_estimate` / `advisory` / `pressure-estimate-v1`; DeathScreen ghost replay now renders a final-path coaching readout (pinned/sprinting/trapped/drifting); and Run History trust copy has a regression guard against deterministic/resimulation overclaims. Validation: focused tests 15/15, `npm test` 432/432, lint clean, build passing.
- [x] [SIL:8] **DONE S86** Combat feel + replay/rivalry completion sprint — `docs/AUDIT_2026-06-12.md` / `.json` shipped all eight items: last-stand-clutch (HP<15% red vignette + setDangerIntensity(1.0) + "LAST STAND!!" text + gs.lastStandActive flag); kill-chain-audio-escalation (soundEnemyDeathAt gains combo param, pitch escalates +80¢/tier, RAMPAGE/GODLIKE/UNSTOPPABLE milestone text at 5/10/15); adaptive-soundtrack-layers (soundLastStand, soundHeartbeatPulse 55-frame periodic pulse, soundBossFinale at boss HP<10%); live-pace-coaching-chip (HUD PACE chip vs careerBest.wave, green=ahead/orange=behind, hidden until wave≥3); phantom-elite-variant (new elite type wave≥25 12% chance, 90-frame opacity toggle 15%↔95%, purple dashed ring, speed×1.1 HP×0.85); weekly-rival-ghost (7-day leaderboard rival + 1h session cache + HUD WEEKLY RIVAL chip); death-recap-mini-replay (DeathScreen final-path replay animation + REPLAY restart); replay-resim-runner (`src/utils/replayResim.js` + `validate-replay` Phase 2B rich-trace drift reporting/rejection above 2%). Validation: focused replay tests 11/11, full `npm test` 429/429, lint clean, build 770.54 kB / 237.91 kB gzip main chunk.
- [x] [SIL:3] **DONE S85** Mobile input proof + aim-check/death-label continuation — `docs/AUDIT_2026-06-08_2.md` / `.json` shipped Mobile Chrome Playwright pointer 360 proof, a HomeV2 AIM CHECK chip backed by tested local calibration nudge states, and a labeled DeathScreen final-killer heatmap marker via `src/utils/ghostPath.js`. Validation: focused tests 11/11, `npm test` 427/427 across 48 files, `npm run lint` clean, `npm run build` passing, and `npm run test:e2e` passing 2/2 across desktop Chromium + Mobile Chrome.
- [x] [SIL:7] **DONE S84** Product intelligence + pointer-proof sprint — `docs/AUDIT_2026-06-08.md` / `.json` now record the eight-item implementation pass plus follow-up dependency remediation: local RunBrain difficulty suggestion, DeathScreen BUILD GRADE card, enemy-type death marker annotation, Playwright pointer 360 browser harness, pointer sweep projection repair, verified current-worktree wave kill attribution, mutation accept flash, coin streak escalation, adaptive spawn damping, and a clean dev-toolchain audit. Validation: `npm test` 423/423 across 47 files, `npm run build` passing, `npm run test:e2e` passing 1/1 Chromium test, and `npm audit --json` reporting 0 vulnerabilities.
- [x] [SIL:1] **DONE S83** Startup helper parity pack — `docs/AUDIT_2026-06-07_2.md` / `.json` shipped local compatibility helpers for `credential-watch`, `ark`, `router`, `check-brief-staleness`, `build-skill-manifest`, and `skill-trace-emit`; canonical startup helper probes now execute from this public game repo. Validation: helper probes pass, `npm run protocol:drift -- --json` reports `status=ok` with `missingRequired=0` and `missingOptional=0`, `npm run lint` clean, `npm test` 412/412, and `npm run build` passing.
- [x] [SIL:4] **DONE S82** Audit follow-through sprint — `docs/AUDIT_2026-06-07.md` / `.json` now have execution logs for all four items: Enemy Lab Run Coach renders repeat-killer drills on DeathScreen; trace-proof next benchmark appears in trust recommendations; launch readiness emits structured owner-gate evidence receipts; and `npm run protocol:drift` reports optional Studio OS helper drift without blocking startup. Validation: focused Run Coach/trust tests 20/20, full `npm test` 412/412, lint clean, build passing, launch readiness JSON verified, protocol drift JSON verified with `missingRequired=0`.
- [x] [SIL:6] **DONE S82** Console/runtime repair + boss-wave soft-lock fix — service worker cache/fetch paths no longer throw on consumed response bodies or offline requests; explicit Install App actions now call the deferred browser prompt; shop/draw/game-loop state guards prevent undefined `health` and null enemy crashes; forced next-round boss-wave cards now show a transient boss banner instead of sticking on the title; Studio event sync no longer retries failed batches forever and the edge function preserves non-UUID browser client ids in payload. Validation: focused tests 32/32, full `npm test` 412/412, lint clean, build passing.
- [x] [SIL:1] [Credential-gated S82] Deploy `sync-studio-events` edge-function repair — **DONE S112 (stale blocker closed with live evidence)**: the local-CLI path lacked `SUPABASE_ACCESS_TOKEN`, but the CI path never did — `gh secret list` shows `SUPABASE_ACCESS_TOKEN` + `SUPABASE_PROJECT_REF` (set 2026-03-31) and `.github/workflows/deploy-supabase-function.yml` has deployed all five functions green 5× since 2026-06-12 (last push-run 27794392576 on 2026-06-18; fresh S112 `workflow_dispatch` run 28555855725 re-verified current tree). Live probe: OPTIONS `sync-studio-events` from `https://callofdoodie.wtf` → 200.
- [x] [SIL:1] **DONE S81** Skill profile personalization shim — fresh `docs/AUDIT_2026-06-05_3.md` shipped the missing `scripts/lib/skill-profile.mjs` game overlay used by `/start`, `/audit`, `/implement`, and `/closeout`; added `scripts/set-active-skill.mjs`, `scripts/lib/medium-quality-gates.mjs`, and `scripts/lib/sil-rubrics.mjs` so the public repo can execute the requested Studio OS skill sequence without private imports. Validation: startup protocol smokes pass, `npm run lint` clean, `npm test` 408/408, build passing.
- [x] [SIL:2] **DONE S80** Protocol integrity repair — fresh `docs/AUDIT_2026-06-05_2.md` shipped repo-local Studio OS helper compatibility (`turn-classifier`, `visual-blocks`, `sil-forecaster`, `blocker-rules`, `skill-cost-ledger`, `scan-secrets`), restored Codex plan-mode `not_required`, enforced the SIL status invariant, and proved protocol smokes plus lint/test/build.
- [x] [SIL:3] **DONE S79** RunBrain guardrail + revenge drill + controller glyphs — fresh `docs/AUDIT_2026-06-05.md` shipped three launch-polish items: `matchesExperiment()` now safely builds a config haystack before safe-opener pattern checks; HomeV2 action stack promotes clustered recent deaths into a `revenge_drill` Most Wanted study CTA; shared `getControllerLabels()` feeds Xbox/PlayStation/generic labels through TutorialOverlay, ControlsPanel, PauseMenu, and remembered HomeV2 controller profile fallback. Validation: focused helper tests 29/29, full `npm test` 408/408, lint clean, build passing.
- [x] [SIL:5] **DONE S78** Nemesis boss mechanic + experiment followthrough loop + aim flow state ring + mutation×difficulty brief + formation flavor — boss kill/death record per type in `cod-boss-kills-v1`; nemesis threshold (3 deaths, 0 kills) shows 🎯 badge on cutscene card + health bar + 30💩 reward on nemesis kill + `nemesis_slain` achievement (66 total); RunBrain experiment tracking with `saveExperimentIntent`/`matchesExperiment`, 🧪 HUD chip + DeathScreen result line; precision streak ≥5 draws animated glow ring (cyan→violet), streak ≥10 adds enemy center-hit window highlights; `getMutationDifficultyBrief()` cross-refs mutation+difficulty from run history; wave preview cards show formation flavor text. Validation: 405/405 tests (+22 new), lint 0 errors, build passing.
- [x] [SIL:3] **DONE S77** Input evidence memory — `src/systems/gameStep.js` now builds a pure four-direction pointer sweep report; `src/utils/inputCalibration.js` persists local-only verified aim coverage; `src/utils/gamepad.js` persists last controller profile metadata; `?debug=input` shows calibration status; HomeV2 surfaces remembered input/controller status. Validation: focused input/control tests 27/27, full `npm test` 383/383, lint clean, build passing.
- [x] [SIL:3] **DONE S76** Input diagnostics + pointer aim contract — `?debug=input` / `cod-debug-input=1` now exposes a hidden in-run QA HUD with controller slot/type/id, stick state, aim angles, action readiness, pointer coordinates, and trace counts; runtime pointer aim now uses tested pure projection helpers. Validation: focused tests 22/22, full `npm test` 378/378, lint clean, build passing.
- [x] [SIL:3] **DONE S75** Controller/input trust repair — normalized gamepad input now selects the active controller, restores Xbox mapping (left stick move, right stick aim, RT shoot, A dash, B grenade, X reload, LB/RB weapon cycle), separates controller movement from keyboard/touch state, and adds focused gamepad regression coverage. Validation: gamepad/gameStep tests 14/14, lint clean, build passing.
- [x] [SIL:2] **DONE S75** Website scrollability + onboarding repair — HomeV2 and modal overlays now scroll at the viewport with safe-area padding instead of clipping top/bottom content; first-run cards now teach survival, aiming, upgrades, and the full-circle aim check.
- [x] [SIL:2] **DONE S75** Current sound variety pass — existing synthesized shoot, hit, pickup, grenade, UI, movement, achievement, and boss cues now use layered chirps/noise, detune, and small variations without new assets or dependencies.
- [x] [SIL:1] **DONE S75** Account/Obelisk posture recorded — `context/OBELISK_ADOPTION.md` declares phase-0 adoption and `docs/AUDIT_2026-05-26.md` ranks Supabase Auth plus Obelisk-style signed migration/callsign receipts as the account path.
- [x] [SIL:3] **DONE S74** Trace-proof coaching + resim readiness — `buildTraceEvidenceContract()` turns weak/basic/rich replay evidence into actionable replay-proof drills; Studio event trust summaries now derive resim readiness from trace evidence and sync health; Run History renders Trace basic + resim readiness chips; Run Brain prioritizes weak-trace drills without LLM/API calls. Validation: focused tests 12/12, lint clean, build passing, full `npm test` 370/370.
- [x] [SIL:3] **DONE S73** Trace-evidence feedback loop — `buildSessionSubmission()` now attaches compact replay trace evidence; leaderboard submission results preserve it across online/rejected/local paths; `submit-score` mirrors evidence analysis, stores `traceEvidence` in member session metadata, and returns it as a receipt; Run History trust ops now shows rich/weak trace counts and weakness reasons. Validation: focused tests 22/22, lint clean, build passing, full `npm test` 363/363.
- [x] [SIL:3] **DONE S72** Replay evidence-quality substrate — `src/App.jsx` now samples movement and aim octants into the bounded command trace; `src/utils/replayCommandTrace.js` adds `analyzeReplayCommandTrace()` with weak/basic/rich evidence classification and weakness reasons; `validate-replay` now returns `traceEvidence` and only grants `trace_contract` confidence to rich body-backed traces. Validation: focused trace/submission tests 15/15, lint clean, build passing, full `npm test` 362/362.
- [x] [SIL:3] **DONE S71** Replay trace-trust follow-up — `src/utils/replayCommandTrace.js` now includes a bounded command recorder + direction bucketing; `src/App.jsx` records real gameplay commands (shoot, dash, grenade, perk, route, shop, reload, swap) into the submission trace; `buildSessionSubmission()` strips malformed traces before network submit; and `validate-replay` validates optional trace bodies against byte/count/digest/frame/action-shape rules. Validation: focused trace tests 13/13, lint clean, build passing, full `npm test` 360/360.
- [x] [SIL:4] **DONE S70** Navigation/formation/feedback/trust audit sprint — `src/systems/flowField.js` extracts deterministic flow-field pathing from `App.jsx`; wave director spawns now get bounded formation identity (`flank`, `pincer`, `surge`); DeathScreen surfaces the active weekly contract progress under Run Brain; replay trace contracts now enforce a 10,000-byte body budget on the client and in `submit-score`. Validation: targeted flow/wave/social/trace tests 22/22, lint clean, build passing, full `npm test` 357/357.
- [x] [SIL:4] **DONE S69** Trace payload + ghost-pack follow-through — `saveToLeaderboard()` now preserves `traceDigest`/`traceLength`/`traceBody` after leaderboard normalization via `buildSubmitScorePayload()`; `buildSessionSubmission()` forwards compact trace bodies; `submit-score` validates trace body count + digest and stores valid bodies only in member `game_sessions.metadata`; `npm run replay:trust-smoke` posts valid/malformed trace contracts to deployed `validate-replay`; `HUD` now renders loaded `gs.topGhosts` as a compact Ghost Pack score target strip. Tests: 350/350. Lint/build clean. Live trust smoke was attempted but blocked by sandbox network and unapproved escalation.
- [x] [SIL:9] **DONE S67** 10-item depth sprint — rhythm-kill-bonus (beat-sync 💩 skill layer), doodie-pass-play-widget (cosmeticTrack reconcile on death → DeathScreen gold card), persistent-ghost-leaderboard (`loadTopGhosts` + `gs.topGhosts`), daily-mission-streak (streak counter + 🔥 chip HomeV2), objective-mastery-deathscreen (completed/failed arrays → DeathScreen OBJECTIVES card), predictive-difficulty-briefing (`getDifficultyBriefing` below difficulty picker), cross-run-coaching-memory (`mostFrequentKiller` + `crossRunTip` in RUN COACH), replay-trace-submission-integration (`commandTraceRef` + `encodeReplayCommandTrace` in submitScore), weapon-unlock-telemetry (unlock detection loop + `track("weapon_unlock")`), app-extraction-slice-1 (`gameStep.js` with `computeMovementVector` + `applyPlayerMovement` + 11 tests). Tests: 347/347. Lint: 0 errors.
- [x] [SIL:2] **DONE S66** Replay command trace v1 — `src/utils/replayCommandTrace.js` now creates bounded replay-input evidence with deterministic normalization, compact encode/decode, digest validation, and summary output; 4 focused tests cover ordering, round-trip, tamper detection, and caps.
- [x] [SIL:1] **DONE S66** Launch readiness JSON — `scripts/launch-readiness.mjs --json` now returns machine-readable checks, owner-only gates, and readiness summary while preserving the existing text report.
- [x] [SIL:1] **DONE S66** Closeout autopilot help hardening — `scripts/closeout-autopilot.mjs --help` / `-h` now prints usage for dry-run/skip-push/yes/message and exits before doctor/git/prompt work.
- [x] [SIL:1] **DONE S64** Replay bootstrap starter hydration — HomeV2 now applies decoded `starterLoadout` from both `?replay=` URLs and pasted replay codes; focused HomeV2 regression covers seed, daily mode, difficulty, and starter hydration.
- [x] [SIL:3] **DONE S63** Replay link fidelity — `DeathScreen.jsx` now receives the actual `starterLoadout` from `App.jsx` and encodes it into shareable replay URLs; `replayCode.test.js` covers non-standard starter loadout round-trip fidelity.
- [x] [SIL:3] **DONE S63** Precision skill memory — best precision streak is tracked for each run, passed into Run Coach/Run Brain, and rendered as precision mastery or precision-gap guidance on the death screen; focused runCoach/runBrain tests added.
- [x] [SIL:1] **DONE S63** Canonical public surface repair — `README.md` now points to `https://callofdoodie.wtf/`, names Cloudflare Pages as canonical hosting, and documents the root dev URL plus post-cutover smoke command.
- [x] [SIL:2] **DONE S63** Post-cutover smoke command — `scripts/post-cutover-smoke.mjs` + `npm run post-cutover:smoke` verify apex, Pages preview, `www`, backup apex, and backup `www`; live run passed 5/5 after network permission.
- [x] [SIL:3] **DONE S62** Replay share links — `HomeV2.jsx` now parses `?replay=` URL params on mount (auto-fills seed/difficulty/mode, opens deploy dropdown); SHARE button copies full `?replay=<code>` URL to clipboard; `DeathScreen.jsx` gains "🔗 SHARE RUN" button that encodes current run as a share link + fires `debrief_share_replay_link` telemetry
- [x] [SIL:2] **DONE S62** Run Coach weapon tips + enemy evasion tips — `runCoach.js` fully rewritten: `buildWeaponTip()` detects waste/dominated/spread-build patterns from `weaponKills` array; `buildKilledBy()` appends enemy-specific evasion tip for boss-type repeat killers (types 4/9/17/3/6/10/19/20); `DeathScreen.jsx` renders the `weaponTip` line in the AI RUN COACH card; 8/8 runCoach tests pass
- [x] [SIL:2] **DONE S62** Precision hit system — `combatResolution.js` exports `isPrecisionHit(bullet, enemy)` (hit within 35% of enemy core radius); App.jsx bullet-enemy collision tracks `gs.precisionStreak`, awards +1💩 per hit with "PRECISION BURST! +3💩" milestone at streak 3+; 15/15 combatResolution tests pass
- [x] [SIL:1] **DONE S62** Rivalry auto-load — `startGame()` in App.jsx auto-loads the first unbeaten rivalry entry from `loadRivalryHistory()` when no explicit `challengeOpts.vs` is passed; existing manual challenge flow unaffected
- [x] [SIL:1] **DONE S62** Beat-sync spawn particles — `sounds.js` exports `getMusicBPM()` and `getMusicBeat()`; beat-aligned spawns (±4 frames) trigger a 6-particle burst, adding perceptual music sync with zero balance impact
- [ ] [SIL:2] [BLOCKER S61] [S60 follow-up · narrowed S112] Update PostHog/Sentry/Ko-fi dashboard URL allowlists for `https://callofdoodie.wtf/` — the Supabase half is CLOSED with evidence (all five edge functions ship `Access-Control-Allow-Origin: *` in code; live OPTIONS on `sync-studio-events` with `Origin: https://callofdoodie.wtf` returns 200, verified S112). Remaining half stays credential-gated: `node scripts/check-secrets.mjs --for analytics` MISSING, and PostHog/Sentry aren't wired until `VITE_POSTHOG_KEY`/`VITE_SENTRY_DSN` exist.
- [x] [SIL:2] **DONE S61** [S60 follow-up] Finish post-cutover routing — verified `www.callofdoodie.wtf` SSL/domain readiness, added Cloudflare Pages middleware redirects, deployed to Pages, and confirmed `www.callofdoodie.wtf`, `playcallofdoodie.com`, and `www.playcallofdoodie.com` now return 301 to `https://callofdoodie.wtf/`.
- [x] [SIL:2] **CLOSED S112 (duplicate row)** — exact duplicate of the S61 allowlist item above (see the narrowed S112 row); tracked there.
- [x] [SIL:1] **DONE S61** [S60 follow-up] Add old-path redirect in `VaultSparkStudios.github.io` from `/call-of-doodie/*` to `https://callofdoodie.wtf/*` — committed and pushed sibling repo commit `a6515ae` with `call-of-doodie/index.html` canonical/meta redirect plus `404.html` `/call-of-doodie/*` guard, leaving unrelated sibling repo worktree changes untouched.
- [ ] [SIL:2] [S60] Supabase Auth / Studio membership implementation decision — if paid tier or membership integration is now desired, implement `docs/AUTH_INTEGRATION_PLAN.md` instead of leaving membership server-only
- [x] [SIL:2] **DONE S113 (bounded edge wiring)** [S59 carryover] validate-replay Phase 2B — edge validator now consumes deterministic slice receipts (contract, movement/aim, combat actions, derived contact enemy) under the existing advisory gate. Full physics parity remains future work, but the browser slices are no longer edge-invisible.
- [x] [SIL:1] **DONE S61** [S59 carryover] App.jsx extraction slice 11 — extracted enemy projectile/player hit resolution, incoming damage math, contact-hit helpers, and grenade explosion damage into `src/systems/combatResolution.js`; wired enemy bullet hits and grenade blast damage through pure helpers in `src/App.jsx`; added regression tests.
- [ ] [Human] [SIL:2] Manual browser QA pass against `docs/QA_CHECKLIST.md` to confirm S55 GIF + white-card + lag fixes hold under real clicks (CLI cannot drive the browser)
- [ ] [Human/Data] [SIL:2⛔] HomeV2 Lighthouse measurement — capture real LCP/CLS deltas vs legacy MenuScreen on production, confirm ≥200ms LCP improvement before removing v1 fallback
- [ ] [Human/Data] [SIL:1] HomeV2 analytics funnel — compare `home_v2_deploy` vs legacy `front_door_action` completion rates after 48h of traffic

## Next
- [x] [SIL:2] **DONE S84** Dev-toolchain vulnerability remediation — package-trust false positive for exact `vitest` was fixed in Studio Ops, then Call-Of-Doodie upgraded to `vitest@4.1.8`, `@vitest/coverage-v8@4.1.8`, `vite@7.3.5`, `@vitejs/plugin-react@5.1.4`, and exact npm overrides for patched transitive packages. Validation: `npm audit --json` 0 vulnerabilities, tests/build/e2e green.
- [x] [SIL:3] **DONE S84** Playwright pointer 360 harness — `@playwright/test@1.60.0`, `playwright.config.ts`, and `tests/pointer-360.spec.ts` now drive the real canvas through `?debug=input` and assert the debug HUD reaches `pointer:4/4`.
- [x] [SIL:2] **DONE S84** Adaptive enemy difficulty curve — repeated killer history now feeds spawn dampening and the run-start "Adapted for you" feedback chip in the current worktree.
- [x] [SIL:1] **DONE S79** Device-specific control hint labels — remembered `gamepad.js` controller profile now feeds Xbox/PS/generic labels in TutorialOverlay + Command Center/Pause controls.
- [x] [SIL:2] **DONE S84** Enemy-annotated death heatmap, bounded pass — the final ghost-path death marker now carries and renders the killer type on DeathScreen; multi-death cluster centroid grouping remains a richer follow-up layer if needed.
- [x] [SIL:2] **DONE S76** Real device input lab — `?debug=input` now shows gamepad identity, slot, source, stick axes, aim angle, pointer position, action state, and trace counts for Xbox/mouse/touch QA.
- [x] [SIL:2] **DONE S84** Pointer 360 browser regression — Playwright enters a live run, sweeps pointer aim around the canvas, and proves all four pointer buckets through the debug HUD.
- [ ] [SIL:2] Supabase Auth + Obelisk account bridge — implement magic-link/Google Auth, profiles, nullable leaderboard `user_id`, guest-to-account migration, and signed migration/callsign claim receipts.
- [ ] [SIL:1] First-session control calibration — add an optional short target-ring calibration to detect drift and verify full-circle aim before the first serious run.
- [x] [SIL:2] **DONE S68** Replay command trace contract — client trace binding already shipped in S67; S68 updates `validate-replay` to accept `traceDigest`/`traceLength` as a trace-backed replay contract and updates `submit-score` to reject malformed trace metadata before leaderboard insert.
- [ ] [SIL:1] HomeV2/MenuScreen retirement gate — once Lighthouse + funnel data confirms HomeV2 wins, remove the legacy `?home=v1` path and reclaim the `MenuScreen` chunk
- [ ] [SIL:2] Supabase Auth integration per `docs/AUTH_INTEGRATION_PLAN.md` — magic-link + Google OAuth, profiles table, leaderboard user_id backfill, grace-period dual-path on submit-score; trigger when traffic warrants or paid tier ships
- [ ] [SIL:1] App.jsx extraction roadmap per `docs/APP_EXTRACTION_ROADMAP.md` — slice 1 is the game loop (player/bullet/enemy update); pure `step(gs, frame)` modules with their own tests
- [x] [SIL:1] **DONE S67** Weapon unlock telemetry — `handlePlayerDeath` detects newly unlocked weapons after account-level changes and emits `track("weapon_unlock", {...})`; future balance work needs real traffic, not more local wiring.
- [x] [SIL:2] **DONE S69** Trace body storage contract — compact command trace bodies now reach `submit-score`, are checked against digest/count, and are retained in member session metadata only; deterministic resim still needs the runner itself.
- [x] [SIL:2] **DONE S55** GIF highlight encoder — non-blocking palette reuse + 36-frame cap + 6-frame yields + skip on mobile/PERF mode
- [x] [SIL:2] **DONE S55** First-card readability — PerkModal tier pill black-on-solid + DraftScreen 6-char hex + dark card body
- [x] [SIL:2] **DONE S55** Frame-budget adaptive quality — `window.__codReducedEffects` flips on sustained drops; particle render halved; HUD `⚡ PERF MODE` chip surfaces it; GIF capture skipped while active
- [x] [SIL:1] **DONE S55** SettingsPanel Quick / Advanced split + Reset-to-defaults button
- [x] [SIL:2] **DONE S55** Weapon unlock progression — `WEAPON_UNLOCK_LEVELS` + grandfathered legacy loadouts in builder
- [x] [SIL:1] **DONE S55** Mutation banner dismissible (sessionStorage) and roadmap docs (auth plan, extraction plan, QA checklist) committed
- [x] [SIL:2] **DONE S56** Parody disclaimer footer added to `HomeV2.jsx` + `MenuScreen.jsx` (Activision/Call of Duty&reg; non-affiliation language) — closes the trademark-dilution risk lane that the parody name exposed; the footer is the textbook safe-harbor pattern
- [x] [SIL:1] **DONE S56** Domain selection scored — `.wtf` (49/60) chosen as canonical, `playcallofdoodie.com` (47/60) chosen as 301-target hedge, `.win` (35/60) rejected as spam-tainted TLD; rationale in `docs/DOMAIN_MIGRATION_PLAN.md`
- [x] [SIL:1] **DONE S56** Hosting evaluation — Cloudflare Pages (57/60) chosen over Vercel/Netlify on bandwidth-cap headroom (CF Pages free tier is unlimited; Vercel/Netlify cap at 100GB); Itch.io retained as a secondary distribution channel rather than the canonical home
- [ ] Optional: Ko-fi → leaderboard end-to-end test once the webhook is live and a real donation flows through
- [x] [SIL:1] App-runtime architecture pass, slice 10 — **DONE S54**: extracted run-start/death/score-submit session flow helpers to `src/systems/runSession.js`, reducing more of the lifecycle bookkeeping burden in `src/App.jsx`
- [x] [SIL:1] Replay loop hardening — **DONE S54**: Run History now turns rivalry rows, featured seeds, ghost-board cards, and seeded history into direct replay/challenge actions instead of passive summaries
- [x] [SIL:1] Launch readiness tooling — **DONE S54**: added `npm run launch:assets` for PNG exports of the launch stills, `npm run launch:readiness` for the current asset/telemetry/human-gate summary, and surfaced measurement status on HomeV2
- [x] [SIL:1] Studio event queue server sync — **DONE S53**: browser-local Studio events now carry queue metadata, sync through `sync-studio-events`, surface sync health in Run History trust ops, and deploy via migration `2026-04-22_studio_game_events.sql`
- [x] [SIL:1] Roast Director runtime completion — **DONE S53**: completed remaining live hooks (`wave_clear`, `perk_chosen`, `coin_milestone`, `death`) and cleared the stale note that `near_death` / `first_blood` / `low_ammo` were still unwired
- [x] [SIL:1] Economy clarity pass, slice 2 — **DONE S51**: route forecasting (routeForecast.js + RouteSelectModal hover panel) + shop tradeoff language (shopForecast.js + WaveShopModal hover advisories, urgency-rated by game state); reroll/lock mechanics deferred as a game design decision
- [x] Social retention layer — **DONE S52**: `socialRetention.js` now drives weekly contracts, featured seeds, revenge links, fresh ghost cards, and async competition prompts in `RunHistoryPanel`
- [x] Social rivalry loop — **DONE S52**: rivalry streaks, featured seed cards, revenge links, and ghost-board summaries now surface stored rivalry data as a visible player-facing loop
- [x] Telemetry/balance loop — **DONE S52**: normalized local Studio events now capture perk picks, route picks, first-death wave, abandonments, score-submit results/rejections, and weekly contract progress
- [ ] [SIL:1] Replace launch-asset SVG placeholders with real PNG gameplay screenshots — improves Itch.io listing fidelity and Chrome install-card presentation (human action: screenshot capture)
- [x] Meta clarity pass — **DONE S51**: `metaClarity.js` + wired into `menuGuidance.js`, HomeV2, MenuScreen; career-weakness-targeted upgrade recommendations in front-door action stack
- [x] Security/trust v2 ops surface — **DONE S52**: `RunHistoryPanel` now exposes trust-op counts, rejection summaries, anomaly guidance, and score-submit outcome telemetry backed by normalized local Studio events
- [x] App-runtime architecture pass, slice 8 — **DONE S51**: pickup spawning extracted to `src/systems/pickupSpawning.js` (pure fn + `getPickupWeights`); App.jsx wrapper collapses to 2 lines; 11 tests
- [x] App-runtime architecture pass, slice 9 — **DONE S52**: boss-wave preview/spawn planning extracted to `src/systems/bossWaveFlow.js`; `App.jsx` now consumes a boss-wave plan instead of owning the full inline preview/spawn decision tree
- [x] Roast Director — **DONE S51**: `src/utils/roastDirector.js` with 10 event pools, per-event wave-cooldown rate limiting; wired in game loop at kill_streak (every 5 kills) and boss_kill; 12 tests
- [x] Studio Hub/Social Dashboard event contract — **DONE S52**: `buildStudioGameEvent()` now emits contract v2 payloads across front-door, trust, telemetry, debrief, rivalry, and weekly-contract surfaces for downstream Studio OS consumers
## Done
- [x] [SIL:2] **DONE S90** Replay proof presenter + innovation-pack command — DeathScreen replay proof composition now lives in a pure tested presenter; submission feedback proof readouts reuse the same trust language; `ops.mjs innovation-pack` generates `docs/INNOVATION_PACK.md`; protocol drift reports 20/20 present.
- [x] [SIL:3] **DONE S89** Replay proof receipt — trace evidence now produces a player-facing proof receipt, valid submissions carry `traceReceipt`, and DeathScreen shows proof score, proof lines, and next action for every evidence level.
- [x] [SIL:2] **DONE S89** Replay proof trend — local run history stores compact proof receipts, DeathScreen aggregates last-10 proof quality, and generated score share cards include a replay proof stamp.
- [x] [SIL:2] **DONE S89** Replay pressure/fixture parity — browser-side replay pressure math is exported as `buildReplayPressureProfile()`, shared rich/basic/weak/malformed fixtures are available, and `scripts/validate-replay-trace-fixtures.mjs` validates fixture evidence plus pressure-profile parity.
- [x] [SIL:4] **DONE S60** Standalone-domain custom cutover — Cloudflare zones created for `callofdoodie.wtf` + `playcallofdoodie.com`; Namecheap nameservers set to `journey.ns.cloudflare.com` + `piers.ns.cloudflare.com`; Pages custom domains attached; Namecheap parking A record removed; apex + backup DNS CNAMEs point at `call-of-doodie.pages.dev`; `https://callofdoodie.wtf/` live-site check passed 5/5.
- [x] [SIL:1] **DONE S60** Studio URL source-of-truth repair — `context/PROJECT_STATUS.json`, `context/STUDIO_MANIFEST.json`, `context/runtime-pack/RUNTIME_PACK.json`, and `docs/STARTUP_BRIEF.md` now advertise `https://callofdoodie.wtf/` so website/portfolio agents can discover the live URL.
- [x] [SIL:4] **DONE S59** Standalone-domain migration implementation — root-scoped Cloudflare Pages build support, canonical `callofdoodie.wtf` metadata/share/PWA updates, runtime-scoped service worker, Cloudflare Pages workflow, Pages security headers, cutover runbook, Cloudflare/Namecheap platform automation scripts, and live deployment to `https://a660c406.call-of-doodie.pages.dev/`. Custom apex remains blocked by Cloudflare zone-create permission + Namecheap NS gates, not by repo code.
- [x] [SIL:1] **DONE S59** Auth/Studio membership audit — confirmed no public create-account/sign-in UI exists today; callsign + local anon UUID remains active identity; Studio membership recognition exists only server-side when an authenticated Supabase `uid` is present.
- [x] [SIL:5] **DONE S58** Founder-requested all-items refinement pass — deterministic combat-resolution foundation (`src/systems/combatResolution.js` now owns bullet/enemy overlap, crit, shield-facing multiplier, lightning target selection, pierce, and obstacle bounce helpers); objective mastery chains + four new achievements; Heat Meter visual overlay; zero-token Run Brain; Run History Bounty Board; first-three-run HomeV2 onboarding; replay-contract confidence in `validate-replay`; SettingsPanel hook cleanup; legacy MenuScreen lazy loading. Validation: 315/315 tests, lint clean, build passing. Bundle: main chunk trimmed to 730.41 kB raw / 222.57 kB gzip after splitting MenuScreen.
- [x] [SIL:5] **DONE S57** 12-item depth + retention sweep — Best-Moment GIF fix + Web Worker offload (`src/workers/gifEncode.worker.js`); Daily Crown 👑 banner + leaderboard badge (`getDailyChampion`, HomeV2, LeaderboardPanel); HUD Density preset Min/Std/Tac (`hudFlags`, SettingsPanel); adaptive enemy telegraphing (`recordDeathByEnemy` + `getTelegraphMultiplier`, BulletRing/GroundSlam timers widen 1.5x–2x); Heat Meter (`src/systems/heatMeter.js` — replaces combo-driven music tier); AI Run Coach 3-line debrief (`src/utils/runCoach.js` composing metaClarity + runDebrief + recent killer); 12-char Replay Codes (`src/utils/replayCode.js` with mod-16 checksum, paste-to-load + share on HomeV2); scoreLedger + combatResolution scaffold (`src/systems/scoreLedger.js` used at both kill sites for hot-zone bonus, `combatResolution.js` ships pointInCircle/dist2); Dynamic Objective System (`src/systems/objectiveDirector.js` — Hot Zones / Bounty / Sniper / Lockdown / Escort, weighted by `metaClarity.identifyWeakness`, drawn in `drawGame.js`); Doodie Pass Lite cosmetic track (`src/utils/cosmeticTrack.js` — 10 cosmetics × 4 weeks, free vs supporter); validate-replay Edge Function heuristic phase (`supabase/functions/validate-replay/`, deploy YAML); skill-cost telemetry (`scripts/log-skill-cost.mjs`). Tests: 303/303 (was 248; +55 new across 7 modules). Lint: 0 errors.
- [x] [SIL] Session 50 NOW bucket — HomeV2 intel ticker sessionStorage persistence, CareerStatsPanel "—" placeholder for pre-49 saves, Command Center gamepad nav (useGamepadNav + focus rings), `scripts/schema-lint.mjs` (`npm run schema:lint`), balance telemetry v2 (`shop_buy`, `coin_shop_buy`, `mode_abandon`, `debrief_view_leaderboard`, `debrief_menu` events), user feedback clarity (local-save fallback + rejection copy), bundle split cleanup slice 3 (PauseMenu + PerkModal + WaveShopModal + RouteSelectModal + TutorialOverlay + DraftScreen all lazy), front-door simplification (first-run stripped action stack + collapsible Command Center auto-expanded for returning players), level-flow cadence slice 2 (every-3-levels through level 18 + `getWaveSurvivalBonus` + `getPerkBankingState`), build identity depth (DOCTRINE FORGED milestone + `getDoctrineMilestones()` + irreversible-fork doctrine text per archetype), App.jsx domain refactor slice 7 (`src/systems/mutationResolution.js` + wired into App.jsx), leaderboard trust v2 (`scripts/leaderboard-audit.mjs` + `npm run leaderboard:audit`), render/update optimization (frame budget monitor in `useGameLoop.js` dev-only — warns on >16ms frames).
- [x] [SIL:3⛔] Run Intelligence Spine — reusable intelligence foundation shipped across front door, post-run diagnosis, rematch/rivalry prompts, balance telemetry, Studio event shape, and trust digest slices
- [x] [SIL:3⛔] Integrated refinement tranche — coordinated slices shipped across App extraction, coaching, telemetry, trust, performance, rivalry, roast/personality, and protocol repair
- [x] [SIL] Run Intelligence Spine, slice 2 — menu recommendations now consider recent run history and unresolved rivalry losses instead of only current mode/loadout context
- [x] [SIL] Leaderboard trust v3, slice 1 — compact event digest upgraded to v2 timeline bands, and the submit Edge Function validates timeline coherence before accepting leaderboard rows
- [x] [SIL] Studio Hub/Social Dashboard event contract, slice 1 — local `vaultspark.game-event.v1` event persistence now captures front-door and debrief intelligence events for downstream Studio surfaces
- [x] [SIL] Rivalry network, slice 1 — local rivalry history records seed, target score, win/loss, delta, mode, difficulty, and wave, then feeds rematch recommendations back into the menu
- [x] [SIL] App-domain extraction, slice 6 — score/session submission now routes through `buildSessionSubmission`, keeping the digest-aware leaderboard payload out of `App.jsx`
- [x] [SIL] Run Intelligence Spine, slice 1 — `src/utils/runIntelligence.js` now centralizes menu recommendations, post-run diagnosis, rivalry prompts, compact trust digests, Studio event shape, and a first rule-based roast callout layer
- [x] [SIL] Integrated refinement tranche, slice 1 — menu run intel, death-screen run intelligence, debrief telemetry, event-digest score submission, DeathScreen code-splitting, and startup/action-queue protocol repair shipped together
- [x] [SIL] Protocol repair — `scripts/ops.mjs action-queue`, `scripts/ops.mjs blocker-preflight`, `scripts/render-startup-brief.mjs`, and `scripts/validate-brief-format.mjs` now exist and pass local dry runs
- [x] [SIL] Bundle split cleanup, slice 2 — `DeathScreen` is lazy-loaded from `App.jsx`; production build now emits a separate `DeathScreen` chunk and reduces the main app chunk
- [x] Phase 1 launch validation — live Edge Function health check added at `scripts/health-check.mjs` and passed against production (`issue-run-token` + `submit-score`)
- [x] Phase 1 launch validation — minimal launch smoke test added at `src/App.launch.test.jsx` and passing in the full suite
- [x] Phase 1 live QA (terminal-verifiable) — repeatable live site checks added at `scripts/live-site-check.mjs`
- [x] Phase 1 live QA (terminal-verifiable) — `npm run launch:qa` passes against the live backend and deployed site shell/PWA assets
- [x] Source-controlled the Cloudflare Worker / CSP config used by the live deployment path in `cloudflare/`
- [x] Launch execution package prepared in `docs/LAUNCH_EXECUTION.md` with screenshot shot list, Itch.io copy, launch sequence, and telemetry decision
- [x] Added `npm run launch:verify` to combine launch smoke, live function checks, and live site verification
- [x] Added launch-surface visibility checks via `scripts/launch-surface-check.mjs`
- [x] Added shared leaderboard audit via `scripts/shared-leaderboard-check.mjs`
- [x] Prepared a ready-to-upload launch media pack in `public/launch-assets/`
- [x] Verified launch-surface visibility on production: homepage, sitemap, live page branding, and `/games/` all reference Call of Doodie
- [x] Verified readable shared-table state on production: no non-`cod` rows found in the latest 200 leaderboard entries
- [x] Confirmed Edge Function redeploy succeeded (deploy-supabase-function.yml — last run 2026-04-02, success)
- [x] Validated live leaderboard submit end-to-end: `npm run health:check` → 5/5 assertions passed against production
- [x] Gameplay smoke test — `src/gameHelpers.test.js` (26 tests: spawnEnemy wave 1–3 logic, spawnBoss, BOSS_ROTATION, mutation flag propagation)
- [x] Wired VITE_POSTHOG_KEY + VITE_SENTRY_DSN into deploy.yml build env
- [x] PWA manifest screenshots populated — 5 screenshots added to `public/manifest.json` (4 wide/desktop, 1 narrow/mobile)
- [x] `apple-mobile-web-app-title` added to `index.html` for correct iOS home screen label
- [x] [SIL] PNG icon build pipeline — `scripts/generate-icons.mjs` (sharp), `icon-192.png` + `icon-512.png` committed, manifest + index.html wired, sw.js bumped to `cod-v4`, `prebuild` hook ensures PNGs stay in sync with `icon.svg`
- [x] [SIL] Ko-fi webhook Edge Function — `supabase/functions/kofi-webhook/index.ts` with verification-token validation, idempotent `kofi_events` audit log, callsign extraction from Ko-fi `message`, auto-deploy wired into `.github/workflows/deploy-supabase-function.yml`, migration `2026-04-14_kofi_webhook.sql` added
- [x] CI stability — raised Vitest `testTimeout` to 15000ms in `vite.config.js` after observing launch-smoke variance (1.2s–5.5s) near the 5s default
- [x] Session 42 audit recorded in `docs/IMPROVEMENT_PLAN.md` with ranked implementation order for trust, UX, depth, speed, and architecture
- [x] Session 42 execution slice expanded: perk-driven build archetypes now unlock capstone bonuses, and perk/shop/route surfaces now mark build-fit recommendations
- [x] [SIL] Score plausibility validation in Edge Function — reject runs whose kills/damage ratios exceed realistic ceilings for the reported wave
- [x] [SIL] Upgrade the death screen into a tactical debrief — show build identity, strengths, and next-step guidance after each run
- [x] [SIL] Add menu-side command briefing — surface mode/loadout strategy and weekly mutation context before deploy
- [x] [SIL] Build identity layer — archetypes/capstones so runs feel more memorable than “more perks”
- [x] [SIL] Economy clarity pass, slice 1 — perk/shop/route screens now signal build fit instead of presenting flat choices
- [x] [SIL] Session 43 architecture/perf groundwork — extracted perk/route helper logic from component files, lazy-loaded non-core menu panels, fixed RouteSelect focus outline bug, and reduced UI-layer warning debt
- [x] [SIL] Session 43 closeout — cleared the remaining `src/App.jsx` lint warnings; validation baseline is now lint-clean (`0` warnings / `0` errors)
- [x] Session 43 audit expansion — combined refinement roadmap now explicitly ranks architecture split, front-door simplification, pacing/readability, social rivalry, telemetry, trust, and bundle cleanup as one quality program
- [x] [SIL] Front-door simplification, slice 1 — menu now leads with a recommended-next-action card plus Daily Challenge, Challenge Friend, and a progressive `Command Center` reveal for deep systems
- [x] [SIL] Front-door action stack — `Play Now`, `Daily Challenge`, `Best Next Upgrade`, and `Challenge Friend` now run through a reusable priority stack backed by `src/utils/menuGuidance.js`
- [x] [SIL] Bundle split cleanup, slice 1 — converted App/Pause/Death panel imports to real `lazy()` boundaries so the build now emits separate Achievements, Settings, and Leaderboard chunks
- [x] [SIL] App domain extraction, slice 1 — wave shop and coin shop option generation moved from `src/App.jsx` into `src/systems/shopOptions.js`
- [x] [SIL] App domain extraction, slice 2 — leaderboard/run-claim payload shaping moved into `src/utils/runSubmission.js` instead of staying embedded in `src/App.jsx`
- [x] [SIL] App domain extraction, slice 3 — progression reward sequencing now routes through `src/systems/progressionFlow.js` instead of being hand-wired inline in `src/App.jsx`
- [x] [SIL] App domain extraction, slice 4 — perk synergy resolution and archetype capstone effects now live in `src/systems/perkResolution.js` instead of being embedded inside the perk-pick callback
- [x] [SIL] App domain extraction, slice 5 — regular shop and coin-shop consequence rules now live in `src/systems/shopResolution.js` instead of long inline `switch` blocks in `src/App.jsx`
- [x] Public-facing copy refresh — README, launch/store copy, manifest metadata, OG preview text, and in-product share copy now match the current shipped feature set
- [x] Closeout wrapper hardening — `scripts/closeout-autopilot.mjs` now uses argv-based git execution and top-level error handling so non-interactive closeout runs finish their cleanup/status path reliably
- [x] [SIL] Wave director pacing — non-boss waves now run through scouting/pressure/climax/recovery pacing plans with alive-budget-aware spawn cadence, telegraphed elite spikes, and clearer incoming-wave identity
- [x] [SIL:2⛔] Post-run coaching v2 — death screen now includes collapse reason, missed-value hints, and seeded corrective rematch guidance
- [x] [SIL] Boss-wave anticipation pass — boss preview/cutscene layer now includes concrete dodge verbs and escort-pressure guidance
- [x] [SIL] Director telemetry hooks — wave-director stage transitions and pressure bands now emit telemetry snapshots for future tuning
- [x] [SIL] Trust v2 groundwork — `issue-run-token` now returns a signed run summary claim, `submit-score` validates it, and optional anomaly logging is wired via `supabase/migrations/2026-04-15_run_anomalies.sql`
- [x] [SIL] Score-submit clarity pass — rejected competitive runs now report server reasons instead of being mislabeled as local-save fallbacks
- [x] [SIL] Build-identity milestone pass — HUD/perk/shop/route surfaces now explain doctrine state and the next archetype milestone
- [x] [SIL] Debrief follow-through telemetry — replay/copy actions and score-submit outcomes now emit analytics events
- [x] [SIL:1] Combat readability pass, slice 1 — dangerous enemies now get stronger contrast rings, threat brackets, and ranged prefire telegraphs in `src/drawGame.js`
- [x] [SIL:1] Level-flow cadence pass, slice 1 — perk rewards are now banked during combat, opened at the next wave-clear safe point, exposed in the HUD, and shifted onto a softer perk-breakpoint curve via `src/utils/levelFlow.js`
- [x] Protocol sync — start/closeout prompts, prompt templates, mode/secrets helpers, and local closeout wrapper now align with the latest Studio OS/ops workflow without pointing at missing repo-local commands
- [x] [SIL] **DONE S101** Full `/audit` implementation sweep — shipped all 12 items from `docs/AUDIT_2026-06-18_3.md`: visitor-safe ops copy, release security header gate, Obelisk verify endpoint, legacy home retirement gate, Journey card/front-door clarity, Aim Check control rite, DeathScreen next-run drill bridge, local Balance Lab, HUD debug collision overlay, Rival Pace ghost chip, screenshot truth pack with verified captures, and DeathScreen prop extraction. Validation: full suite 540/540, build passing, release security gate with npm audit 0 vulnerabilities, launch media gate passing.

## Deferred
- [ ] Discord invite/community link when the community entry point is ready
- [x] [SIL:2] Obelisk verify backend — `/api/obelisk-verify` now exists as a Cloudflare Pages Function proxy with redacted receipts and honest not-configured behavior; guest play remains default.
- [x] [SIL:3] **DONE S67** App.jsx extraction slice 1 — `gameStep.js` exports `computeMovementVector` + `applyPlayerMovement`; 11 tests; 347/347 passing.
- [ ] [SIL:2] [S62 deferred] MenuScreen → MenuPanels.jsx unification — ~900 duplicated lines; pure refactor with no user-facing impact; HomeV2 is the default (v1 opt-in only), so deferral has minimal player impact
- [ ] [SIL:2] [S62 deferred] Coordinated enemy formations — wave 20+ group-spawn patterns (flanks, pincer, escort); game design work (~4h) beyond current scope
- [ ] [SIL:1] [S62 deferred] Mid-run challenge contracts — optional per-wave objectives for bonus coins beyond Dynamic Objective System; needs design pass to avoid overlap
- [x] [SIL:1] **DONE S68** [S62 deferred] Input-timeline digest contract — compact command trace metadata now reaches the edge contract (`traceDigest` + `traceLength`); deterministic resim remains a separate runner/storage milestone.
- [ ] [SIL:1] [S62 deferred] HomeV2 v1 fallback retirement — gate on ≥200ms Lighthouse LCP improvement confirmed on production (human measurement required)
- [ ] [SIL:1] [S101 follow-up] Full five-scene screenshot replacement — two verified browser captures now exist; replace all five manifest screenshots with verified PNG captures after combat, boss, build/debrief, leaderboard, and mobile scenes are captured.
- [x] [SIL:1] [S101 follow-up] **DONE S111** Death-submit extraction slice 2 — `deathFlow.js` now owns DeathScreen props plus debrief/score-submit fallback Studio event planning through tested pure builders.

## Deferred to Project Agents

- cross-repo item owned by another repo agent:
- [x] [SIL:2] **DONE S107** Deploy gate repair — `scripts/render-startup-brief.mjs` now normalizes plain `generate-genius-list --brief` output into a canonical boxed `GENIUS HIT LIST`, always renders a `HUMAN PRESSURE` tile with an honest empty state, and GitHub Actions has the missing Cloudflare Pages deploy secrets restored from the Studio Ops gateway. Validation: brief validator, lint, 550/550 tests, and build passing.
