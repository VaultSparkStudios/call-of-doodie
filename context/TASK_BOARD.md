# Task Board

## Now
- Playtest and balance session 4 additions (new weapons, enemies, obstacles)

## Next
- Global leaderboard via Supabase free tier (biggest retention driver currently missing)
- Enemy pathfinding around obstacles (currently enemies walk through walls)
- End-of-run summary screen (show wave, kills, damage dealt, perks taken, missions)

## Later
- Capacitor wrapper for iOS App Store submission
- PWA setup (manifest + service worker) for "Add to Home Screen"
- Gamepad / controller support
- Background music (procedural loop via Web Audio API)
- App.jsx game loop extraction to a custom hook (`useGameLoop`)
- Run seed sharing (seed visible on death screen, shareable for comparison runs)
- Boss shield mechanic (was in a reverted PR — candidate for re-implementation)
- More weapon types (e.g. shotgun spread, boomerang)
- More daily mission types
- Colorblind mode
