<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-15 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 2
- Combined Priority: 116.8
- Top 3: startup-brief-canonical-boxes · startup-brief-regression-harness

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed / organization / efficiency | 1h | 9 | 8 | 86.4 | **startup-brief-canonical-boxes** — The current /start renderer emits the generated genius list as a raw numbered list when `generate-genius-list.mjs --brief` does not return a box, so `validate-brief-format` fails after the official repair path. The fix is a renderer-level normalization: accept either boxed or plain generator output, but always write a canonical `GENIUS HIT LIST` tile and a `HUMAN PRESSURE` tile. **Recipe:** Add a normalization wrapper in `scripts/render-startup-brief.mjs` that detects unboxed genius stdout and wraps its first lines inside `top('GENIUS HIT LIST')` / `row()` / `bot()`. Render a HUMAN PRESSURE tile even when no compiled pressure file exists, using an explicit no-pressure row instead of omitting the canonical block. |
| 2 | ⚡ | security | 45m | 8 | 4 | 30.4 | **startup-brief-regression-harness** — Session 80 previously proved `validate-brief-format` passed, but today's renderer drift reappeared because the generator stdout shape was not covered. A small focused test or script check should prove that plain numbered recommendations are boxed and validate successfully. **Recipe:** Add a lightweight regression check for the normalization helper or rendered output, then verify with `node scripts/render-startup-brief.mjs`, `node scripts/validate-brief-format.mjs docs/STARTUP_BRIEF.md`, `npm test`, lint, and build. |

## Innovation reserve

- startup-brief-canonical-boxes

## Skipped

- full-deterministic-resim-runner — Still important for replay trust, but current /start protocol failure must be repaired first because it blocks reliable unattended sessions.
- new-gameplay-content-pack — The game already has strong content depth; session evidence points to launch-hygiene automation as the highest leverage current gap.
- home-v2-v1-retirement — Still gated by real Lighthouse and funnel evidence per existing decisions, so code removal would be premature.

## Execution Log

- startup-brief-canonical-boxes: shipped — Renderer now normalizes raw genius-list stdout into the canonical `GENIUS HIT LIST` box and always emits `HUMAN PRESSURE` with an honest empty state when no compiled pressure queue exists. Evidence: `node scripts/render-startup-brief.mjs` passed; `node scripts/validate-brief-format.mjs docs/STARTUP_BRIEF.md` passed; `npm test` passed 489/489; `npm run lint` completed with 0 errors / 7 baseline warnings; `npm run build` passed.
- startup-brief-regression-harness: shipped — Focused regression coverage now protects plain, boxed, and empty genius-list output plus both human-pressure paths. Evidence: `npx vitest run tests/startup-brief-boxes.test.js` passed 5/5 and full validation stayed green.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
