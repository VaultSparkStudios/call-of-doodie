<!-- generated-by: /arc Codex session 115 -->
<!-- generated-at: 2026-07-02 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie — Session 115

Live-code audit found the primary startup list still dominated by dashboard, device, or production-data gates. The next repo-executable product slice was the REMATCH drill L3 follow-up from Session 114: the game already launched practice rematches safely, but the live practice run did not carry the death-screen coaching reason into the heads-up display (HUD) or expose a mastery receipt.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | FIRE | game loop / retention | 1h | 7 | 5 | 18.9 | **rematch-drill-l3-coach-receipts** — Carry the death-screen coaching reason into REMATCH practice runs and show best-of-3 mastery receipt state in the live HUD without changing leaderboard trust. |

## Execution Log

- rematch-drill-l3-coach-receipts: shipped — `src/systems/rematchDrill.js` now builds a practice drill brief plus best-of-3 mastery receipt labels; `DeathScreen` passes the selected `nextRunDrill` into REMATCH starts; `App.jsx` stores that brief on the practice game state; `HUD.jsx` renders a compact REMATCH chip during practice runs.

## Validation

- `npx vitest run src/systems/rematchDrill.test.js` — 12/12.
- `npx eslint src/App.jsx src/components/HUD.jsx src/components/DeathScreen.jsx src/systems/rematchDrill.js src/systems/rematchDrill.test.js --max-warnings 0` — clean.
- `npx vitest run src/systems/rematchDrill.test.js src/App.launch.test.jsx` — 13/13.
- `git diff --check` — clean.

## Honest Deferrals

- Analytics/dashboard allowlists remain provider-credential gated.
- Physical PWA install and real gamepad QA remain device-evidence gated.
- HomeV2 Lighthouse/funnel analysis remains production-data gated.
- MenuScreen/MenuPanels full unification remains a larger coverage-first refactor.
