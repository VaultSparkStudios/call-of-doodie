<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-21 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 132.7
- Top 3: trace-evidence-submission-loop · trust-ops-trace-surface · edge-trace-quality-receipts

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | feedback loop / replay trust / AI-intelligence | 1h | 9 | 8 | 55.4 | **trace-evidence-submission-loop** — Replay command traces now have evidence quality analysis, but score submission only forwards raw trace fields; the local game cannot remember whether the submitted run had weak, basic, or rich replay evidence. This turns the replay-trust work into visible player/run intelligence without adding model calls. **Recipe:** Have `buildSessionSubmission()` attach compact trace evidence from `analyzeReplayCommandTrace()`; preserve that evidence through `saveToLeaderboard()` result objects so debrief and local Studio events can record it. |
| 2 | 🔥 | UX / trust / gamification | 1h | 8 | 8 | 49.2 | **trust-ops-trace-surface** — The Run History trust ops panel counts submissions and rejections, but it does not show whether recent accepted runs have replay-grade input evidence. Making evidence level visible gives players and operators a reason to care about clean trace capture, and gives the next deterministic resim slice a target dashboard. **Recipe:** Extend `studioEventOps` summaries and `RunHistoryPanel` trust chips to show latest trace-evidence level plus rich/basic/weak counts, with weakness reasons surfaced for weak traces. |
| 3 | ⚡ | security / automation / token-cost discipline | 45m | 7 | 7 | 28.1 | **edge-trace-quality-receipts** — The `submit-score` Edge Function validates trace shape but does not emit a trust receipt describing the trace quality it stored for Studio members. This leaves accepted runs hard to audit later. **Recipe:** Mirror lightweight trace-evidence analysis inside `submit-score`, include `traceEvidence` in member `game_sessions.metadata`, and return it in the success response without adding any external API or token cost. |

## Innovation reserve

- 2 items: `trace-evidence-submission-loop`, `trust-ops-trace-surface`

## Skipped

- Deterministic headless resimulation remains the bigger trust milestone, but it is too large for this audit pass; this sprint builds the evidence plumbing that resimulation will need.
- Supabase Auth and HomeV2 retirement remain data/traffic-gated by existing decisions.
- Physical launch QA, screenshots, and Itch.io publication remain human-device/publication tasks.

## Execution Log

- ✅ `trace-evidence-submission-loop` — shipped. `buildSessionSubmission()` now attaches compact `traceEvidence` from the replay trace analyzer, `saveToLeaderboard()` preserves it across online/rejected/local outcomes, and submit analytics/local Studio events receive the evidence level.
- ✅ `edge-trace-quality-receipts` — shipped. `submit-score` mirrors trace evidence analysis, stores it in Studio member `game_sessions.metadata`, and returns it in the success response.
- ✅ `trust-ops-trace-surface` — shipped. Run History trust ops now summarizes rich/weak trace evidence counts and shows per-event evidence/gap lines.

Validation: focused trace/submission/session/event tests 22/22, `npm run lint` clean, `npm test` 363/363, `npm run build` passing.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
