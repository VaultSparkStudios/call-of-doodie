<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-15 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 14
- Combined Priority: 715.4
- Top 3: boss-grudge-escalation-sound · combo-decay-urgency-audio · run-narrative-share-card

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification / engagement / immersion | 1h | 8 | 8 | 121.5 | **boss-grudge-escalation-sound** — Session escalation (grudge/nemesis tiers at ≥2/3 boss deaths) is text-only — the dialogue changes but no audio cue signals the shift. A 2-note descending 'nemesis recognition' sting wired at the `bossSessionDeathsRef` cr **Recipe:** Add `soundBossGrudge(tier)` to sounds.js — tier 1 = descending minor 3rd (440→330Hz, 0.35s), tier 2 = descending tritone sting (440→311→220Hz). Wire in App.jsx at the bossSessionDeathsRef crossing gate (same location whe |
| 2 | 🔥 | gamification / engagement / immersion | 1h | 7 | 8 | 106.3 | **combo-decay-urgency-audio** — Combo chains are a core tension mechanic but their decay is completely silent. When a 15× GODLIKE streak slips away, only the HUD counter updates — no audio confirms the loss. A ticking urgency sound in the final 30 fram **Recipe:** Add `soundComboTick(framesLeft)` for ticking audio (pitch rises from 220→880Hz as framesLeft drops 30→0, played every 4 frames for combos ≥10). Add `soundComboBreak(count)` descending glide sting fired on reset when coun |
| 3 | 🔥 | gamification / engagement / immersion | 2h | 7 | 8 | 84 | **run-narrative-share-card** — buildRunNarrative() tracks up to 3 turning points per run (LAST STAND, AIM LOCKED, BOSS SLAYER, CHAIN REACTION) but only the act string flows into the share card. The moments array is computed and immediately abandoned.  **Recipe:** 1. Update `runDnaShareCard.js` payload to include `moments` array from buildRunNarrative. 2. In the worker canvas render, add a 3rd section below DNA bar: italicized turning points (icon + label + desc, up to 2 per card) |
| 4 | 🔥 | gamification / engagement / immersion | 3h | 8 | 7 | 72.4 | **enemy-personal-record-tracker** — The MOST WANTED panel shows enemy names and types but no player-specific stats. Career tracks total kills but not per-enemy personal bests. Adding 'most killed in one wave', 'career kills', and 'times killed by this enem **Recipe:** 1. Add `enemyKillBests: {}` to DEFAULT_CAREER (maps typeIndex → {waveMax, careerKills, killedByCount}). 2. Add `updateEnemyCareerStats(typeIndex, waveKills)` to storage.js. 3. Call from App.jsx on wave clear + on player  |
| 5 | ⚡ | gamification / engagement / immersion | 0.5h | 6 | 5 | 68.2 | **wave-respite-visual-cue** — The 120-frame loot lull after high-threat waves exists architecturally (_waveTransitDone/_respiteLock from S88) but has zero visual signaling. Players don't know they're in a breather window, so they keep sprinting and d **Recipe:** In App.jsx, when _waveTransitDone fires and gs.threat >= 4, add `addText(gs, W/2, H/2, '✦ RESPITE ✦', '#88FFCC', 90)`. In drawGame.js, during respite window, add a soft 0.15-alpha pulse ring around pickups (sin(frame/8)  |
| 6 | ⚡ | ai / intelligence integration | 2h | 7 | 8 | 42 | **runbrain-choke-point-coach** — getCommunityChokePoints() identifies waves where ≥3× median players die, and buildRunBrain() generates coaching, but they've never been connected. Wiring them creates the first data-driven pre-wave coaching signal in the **Recipe:** In buildRunBrain(), add `chokeWaves: communityChokePoints` param (optional). Detect if the player's worst wave matches a choke point. Add `chokeWarning` field to the output: { wave, tip }. Surface in DeathScreen Run Coac |
| 7 | ⚡ | speed / organization / efficiency | 1h | 7 | 4 | 35.4 | **run-arc-vignette-gpu-dirty-check** — drawGame.js:1211 calls ctx.createRadialGradient() every frame for the atmospheric run-arc vignette. Acts change at most a handful of times per run (wave ~10, ~25, ~35), yet a new gradient object is allocated at 60fps. S9 **Recipe:** Add `gs._runActPrev = null` to game state init. In drawGame.js:1210, check `if (!_ac || _aa === 0 || gs._runAct === gs._runActPrev) { /* reuse cached */ }`. Cache the gradient as `gs._runActVignetteStyle`. Recreate only  |
| 8 | ⚡ | ui / ux / user-experience | 1h | 5 | 5 | 31.6 | **ammo-depletion-urgency-pulse** — Per-weapon ammo bars use green/yellow/red color coding but are static — there's no motion signal that you're about to run out. Under combat pressure, players miss the color shift. A subtle pulse animation on the red-zone **Recipe:** In HUD.jsx, for ammo bars in red zone (<10%), add a CSS animation class or inline `opacity: 0.7 + 0.3 * Math.sin(Date.now()/200)` on the bar fill. Yellow zone (<30%): slow pulse (400ms period). Red zone (<10%): fast puls |
| 9 | ⚡ | ai / intelligence integration | 2h | 6 | 7 | 31.5 | **enemy-behavior-prediction-layer** — recentDeathsByEnemy and getTelegraphMultiplier exist — the game knows which enemies kill the player most. Wave preview cards show incoming count and event type but not enemy-specific coaching. Adding 'Recent killer on th **Recipe:** In App.jsx wave preview card assembly, check recentDeathsByEnemy for the top recent killer. If that enemy type spawns in the current wave plan, add a 'WATCH FOR: [EnemyName] — [specific tip]' line to the preview card dat |
| 10 | ⚡ | new innovative features | 2h | 7 | 8 | 28 | **run-momentum-sparkline** — DeathScreen shows final stats but nothing about the shape of the run. A 20-pixel-tall sparkline of score-per-wave reveals dramatic swings: a LEGEND run that peaked at wave 12 then collapsed looks totally different from a **Recipe:** Track `gs._waveScoreLog = []` in App.jsx — push current score at each wave clear. On death, pass to DeathScreen. Render a 120×20px inline SVG sparkline using the score array. Normalize to max score. Color: green until pe |
| 11 | ⚡ | new innovative features | 1h | 6 | 7 | 26.6 | **peak-moment-capture** — The game tracks combo maximums but never captures the specific context of a peak moment. Storing a lightweight snapshot at the frame where comboRef.current.count hits its new all-time-this-run maximum (wave, timestamp, e **Recipe:** Add `peakMomentRef = useRef(null)` in App.jsx. When comboRef.current.count exceeds the session best, store { wave: gs.currentWave, count, enemiesAlive: gs.enemies.length, label }. Pass to DeathScreen as peakMoment. Rende |
| 12 | ⚡ | user feedback loop | 2h | 7 | 7 | 24.5 | **cross-weapon-death-coach** — buildWeaponTip() detects weapon usage patterns. buildKilledBy() detects repeat killers. These run independently — there's no cross-reference between 'you mainly used a shotgun' and 'you died to ranged enemies 4× this wee **Recipe:** Create a `buildWeaponDeathCoach(weaponKills, recentDeathsByEnemy)` function in runCoach.js. Map enemy type to its primary threat category (melee/ranged/aoe/chase). Map weapons to their range coverage (shotgun = close, sn |
| 13 | ⚡ | new innovative features | 4h | 7 | 8 | 21.7 | **enemy-intel-cards-most-wanted** — MOST WANTED panel shows static enemy names. Career stats don't track per-enemy intel depth. Adding progressively unlocked enemy intel — quick bio + attack pattern + counter weapon — earned through kill milestones (10/25/ **Recipe:** Define ENEMY_INTEL[typeIndex] = { bio, pattern, counter, unlockAt } in constants.js for all 22 types. Add `enemyKillCounts: {}` to career storage. In updateCareerStats, increment per enemy type from weaponKills. In MostW |
| 14 | ⚡ | new innovative features | 8h | 8 | 9 | 21.7 | **weapon-evolution-branching** — WEAPON_EVOLVED_NAMES gives a linear passive bonus (×1.05 damage) at 1000 career kills. Branching evolution — offering 2 choices at the threshold — transforms weapon mastery from a passive buff into an active identity dec **Recipe:** Define WEAPON_EVOLUTION_BRANCHES[weaponIdx] = [{ name, stat, bonus }, { name, stat, bonus }] in constants.js. At 1000-kill threshold, show a 2-option choice modal (PerkModal pattern). Persist choice to career. Apply in g |

## Innovation reserve

- boss-grudge-escalation-sound
- combo-decay-urgency-audio
- run-narrative-share-card
- runbrain-choke-point-coach
- run-momentum-sparkline
- enemy-intel-cards-most-wanted
- weapon-evolution-branching

## Skipped

- lint-warning-cleanup — Priority 4.5 — too low; warnings are intentional patterns (game loop ref stability, stale closure, fast-refresh). Prefix _var fix is trivial but offers no player value.
- menuscreen-menuPanels-unification — Pure refactor, Priority <10, deferred in S62 TASK_BOARD — no player-facing value per session.
- supabase-auth-integration — Requires credentials + human decision (auth plan decision). Explicitly blocked.
- validate-replay-phase2b — Credential-gated (SUPABASE_ACCESS_TOKEN missing). Explicitly blocked.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
