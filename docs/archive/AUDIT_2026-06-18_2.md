<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-18 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 2
- Combined Priority: 147.7
- Top 3: unicode-safe-model-router-transport · codex-plan-mode-truth

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed / organization / efficiency | 1h | 9 | 7 | 79.5 | **unicode-safe-model-router-transport** — The live `/start` run proved `compact-handoff` can fail before startup finishes when `LATEST_HANDOFF.md` contains malformed UTF-16. JSON escaping alone is not enough because the upstream API rejects lone surrogate code units as invalid Unicode. **Recipe:** Add a recursive Unicode scalar sanitizer in `scripts/lib/model-router.mjs`, use it for Anthropic JSON payload serialization, and cover both lone high/low surrogates and valid emoji preservation with focused tests. |
| 2 | ⚡ | feedback-loop / token-cost | 30m | 8 | 6 | 68.2 | **codex-plan-mode-truth** — The startup verifier currently tells a Codex session to run `/model opusplan`, which Codex cannot execute. That is a lying operational surface: it burns attention, leaves the session lock marked missing, and makes future startup briefs look worse than reality. **Recipe:** Restore `verify-plan-mode.mjs` session-agent detection and `not_required` lock/status stamping for non-Claude Code agents. Add a regression test that runs the script in a temp Codex project and verifies JSON output, `PROJECT_STATUS.json`, and `.session-lock` all agree. |

## Innovation reserve

- unicode-safe-model-router-transport

## Skipped

- disable-compact-handoff-api-call — Rejected on verification: the compactor has a valid API-key path and real token-saving purpose. Disabling it would hide the malformed-Unicode transport bug instead of fixing the shared chokepoint.

## Execution Log

- unicode-safe-model-router-transport: shipped — Added recursive Unicode scalar sanitization at the model-router API chokepoint, reused it for messages/raw/batch payloads and semantic cache keys, and covered malformed high/low surrogates plus valid emoji preservation. Verified with focused Vitest coverage and `node scripts/compact-handoff.mjs --force`, which now succeeds after the original startup failure.
- codex-plan-mode-truth: shipped — Restored session-agent detection in `scripts/verify-plan-mode.mjs` so Codex sessions stamp `not_required` instead of prompting for Claude Code `/model opusplan`. Verified with focused temp-project regression coverage and live `node scripts/verify-plan-mode.mjs --json` returning `not_required` for the current Codex lock.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
