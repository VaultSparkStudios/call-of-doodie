<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-13 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call of Doodie (Session 88)

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 8
- Combined Priority: 319.4
- Top 3: `flow-state-time-dilation` · `boss-grudge-dialogue` · `enemy-combat-taunts`

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 1h | 9 | 10 | 56.8 | **flow-state-time-dilation** — At precision streak ≥10, wire the next precision hit to trigger `gs.timeDilationTimer = 30` (0.5s bullet-time). The precision streak and time-dilation systems exist independently; connecting them creates a mechanical flow-state reward nobody in this genre does. **Recipe:** In `src/App.jsx` bullet-enemy hit handler where `isPrecisionHit()` returns true and `gs.precisionStreak >= 10`, set `timeDilationTimer = 30` + emit `'⚡ FLOW STATE'` floating text in cyan. Guard with a 300-frame `_flowStateCooldown`. |
| 2 | 🔥 | gamification | 1h | 9 | 8 | 45.4 | **boss-grudge-dialogue** — Boss cutscene quotes are static despite `getBossKillRecord()` data sitting in `cod-boss-kills-v1`. Four history-aware variants (intro/taunt/grudge/nemesis) would turn the game's highest-attention narrative moment into a personalized adversarial callback. **Recipe:** Add `BOSS_GRUDGE_QUOTES` in `src/constants.js` per boss type; in `src/App.jsx` `setBossCutscene` call, pick variant based on `_bossRec.kills` and `_bossRec.deaths`; render as `dynamicQuote`. |
| 3 | 🔥 | gamification | 2h | 10 | 9 | 45.0 | **enemy-combat-taunts** — Enemy death quotes fire when killed; roast director fires between waves — but no enemy taunts the player mid-combat while alive. Karen has never screamed 'I WANT THE MANAGER!' at you while charging. Contextual alive-enemy taunts are the single highest-personality upgrade possible. **Recipe:** Add `combatTaunts[]` to 8 enemy types in `ENEMY_TYPES`; in App.jsx enemy update loop, at 0.4%/frame probability, call `addText(gs, e.x, e.y - e.size - 10, taunt, e.color)` with per-enemy 180-frame cooldown + global 60-frame any-taunt cooldown. |
| 4 | ⚡ | gamification | 0.5h | 8 | 7 | 42.4 | **wave-respite-breath** — Threat-4+ waves clear with the player depleted, but the next wave launches on the same cadence. A 120-frame pickup lull ("BREATH TAKEN") after high-threat waves creates the tension-release pacing rhythm that distinguishes great action games. The wave threat rating already exists; this pacing beat makes it meaningful. **Recipe:** In wave-clear handler, after `computeWaveThreatRating() >= 4`: set `gs.respiteTimer = 120`, spawn 2 HP packs + 1 ammo crate, skip next wave start until timer hits 0. |
| 5 | 🔥 | gamification | 3h | 9 | 9 | 34.9 | **run-narrative-arc** — Every run has a natural dramatic arc but the death screen reports flat stats. Act classification (THE OPENER/GRIND/PUSH/LEGEND) + 1-3 turning-point moments (first near-death, precision peak, boss phase-two) turns every death screen into a story recap that players want to screenshot and discuss. **Recipe:** Create `src/utils/runNarrative.js` with `buildRunNarrative()`. Track `gs._nearDeathEvents` in App.jsx. Render a RUN ARC card below BUILD GRADE on DeathScreen. |
| 6 | ⚡ | gamification | 2h | 8 | 8 | 32.0 | **weapon-legend-milestones** — Career stats have no per-weapon kill memory. Adding `weaponLegendKills[]` to career storage + rank tiers (Recruit 50 / Veteran 200 / Elite 500 / Legend 1000) creates a separate mastery axis that drives weapon specialization and surfaces on DeathScreen when a threshold is crossed. **Recipe:** Add `weaponLegendKills: new Array(12).fill(0)` to `updateCareerStats()` in `storage.js`; add `getWeaponLegendRank(kills)`; show milestone chip on DeathScreen when threshold crossed this run. |
| 7 | ⚡ | ui / ux | 2h | 8 | 8 | 32.0 | **run-dna-fingerprint** — The death screen has no single visual that captures the whole run at a glance. A 200px horizontal kill-distribution bar (weapon segments proportionally colored) + a wave-kill sparkline SVG gives each run a visual fingerprint that's screenshot-worthy and immediately readable. **Recipe:** In `DeathScreen.jsx` below BUILD GRADE, render a RUN DNA strip: flex div with `WEAPONS[i].color` segments proportional to `weaponKills[i]/total`, followed by a 200×30 SVG sparkline. Pure client-side, no deps. |
| 8 | ⚡ | gamification | 1h | 7 | 7 | 30.9 | **enemy-chain-fear-enrage-spawn** — `_chainEnrageLevel` increases enemy speed/rate but has no on-spawn visual signal. Newly spawned enemies with enrage≥1 should briefly flash yellow/orange/red (20-frame fade tint + ⚡ text above) so players can SEE the game reacting to their kill chain. The mechanic already has drama; this makes it readable. **Recipe:** In App.jsx spawn handler, set `e._spawnFlashTimer = 20` and `e._spawnEnrageLevel = gs._chainEnrageLevel`. In `drawGame.js` enemy render, tint fill color during flash + draw ⚡ text at enrage ≥1. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `flow-state-time-dilation` (innov: 10) — genre-first flow-state mechanical reward
- `enemy-combat-taunts` (innov: 9) — contextual alive-enemy taunts nobody does in browser shooters
- `run-narrative-arc` (innov: 9) — dramatic act detection on death screen
- `boss-grudge-dialogue` (innov: 8) — history-aware adversarial boss callbacks
- `weapon-legend-milestones` (innov: 8) — per-weapon career mastery axis
- `run-dna-fingerprint` (innov: 8) — visual run fingerprint

## Skipped (Priority < threshold or deferred)

- `ghost-pack-canvas-render` — High canvas state coupling risk; deferred until drawGame decoupling advances. Priority 24.4.
- `precision-streak-achievement-push` — 66 achievements already; adding more without a category system adds noise.

---
*Generated by `/audit` v1.0 · Session 88 · 2026-06-13 · ready for `/implement`*
