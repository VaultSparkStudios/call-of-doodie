<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-14 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 2
- Combined Priority: 52.5
- Top 3: deathscreen-replay-proof-presenter · ops-innovation-pack-command

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | ⚡ | speed / organization / efficiency / ui / ux / user-experience / security | 1h | 8 | 7 | 32.5 | **deathscreen-replay-proof-presenter** — Current code correctly turns replay trace evidence into receipts and a last-10 trend, but `DeathScreen.jsx` still owns the composition of the current receipt, history trend, and share-card stamp. That makes a trust contr **Recipe:** Create `src/utils/replayProofPresenter.js` with `buildReplayProofPresenter({ traceEvidence, runHistory })`, returning `receipt`, `trend`, and `shareStamp`. Replace inline DeathScreen receipt/trend composition with the he |
| 2 | ⚡ | speed / organization / efficiency / token / api consumption reduction | 0.5h | 6 | 5 | 20 | **ops-innovation-pack-command** — The local session protocol tells `/implement` to expand into an innovation pack when the audit list is exhausted, but `node scripts/ops.mjs innovation-pack` currently exits as an unknown command. That makes low-context c **Recipe:** Add an `innovation-pack` command to `scripts/ops.mjs` that gathers open Now/Deferred task-board items plus cached genius-list signals, writes `docs/INNOVATION_PACK.md`, and documents guardrails for repo-local implementat |

## Innovation reserve

- None

## Skipped

- replay-proof-receipt — Rejected on verification: `buildReplayProofReceipt()`, DeathScreen proof card, and share-card proof stamp already shipped in `docs/AUDIT_2026-06-14.json`.
- trace-fixture-harness — Rejected on verification: `src/utils/replayTraceFixtures.js` and `scripts/validate-replay-trace-fixtures.mjs` already exist and pass the current fixture parity contract.
- physics-parity-replay-resim — Deferred: the current code intentionally labels replay validation as an advisory pressure estimate; replacing it with true frame simulation is larger than a safe continuation slice.

## Execution Log

- deathscreen-replay-proof-presenter: shipped-l3 — Added `src/utils/replayProofPresenter.js` to compose DeathScreen replay proof receipts, proof trends, and share-card stamps through one pure tested helper; DeathScreen now consumes that presenter instead of owning trust composition inline. Climbed L3 by routing online/rejected/local submission feedback proof readouts through the same presenter. Verification: focused proof/replay/session tests 27/27, full `npm test` 453/453, replay fixture validator 4/4, `npm run lint` 0 errors / 8 existing warnings, and `npm run build` passing.
- ops-innovation-pack-command: shipped — Added `node scripts/ops.mjs innovation-pack`, generated `docs/INNOVATION_PACK.md`, and added the artifact to protocol drift visibility. Verification: command smoke passed and `npm run protocol:drift -- --json` reports required helpers present.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
