<!-- generated-by: /arc Codex session 113 -->
<!-- generated-at: 2026-07-02 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie — Session 113

Live-code audit found one high-leverage repo-executable replay-trust follow-up: wire the deterministic replay slices from the browser utility into the Supabase validate-replay edge receipt surface without changing the existing advisory gate.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | FIRE | security / replay trust | 1.5h | 8 | 6 | 26.8 | **validate-replay-edge-deterministic-slice-receipts** — The browser-side deterministic movement/combat/contact-enemy slices existed, but the edge validator still emitted only the older pressure receipt. Port a Deno-compatible summary receipt under the existing advisory pressure estimate, and extend parity validation against browser `runResim()`. |

## Execution Log

- validate-replay-edge-deterministic-slice-receipts: shipped — `supabase/functions/validate-replay/pressure.js` now emits `deterministicSlices` containing contract, stepper, combat-slice, and derived contact-enemy receipts under the existing `heuristic_pressure_estimate`/advisory gate; `index.ts` types the new receipt; `npm run replay:edge-fixtures` compares edge summaries against browser `runResim()`.

## Validation

- `node scripts/validate-edge-replay-pressure-fixtures.mjs` — 4 fixtures.
- `npx vitest run src/utils/replayResim.test.js` — 17/17.
- `npm run replay:edge-fixtures` — 4 fixtures.
- `npm run replay:state-stepper` — 4 fixtures.
- `npm run lint` — clean.
- `npm test` — 595/595.
- `npm run build` — passed.
- `deno check supabase/functions/validate-replay/index.ts` — passed.

## Honest Deferrals

- Analytics/dashboard allowlists remain provider-credential gated.
- Physical PWA install and real gamepad QA remain device-evidence gated.
- Lighthouse/funnel analysis remains production-data gated.
- Remaining launch screenshots still require verified browser captures.
