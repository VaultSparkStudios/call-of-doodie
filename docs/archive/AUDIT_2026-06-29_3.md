<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-29 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 197.8
- Top 3: deterministic-replay-state-stepper · game-loop-protocol-source-bridge · launch-media-verified-capture-contract

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | fire | launch-confidence | 2h | 9 | 9 | 72 | **deterministic-replay-state-stepper** — Replay trust is the game's most differentiated confidence loop, and current code now honestly reports readiness without claiming physics parity. The next non-fake step is a narrow deterministic movement/aim state-stepper **Recipe:** Add a pure movement/aim-only deterministic replay state-stepper to `src/utils/replayResim.js`, expose it from `runResim()` when the contract is ready, and test valid, invalid, and diagonal movement cases without changing |
| 2 | high | process quality | 0.5h | 8 | 5 | 53.3 | **game-loop-protocol-source-bridge** — The game-loop-review protocol reads `context/GAME_LOOP.md`, but this repo's actual game-loop source lives at `docs/GAME_LOOP.md`. That mismatch makes future design audits look under-documented even though the source exis **Recipe:** Add `context/GAME_LOOP.md` as the protocol-readable game-loop summary, preserving the existing public `docs/GAME_LOOP.md` language and adding a short evidence note that points audits to live code and launch checks. |
| 3 | high | ui / ux / user-experience | 1.5h | 8 | 7 | 72.5 | **launch-media-verified-capture-contract** — The manifest still points every screenshot entry at SVG placeholders even though the repo has two verified Chromium captures. The honest upgrade is to let the manifest and media gate prefer verified captures where they e **Recipe:** Update `public/manifest.json` to use verified capture PNGs for combat and mobile, keep uncaptured scenes as SVG fallbacks, and extend `scripts/validate-launch-media.mjs` so manifest PNG entries must be listed as producti |

## Innovation reserve

- deterministic-replay-state-stepper

## Skipped

- sync-studio-events-live-deploy — Credential-gated: `check-secrets --audit` reported no ready capabilities and PROJECT_STATUS records SUPABASE_ACCESS_TOKEN unavailable.
- posthog-sentry-production-analytics — Provider/dashboard-gated: blocker-preflight lists PostHog and Sentry GitHub Actions secrets as missing; no fabricated analytics data should be introduced.
- physical-pwa-gamepad-qa — Manual device QA: blocker-preflight correctly classifies real PWA install acceptance and real gamepad/browser combo verification as manual/device steps.

## Execution Log

- deterministic-replay-state-stepper: shipped — Added movement/aim-only deterministic replay state stepping behind the readiness contract while preserving the existing advisory pressure gate. Verified with replayResim focused tests 8/8.
- game-loop-protocol-source-bridge: shipped — Added context/GAME_LOOP.md so the canonical game-loop-review protocol has the expected context source with live evidence pointers and open risk boundaries.
- launch-media-verified-capture-contract: shipped — Manifest now uses verified browser-capture PNGs for combat and mobile scenes; launch media validation requires production-ready browser-capture asset records for manifest PNG screenshots.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
