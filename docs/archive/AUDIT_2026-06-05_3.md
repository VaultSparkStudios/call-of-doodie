<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-05 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie Skill Entry Integrity

> Focused continuation audit after `/start` verification exposed one remaining command-entry gap.
> Run `/implement` to keep the Studio OS skill sequence executable before `/closeout`.

## Top-line

- Total items: 1
- Combined Priority: 28.4
- Top 3: skill-profile-personalization-shim

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | ⚡ | speed / organization / efficiency | 30m | 9 | 5 | 28.4 | **skill-profile-personalization-shim** — Make every requested Studio OS skill executable from this public game repo. `/start`, `/audit`, `/implement`, and `/closeout` all call `scripts/lib/skill-profile.mjs` first, and the file was missing. **Recipe:** Add a deterministic local profile resolver, active-skill marker, and minimal medium gate/rubric shims so Call-Of-Doodie gets a game overlay without private Studio Ops imports. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- None in this repair audit. This is command-entry reliability work that protects the launch session loop.

## Skipped

- `playwright-pointer-360` — still the next best product-quality sprint, but this continuation must first close the command-entry execution gap discovered during verification.
- `deterministic-resim-runner` — high-value leaderboard trust milestone, deferred because the active goal is the Studio OS command sequence and closeout.

## Execution Log

- `skill-profile-personalization-shim` — SHIPPED. Evidence: `scripts/lib/skill-profile.mjs` returns a Call-Of-Doodie `game` overlay for `/start` and `/audit`; `scripts/set-active-skill.mjs` writes `.cache/active-skill.json`; `medium-quality-gates` and `sil-rubrics` shims keep implement/closeout helper imports available; `node --check` passes for the new protocol shim scripts.

## Validation

- Protocol: `skill-profile`, `set-active-skill`, `compact-handoff`, `render-startup-brief`, `validate-brief-format`, `verify-plan-mode`, `context-meter`, and `write-project-status` all pass.
- Startup: Session 81 rendered `docs/STARTUP_BRIEF.md` v3.2 and the validator reports all required canonical blocks present.
- Status invariant: SIL score is clean at 1000/1000.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
