# Audit — 2026-07-01 — Session 106

Live-code audit found the Unified Genius List mostly gated by credentials, dashboards, physical-device QA, production data, or an explicit product decision. The repo-executable item was validate-replay Phase 2B: advance deterministic replay in a bounded, honest slice without relabeling the existing advisory gate as full physics parity.

## Shipped

1. Deterministic replay combat slice — `runDeterministicReplayCombatSlice()` now simulates trace-driven movement, dash impulse, fire cooldown, ammo, reload completion, grenade cooldown, and blocked-action receipts. `runResim()` exposes the slice only when the deterministic input contract is ready and still reports `heuristic_pressure_estimate` / `advisory`.
2. Replay fixture gate expansion — `npm run replay:state-stepper` now validates both movement/aim stepping and the combat-slice contract against the shared replay fixtures.
3. Protocol-tool green-gate repair — full-suite validation exposed pre-existing dirty worktree regressions, so the session restored Unicode scalar sanitization in `model-router`, compact-handoff no-network Unicode smoke mode, and Codex plan-mode `not_required` stamping.

## Honest Deferrals

- Supabase `sync-studio-events` deploy remains credential-gated; the secrets audit reports no ready capabilities.
- PostHog/Sentry/Ko-fi allowlist and secret work remains dashboard/credential gated.
- PWA install, real gamepad QA, Lighthouse comparison, and funnel analysis require physical/browser/production data evidence.
- Supabase Auth / Studio membership remains a product decision; no account gate was invented.

## Validation

- `npx vitest run src\utils\replayResim.test.js` — 11/11
- `npm run replay:state-stepper` — 4 fixtures
- `npx vitest run tests\model-router-unicode.test.js tests\compact-handoff-unicode-smoke.test.js tests\verify-plan-mode.test.js` — 4/4
- `npm run lint` — pass
- `npm test` — 550/550 across 67 files
- `npm run build` — pass
- `npm run replay:edge-fixtures` — 4 fixtures
- `npm run launch:media-check` — pass
