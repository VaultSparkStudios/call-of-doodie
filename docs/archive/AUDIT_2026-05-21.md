<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-21 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 168.9
- Top 3: flow-field-extraction-contract · formation-spawn-identity · deathscreen-contract-closure

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed / organization / replay foundation | 1h | 8 | 7 | 49.2 | **flow-field-extraction-contract** — enemy navigation is still a large inline `App.jsx` island even though replay Phase 2B needs deterministic movement primitives. The existing BFS flow-field logic is pure enough to extract now and test directly. **Recipe:** Move `FF_CELL`, `buildFlowField()`, and flow-field sampling into `src/systems/flowField.js`, wire `App.jsx` through the helpers, and add tests for obstacle blocking plus fallback steering. |
| 2 | 🔥 | gamification / immersion / feature depth | 2h | 8 | 8 | 48.0 | **formation-spawn-identity** — the wave director names pressure stages, but individual spawns still arrive as unrelated edge enemies. Late-wave runs need memorable tactical shapes without adding more raw enemy types. **Recipe:** Add a deterministic formation planner to `waveDirector`, tag scouting/pressure/climax spawns as flank/pincer/surge patterns, apply small post-spawn offsets in `App.jsx`, and cover the planner with unit tests. |
| 3 | ⚡ | feedback loop / UX / engagement | 1h | 7 | 7 | 43.1 | **deathscreen-contract-closure** — weekly contracts and bounty loops live in Run History, but the post-run screen is where motivation is highest. The debrief should tell the player whether the just-finished run advanced the current contract. **Recipe:** Reuse `buildWeeklyContract()` on `DeathScreen`, render one compact "Next Contract" panel under Run Brain, and add a social-retention regression for contract progress copy. |
| 4 | ⚡ | security / automation / token-cost discipline | 1h | 7 | 5 | 28.6 | **trace-contract-byte-budget** — trace bodies now reach `submit-score`, but the edge contract caps event count rather than encoded byte size. A malicious compact string can still waste request/log budget before quarantine. **Recipe:** Add a trace body byte cap and reason to `collectTraceFailures()`, mirror the cap in client trace constants if needed, and document the measurement target in the validation log. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- **formation-spawn-identity** (Innovation: 8) — turns the existing director from pacing metadata into readable battlefield choreography.

## Skipped (Priority < 1.5)

- 0 items deferred. Human-only launch gates remain in `context/TASK_BOARD.md`: physical PWA/gamepad QA, Itch.io publication, analytics credential injection, and Cloudflare token rotation.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| flow-field-extraction-contract | SHIPPED | `src/systems/flowField.js` now owns `buildFlowField()` and `sampleFlowField()`; `src/App.jsx` imports the helpers; `src/systems/flowField.test.js` covers path steering, obstacle routing, and fallback-null behavior. |
| formation-spawn-identity | SHIPPED | `src/systems/waveDirector.js` now exports deterministic formation plans plus bounded post-spawn offsets; `src/App.jsx` applies them to director spawns; `src/systems/waveDirector.test.js` covers pincer identity and bounds clamping. |
| deathscreen-contract-closure | SHIPPED | `src/components/DeathScreen.jsx` now builds the active weekly contract from run history/rivalries/studio events and renders its progress under Run Brain; `src/utils/socialRetention.test.js` covers latest progress labels. |
| trace-contract-byte-budget | SHIPPED | `src/utils/replayCommandTrace.js` exports a 10,000-byte trace body budget and rejects oversized traces; `supabase/functions/submit-score/index.ts` rejects trace bodies over the same encoded budget before storage; trace tests cover the client guard. |

## Validation

- `npx vitest run src/systems/flowField.test.js` — 3/3 passing.
- `npx vitest run src/systems/waveDirector.test.js` — 9/9 passing.
- `npx vitest run src/utils/socialRetention.test.js src/utils/replayCommandTrace.test.js` — 10/10 passing.
- `npm run lint` — passing.
- `npm run build` — passing.
- `npm test` — 357/357 passing across 44 files.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
