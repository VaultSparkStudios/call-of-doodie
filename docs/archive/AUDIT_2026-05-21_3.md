<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-21 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> Same-day Session 72 audit. Focus: turn replay traces from "valid body exists" into enough player-intent evidence for the first deterministic drift detector.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 135.2
- Top 3: replay-input-signal-coverage · replay-trace-evidence-summary · validate-replay-trace-quality-gate

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security / replay trust / gamification | 1h | 9 | 8 | 55.4 | **replay-input-signal-coverage** — command traces now capture discrete buttons, but movement and aim are still mostly absent even though those are the inputs a future replay runner needs most. Without low-rate movement/aim samples, trace bodies prove activity but not player intent. **Recipe:** Add a tiny throttled trace sampler in `App.jsx` that records movement octants and aim octants only when buckets change or every bounded interval, keeping the 240-event cap intact. |
| 2 | 🔥 | AI/intelligence / feedback loop / trust | 1h | 8 | 8 | 49.2 | **replay-trace-evidence-summary** — the game can validate a trace body, but it cannot yet describe whether that body is useful evidence. A local evidence summary gives debriefs, ops scripts, and edge validators one cheap vocabulary for weak vs rich traces. **Recipe:** Extend `src/utils/replayCommandTrace.js` with `analyzeReplayCommandTrace()` returning duration, action mix, movement/aim/shoot counts, evidence level, and weakness reasons; cover rich and weak traces in unit tests. |
| 3 | ⚡ | security / automation / token-cost discipline | 45m | 7 | 7 | 30.6 | **validate-replay-trace-quality-gate** — `validate-replay` currently grants `trace_contract` confidence when digest/count/body shape matches, even if the body is a one-action trace that cannot support drift review. Confidence should reflect evidence quality, not just syntax. **Recipe:** Mirror the lightweight trace evidence checks in `supabase/functions/validate-replay/index.ts`, return `heuristic` rather than `trace_contract` for low-evidence traces, and add local tests or smoke fixture coverage for rich vs weak trace bodies. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- **replay-input-signal-coverage** — makes the parody arcade loop auditable without turning it into a heavyweight telemetry product.
- **replay-trace-evidence-summary** — creates a reusable "replay evidence quality" vocabulary that can later power debrief trust copy, leaderboards, and drift quarantine.

## Skipped (Priority < 1.5)

- Full deterministic headless resimulation remains too large for this pass; this audit builds the missing evidence substrate first.
- HomeV2 v1 fallback retirement remains data-gated on Lighthouse/funnel evidence.
- Supabase Auth/Studio membership remains deferred until traffic or paid-tier pressure justifies the account surface.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| replay-trace-evidence-summary | SHIPPED | `src/utils/replayCommandTrace.js` now exports `analyzeReplayCommandTrace()` with duration, action mix, movement/aim/shoot counts, interaction count, evidence level, and weakness reasons; tests cover rich and weak traces. |
| replay-input-signal-coverage | SHIPPED | `src/App.jsx` now records throttled movement and aim octants through the existing bounded command-trace recorder, sampling only on bucket changes or interval expiry so the 240-event cap remains intact. |
| validate-replay-trace-quality-gate | SHIPPED | `supabase/functions/validate-replay/index.ts` now mirrors trace evidence quality, exposes `traceEvidence`, and reserves `trace_contract` confidence for rich trace bodies; `scripts/replay-trust-smoke.mjs` now checks rich, weak, and malformed trace cases. |

## Validation

- `npx vitest run src/utils/replayCommandTrace.test.js src/utils/runSubmission.test.js` — 15/15 passing.
- `npm run lint` — passing.
- `npm run build` — passing.
- `npm test` — 362/362 passing across 44 files.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
