<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-18 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 203.4
- Top 3: replay-trace-contract-v2 · submit-score-trace-firewall · trace-submission-test-backfill

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security / feature depth | 2h | 9 | 8 | 54.0 | **replay-trace-contract-v2** — The browser now emits a compact replay command trace, but `validate-replay` still treats `inputHash` as the only replay-contract proof. Accept `traceDigest` + `traceLength` as an honest trace-backed contract, validate their shape, and return `confidence: "trace_contract"` when the heuristic is clean. **Recipe:** Extend `ValidateRequest`, add trace metadata validation, let competitive seeded runs satisfy the contract with either `inputHash` or valid trace metadata, and surface malformed trace fields as quarantine reasons. |
| 2 | 🔥 | security / speed | 1h | 8 | 7 | 51.7 | **submit-score-trace-firewall** — `submit-score` accepts `traceDigest` / `traceLength` in client payloads but drops them before plausibility checks, so malformed trace claims can pass through unclassified. Validate trace metadata next to event-digest validation, reject malformed claims with a dedicated anomaly reason, and include valid trace metadata in member session metadata for future audits without requiring a leaderboard schema change. **Recipe:** Add a pure `collectTraceFailures()` helper in `supabase/functions/submit-score/index.ts`; run it before callsign checks; write trace summary into `game_sessions.metadata` only. |
| 3 | ⚡ | automation / feedback loop | 30m | 7 | 5 | 49.0 | **trace-submission-test-backfill** — S67 added trace fields to `buildSessionSubmission()` but the test suite only proves event digest forwarding. Add regression coverage that valid command traces produce `traceDigest` / `traceLength` and empty traces do not inflate payloads. **Recipe:** Extend `src/utils/runSubmission.test.js` with two focused cases using command-trace-like summaries. |
| 4 | ⚡ | organization / token-cost | 30m | 6 | 5 | 42.0 | **trace-truth-repair** — The public task board still lists "Replay command trace integration" as open even though the client binding shipped; that creates future audit churn and wasted agent tokens. Split the remaining work into the actual server-side contract gap and mark the client binding complete. **Recipe:** Update `context/TASK_BOARD.md`, `context/PROJECT_STATUS.json`, and `context/TRUTH_AUDIT.md` so the source of truth distinguishes client trace binding from edge validation. |
| 5 | 💡 | UX / immersion | 1h | 6 | 4 | 6.7 | **run-proof-language** — The player-facing score-submit path gains more trust, but the UI still talks like a generic leaderboard. Surface one compact phrase in the debrief/trust copy that the run was submitted with replay proof when a trace is present; keep it private-safe and non-technical. **Recipe:** Defer broad UI work; add only a small public-safe closeout note and keep product UI unchanged until the edge contract deploys. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- **replay-trace-contract-v2** (Innovation: 8) — moves Call of Doodie from heuristic anti-cheat toward replay-proof trust without pretending full deterministic resim exists yet.

## Skipped (Priority < 1.5)

- 0 items deferred. Human-only launch gates remain in `context/TASK_BOARD.md`: physical PWA/gamepad QA, Itch.io publication, analytics credential injection, and Cloudflare token rotation.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| replay-trace-contract-v2 | SHIPPED | `supabase/functions/validate-replay/index.ts` accepts `traceDigest` + `traceLength`, validates shape/range, lets competitive seeded runs satisfy replay-contract presence with valid trace metadata, and returns `confidence: "trace_contract"` when heuristic checks are clean. |
| submit-score-trace-firewall | SHIPPED | `supabase/functions/submit-score/index.ts` rejects malformed trace metadata with `replay_trace_malformed` anomaly logging and carries valid trace summaries into member `game_sessions.metadata` without leaderboard schema drift. |
| trace-submission-test-backfill | SHIPPED | `src/utils/runSubmission.test.js` now covers trace metadata inclusion and empty-trace omission; targeted test passes 5/5. |
| trace-truth-repair | SHIPPED | `context/TASK_BOARD.md`, `context/PROJECT_STATUS.json`, and `context/TRUTH_AUDIT.md` now distinguish shipped client trace binding from remaining deterministic replay-runner work. |
| run-proof-language | SHIPPED | Public-safe trust language added to `context/TRUTH_AUDIT.md` and task board; product UI copy intentionally unchanged until edge contract deployment is observed. |

## Validation

- `npx vitest run src/utils/runSubmission.test.js` — 5/5 passing.
- `npm run lint` — passing.
- `npm run build` — passing.
- `npm run launch:smoke` — passing after isolated rerun.
- `npm test` — 349/349 passing after rerun; first full run timed out only on launch smoke under runner contention, and isolated + rerun passed.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
