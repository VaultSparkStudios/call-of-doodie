<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-13 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 8
- Combined Priority: 212.1
- Top 3: heat-formation-seeding · kill-chain-ai-escalation · precision-best-shot-replay

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 1h | 8 | 8 | 40.5 | **heat-formation-seeding** — The heatMeter and waveDirector are two live systems that don't talk to each other. Heat tier drives music, but formation shape is currently deterministic from wave type alone. Wiring heatTier(gs.heat) into the formation  **Recipe:** In App.jsx, pass heatTier(gs.heat) as a `heatBias` param to the waveDirector's `applySpawnFormation` call; in waveDirector.js, bias the formation choice array so 'pincer' probability rises with heatBias (0→uniform, 1→50% |
| 2 | 🔥 | gamification | 2h | 9 | 9 | 40.5 | **kill-chain-ai-escalation** — The kill chain currently rewards the player with milestone text and audio (RAMPAGE/GODLIKE/UNSTOPPABLE). It doesn't punish the chain. The genius flip: at UNSTOPPABLE (combo ≥ 15), enemies enter a reactive state — +10% sp **Recipe:** In the game loop frame update, compute `const _chainMod = comboRef.current.count >= 15 ? { speed: 1.10, fireRate: 0.85 } : { speed: 1.0, fireRate: 1.0 }`. Apply _chainMod.speed to each enemy's effective speed in the move |
| 3 | 🔥 | gamification | 2h | 9 | 9 | 40.5 | **precision-best-shot-replay** — The mini-replay in DeathScreen plays the full final-path animation. The precision streak system tracks bestPrecisionStreak but not WHICH frame the peak occurred at. Recording `gs.precisionPeakFrame` when a new streak pea **Recipe:** In App.jsx near the precisionStreak++ block, add: `if (gs.precisionStreak > (gs.precisionPeakStreak || 0)) { gs.precisionPeakStreak = gs.precisionStreak; gs.precisionPeakFrame = frameCountRef.current; }`. In DeathScreen. |
| 4 | ⚡ | gamification | 1h | 8 | 7 | 35.4 | **certified-run-badge** — The replay evidence pipeline (S67-S86) classifies trace quality as rich/basic/weak using analyzeReplayCommandTrace. There is no visual payoff for a rich-evidence run — the player submits and sees nothing about the trust  **Recipe:** In App.jsx, compute evidence level from commandTraceRef when the run ends (analyzeReplayCommandTrace(commandTraceRef.current)). Pass `traceEvidence: {level: 'rich'|'basic'|'weak'}` into the death data. In DeathScreen.jsx |
| 5 | 🔥 | gamification | 2h | 8 | 8 | 32 | **ghost-rivalry-proximity-graph** — loadWeeklyTopGhost returns a single fixed rival (the top 7-day scorer). This creates a permanent ceiling, not a ladder. A proximity graph auto-selects the 3 leaderboard players within ±10% of your personal best score — p **Recipe:** Add `getProximityRivals(myBestScore, leaderboard, n=3)` to storage.js: filter leaderboard entries where |entry.score - myBestScore| / myBestScore <= 0.10, sort by score ascending, return top n. Call in startGame() after  |
| 6 | ⚡ | ux | 1h | 7 | 7 | 31 | **formation-lore-card** — Enemy formations (flank/pincer/surge) are assigned by waveDirector and stored on each enemy entity, but players see no indication of which formation just spawned. The tactics are invisible. A 0.8s banner toast when a new **Recipe:** In App.jsx wave-start block, after the spawn loop, check if any spawned enemy has a new formation type (not seen this wave). If so, call addText(gs, GW()/2, GH()/2 - 60, formationLore[formation.id], '#FFF8DC') with a 60- |
| 7 | ⚡ | ai | 2h | 7 | 7 | 24.5 | **nemesis-intelligence-brief** — When a nemesis boss type is set (3 deaths against it, 0 kills), the boss cutscene card shows the 🎯 NEMESIS badge but gives no tactical guidance specific to the nemesis pattern. BOSS_GUIDANCE already contains verb and pr **Recipe:** In App.jsx nemesis boss detection, after `isNemesis(_primaryType)` is true, build a nemesis briefing object: { name: BOSS_GUIDANCE[type].title, verb: BOSS_GUIDANCE[type].verb, pressure: BOSS_GUIDANCE[type].pressure, reco |
| 8 | ⚡ | ux | 0.5h | 7 | 6 | 31.8 | **wave-threat-rating** — The wave incoming preview card shows wave number, event type, boss/mutation hints, but gives no at-a-glance danger estimate. A threat rating (1-5 skulls, computed from enemy type composition × wave multipliers) gives pla **Recipe:** Add computeWaveThreatRating(wavePlan, gs) in a utility or inline in waveDirector: score = baseEnemyCount × typeWeights (tank/boss-escort = 2.0, ranged = 1.5, standard = 1.0) + event bonus (siege/fast = +1, elite_only = + |

## Innovation reserve

- heat-formation-seeding
- kill-chain-ai-escalation
- precision-best-shot-replay
- ghost-rivalry-proximity-graph

## Skipped

- deterministic-replay-runner — Decision locked (2026-05-14): needs input timeline, not seed+hash. Heuristic advisory is the honest surface until client-server payload changes.
- supabase-auth-bridge — Credential-gated (SUPABASE_ACCESS_TOKEN missing since S82). Task board item pending human action.
- app-jsx-extraction-slice — Priority 5.8 — below 1.5 threshold. Ongoing extraction is additive; no urgent need this session.
- vite-bundle-size-gate — Priority 9.1 — nice-to-have, not transformative. Can add as 5-minute CI addition anytime.
- revenue-signal-meter — Credential-gated (SUPABASE_ACCESS_TOKEN missing). Can't query kofi_events without Supabase access.
- death-readout-drill-bridge — Superseded by nemesis-intelligence-brief which covers the same coaching gap more powerfully.

## Execution Log

- wave-threat-rating: shipped — computeWaveThreatRating in waveDirector.js; 1-5 skull threat display in wave preview card
- heat-formation-seeding: shipped — heatBiasedFormation wired in spawn block; heat tier 2 forces pincer
- formation-lore-card: shipped — Per-formation toast with per-wave _formationToastedThisWave Set
- kill-chain-ai-escalation: shipped — _chainEnrageLevel at combo thresholds; speed and fire-rate pressure applied
- certified-run-badge: shipped — Trace evidence is captured on death and rendered as a VERIFIED RUN chip
- precision-best-shot-replay: shipped — Precision peak frame tracked; BEST SHOT button scrubs ghost replay
- nemesis-intelligence-brief: shipped — _NEMESIS_WEAPON lookup and NEMESIS DOSSIER boss cutscene block
- ghost-rivalry-proximity-graph: shipped — getProximityRivals in storage.js and RIVALRY LADDER card in DeathScreen

---
*Generated by `/audit` v1.0 · ready for `/implement`*
