<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-21 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> Same-day follow-up audit for Session 71. Focus: make the replay trust chain honest enough for the deterministic resim milestone.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 127.4
- Top 3: replay-command-trace-capture · trace-submission-validity-gate · validate-replay-trace-body-parity

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security / replay trust / gamification | 1h | 8 | 7 | 49.2 | **replay-command-trace-capture** — replay command traces are encoded at submission time but never populated during a run, so trace-backed competitive submissions can still be empty. This is the highest-leverage trust gap because the stored trace payload is the bridge from heuristic validation to real replay proof. **Recipe:** Add a tiny bounded command-trace recorder to `src/utils/replayCommandTrace.js`, wire `App.jsx` to record shoot, dash, grenade, perk, route, shop, reload, and weapon-switch events at frame buckets, and cover the recorder with unit tests. |
| 2 | ⚡ | security / token-cost discipline | 30m | 7 | 6 | 44.7 | **trace-submission-validity-gate** — `buildSessionSubmission()` forwards whatever `commandTrace` object it receives, even if the digest/count/body relationship is malformed. A cheap client-side validity gate avoids sending junk trace fields and keeps edge logs cleaner. **Recipe:** Route trace forwarding through `isValidReplayCommandTrace()`, omit invalid trace payloads before network submission, and add `runSubmission` regressions for both valid and invalid traces. |
| 3 | ⚡ | security / automation / replay foundation | 1h | 7 | 6 | 33.5 | **validate-replay-trace-body-parity** — `validate-replay` accepts `traceDigest` and `traceLength` but cannot verify an optional trace body when a caller supplies one, leaving the smoke path weaker than `submit-score`. The live trust smoke should prove the full trace contract, not just metadata shape. **Recipe:** Teach `supabase/functions/validate-replay/index.ts` to accept `traceBody`, enforce body byte/count/digest/action-shape parity with `submit-score`, and update `scripts/replay-trust-smoke.mjs` so live validation proves body-backed contracts. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- 0 items. This audit deliberately favors trust closure over broad feature novelty because the current product breadth is already high and replay proof compounds every rivalry/ghost/leaderboard loop.

## Skipped (Priority < 1.5)

- HomeV2 fallback retirement remains data-gated on Lighthouse/funnel evidence.
- Supabase Auth/Studio membership remains deferred until traffic or paid tier warrants the added account surface.
- Launch publication/device QA remains human-channel work, already tracked in `context/TASK_BOARD.md`.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| replay-command-trace-capture | SHIPPED | `src/utils/replayCommandTrace.js` now exports `recordReplayCommandEvent()` and `directionBucket()`; `src/App.jsx` records shoot, dash, grenade, perk, route, shop, reload, and weapon-swap commands before submission encoding; focused trace tests cover bounded recording and direction buckets. |
| trace-submission-validity-gate | SHIPPED | `src/utils/runSubmission.js` now forwards trace fields only when `isValidReplayCommandTrace()` passes; regressions prove valid traces are included, empty traces omitted, and malformed traces stripped. |
| validate-replay-trace-body-parity | SHIPPED | `validate-replay` now accepts optional `traceBody` and checks byte budget, count, digest, frame, and action shape; `scripts/replay-trust-smoke.mjs` now posts body-backed valid and malformed trace cases. |

## Validation

- `npx vitest run src/utils/replayCommandTrace.test.js src/utils/runSubmission.test.js` — 13/13 passing.
- `npm run lint` — passing.
- `npm run build` — passing.
- `npm test` — 360/360 passing across 44 files.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
