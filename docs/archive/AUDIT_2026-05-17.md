<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 (Session 67 refresh) -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> **Session 67 refresh** — all prior items shipped; this is a fresh 10-item slate.

## Top-line

- Total items: 10
- Combined Priority: 587.4
- Top 3: rhythm-kill-bonus · doodie-pass-play-widget · daily-mission-streak

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 8 | 9 | 108.0 | **rhythm-kill-bonus** — Beat-sync spawn particles already fire when music aligns (S62), but the BPM sync has zero gameplay consequence. Kills within ±4 frames of a music beat earn a `BEAT KILL! ♪ +1💩` bonus and a distinctive text pop — turning the audio engine into an active skill layer nobody in the browser-shooter category does. **Recipe:** In the bullet-enemy collision path in App.jsx, call `getMusicBeat()` and if `|currentFrame % framesPerBeat| < 4`, award +1💩 and emit a `"BEAT KILL! ♪"` floating text event. |
| 2 | 🔥 | gamification | 1h | 7 | 6 | 79.7 | **doodie-pass-play-widget** — The Doodie Pass cosmetic track (S57) only surfaces in menus; players never feel progression momentum during a run. Add end-of-wave and post-death notifications: `"🎖 Doodie Pass XP +1 — Tier 3 unlocked!"` using existing `cosmeticTrack.js` API. Closes the in-play feedback loop with zero balance impact. **Recipe:** After wave clear and on death in App.jsx, call `addCosmeticXP()` from `cosmeticTrack.js`; if tier count changed, push a floating text event `"🎖 Pass Tier N!"` and queue a cosmetic unlock toast on the death screen. |
| 3 | 🔥 | gamification | 4h | 9 | 7 | 73.2 | **persistent-ghost-leaderboard** — Ghost race uses sessionStorage per-mode and resets every browser session. The leaderboard already stores `starterLoadout` + `seed` per entry. Pull top-3 rows per mode from Supabase at run-start and seed them as persistent ghost opponents so players are always racing someone real — even in solo sessions. **Recipe:** Add `loadTopGhosts(mode, difficulty)` to `storage.js` fetching top-3 leaderboard rows; in App.jsx `startGame()`, seed `gs._ghostFrames` from those rows when no sessionStorage ghost exists. |
| 4 | 🔥 | gamification | 2h | 8 | 6 | 72.0 | **daily-mission-streak** — Daily missions (24 types) are isolated per-day with no cross-day binding. Add a 7-day streak counter: completing ≥1 daily mission advances the streak; show `🔥 3-day streak` chip on HomeV2 Command Center; award +5💩 at 3/5/7-day milestones with fanfare. New key: `cod-mission-streak-v1 {streak, lastCompleted}`. The cheapest possible retention hook — no backend, pure localStorage. **Recipe:** Add `getMissionStreak()` / `advanceMissionStreak()` / `resetMissionStreak()` to `storage.js`; call `advanceMissionStreak()` when a daily mission completes; render streak chip in HomeV2 Command Center. |
| 5 | ⚡ | gamification | 1h | 7 | 5 | 66.5 | **objective-mastery-deathscreen** — The Dynamic Objective System (5 types) fires during runs but disappears with no post-run summary. The DeathScreen shows kills/wave/perks but never answers "did I do the objective?". Add a compact `OBJECTIVES` row: `✓ Hot Zone (2/3) · ✗ Bounty — not found`. Closes the feedback loop that makes players attempt objectives intentionally next run. **Recipe:** Track `gs.objectivesCompleted[]` and `gs.objectivesFailed[]` in `objectiveDirector.js`; pass through `runSession.buildSessionSummary()` to DeathScreen as `summary.objectives`; render in DeathScreen below the kills row. |
| 6 | ⚡ | UX | 1h | 7 | 7 | 62.0 | **predictive-difficulty-briefing** — Players pick difficulty blind. `cod-run-history-v1` already stores per-run wave/difficulty. Show a one-line historical briefing below the difficulty picker on HomeV2: `"Your HARD record: avg wave 14 · 42% reach wave 20 (last 12 runs)"`. Pure local data, zero tokens, dramatically reduces frustration from difficulty mismatch. **Recipe:** Add `getDifficultyBriefing(difficulty)` to `runBrain.js` aggregating `cod-run-history-v1` by difficulty; render as subtitle line in the HomeV2 difficulty selector UI. |
| 7 | ⚡ | AI / gamification | 2h | 8 | 8 | 48.0 | **cross-run-coaching-memory** — Run Coach and Run Brain analyze only the current run. Add `mostFrequentKiller(runHistory)` that aggregates `killedByType` across last 10 runs — if Seeker (type-17) killed the player 4 times this week, the Run Coach card says "⚠ Seeker is hunting you (4 deaths this week) — dash through their charge pattern." Zero API tokens, fully local, genuinely personalized. **Recipe:** Add `mostFrequentKiller(runHistory)` to `runBrain.js`; extend `buildRunCoach()` in `runCoach.js` to accept `frequentKiller` and append a cross-run evasion tip when frequency ≥ 3; pass run history from DeathScreen. |
| 8 | ⚡ | feature depth / security | 2h | 8 | 6 | 36.0 | **replay-trace-submission-integration** — `replayCommandTrace.js` (S66) is built and tested but entirely orphaned — not included in any run submission or validation path. Binding its `encodeTrace()` output into the leaderboard submission payload closes the trust loop and is the prerequisite for deterministic Phase 2B. **Recipe:** In `runSubmission.js` `buildSessionSubmission()`, call `replayCommandTrace.encodeTrace(gs._commandTrace)` and include `traceDigest` + `traceLength` in the payload; in `supabase/functions/validate-replay`, treat `traceLength === 0` as a soft-warning flag. |
| 9 | 💡 | feature depth | 1h | 6 | 4 | 22.8 | **weapon-unlock-telemetry** — No tracking exists for which level/wave players reach each weapon unlock or whether grandfathered loadouts get reused vs. reset. This data is the prerequisite for balancing the unlock curve and the "Wanted" meta-balance system. **Recipe:** In the weapon unlock trigger path in App.jsx (or `levelFlow.js`), emit `track("weapon_unlock", {weaponIdx, level, wave, grandfathered})`; also append to `cod-career-v1.weaponUnlockSummary[]` for local analytics before PostHog keys are configured. |
| 10 | 💡 | speed / architecture | 4h | 9 | 5 | 17.4 | **app-extraction-slice-1** — App.jsx is ~3500 lines; the per-frame player+bullet+enemy update block (~400 lines) remains inline in the `gameLoop` useCallback. Extracting it to `src/systems/gameStep.js` as a pure `step(gs, frame, inputs) → {nextGs, events}` function enables isolated testing of core mechanics and is the architectural prerequisite for headless deterministic replay resim. **Recipe:** Identify and extract the per-frame player+bullet+enemy update block to `gameStep.js`; wire back through App.jsx; add ≥5 tests covering movement, collision, and event emission. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- **rhythm-kill-bonus** (Innovation: 9) — turns existing BPM sync into a skill layer; no browser shooter does this
- **cross-run-coaching-memory** (Innovation: 8) — genuinely personalized coaching from local data alone

## Skipped (Priority < 1.5)

- 0 items deferred. Human-only launch gates remain in `context/TASK_BOARD.md`: physical PWA/gamepad QA, Itch.io publication, analytics credential injection, and Cloudflare token rotation.
- **wanted-meta-balance** (Priority: 73.2) deferred — blocked on weapon-unlock-telemetry data accumulating; revisit Session 69+.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| rhythm-kill-bonus | SHIPPED | Beat-kill bonus in App.jsx bullet-enemy collision path; +1💩 + `"🎵 BEAT KILL! +1💩"` floating text + 4 magenta particles on frame ±4 of beat. |
| doodie-pass-play-widget | SHIPPED | `cosmeticTrack.reconcileOwnership()` called on death; `cosmeticUnlocks` state passed to DeathScreen; gold-bordered unlock card rendered post-death. |
| persistent-ghost-leaderboard | SHIPPED | `loadTopGhosts(mode, diff)` in storage.js fetches top-3 from Supabase; called in `startGame()`; results seeded into `gs.topGhosts`. |
| daily-mission-streak | SHIPPED | `getMissionStreak`/`advanceMissionStreak`/`resetMissionStreak` in storage.js; `advanceMissionStreak()` called on mission complete; 🔥 streak chip in HomeV2 Command Center header when streak ≥ 2. |
| objective-mastery-deathscreen | SHIPPED | `gs.objectivesCompleted[]`/`gs.objectivesFailed[]` tracked in App.jsx; `objectivesSummary` state passed to DeathScreen; OBJECTIVES card rendered with ✓/✗ per objective. |
| predictive-difficulty-briefing | SHIPPED | `getDifficultyBriefing(difficulty, runHistory)` in runBrain.js; rendered as subtitle line below difficulty picker in HomeV2. |
| cross-run-coaching-memory | SHIPPED | `mostFrequentKiller(runHistory)` in runBrain.js; `buildRunCoach()` extended with `crossRunTip`; rendered in DeathScreen RUN COACH card after precisionTip. |
| replay-trace-submission-integration | SHIPPED | `commandTraceRef` in App.jsx; `encodeReplayCommandTrace()` called in `submitScore()`; `traceDigest`/`traceLength` included in `buildSessionSubmission()`. |
| weapon-unlock-telemetry | SHIPPED | Weapon unlock detection loop in `handlePlayerDeath`; compares prev/new account level; emits `track("weapon_unlock", {...})` for each newly unlocked weapon. |
| app-extraction-slice-1 | SHIPPED | `src/systems/gameStep.js` — `computeMovementVector()` + `applyPlayerMovement()`; 11 tests in `gameStep.test.js`; all 347 tests pass. |

### Prior session execution log (S66 items — all SHIPPED)

| Item | Status | Evidence |
|---|---|---|
| replay-command-trace-v1 | SHIPPED | `src/utils/replayCommandTrace.js` adds bounded normalization, compact encode/decode, digest validation, and trace summaries; 4 tests. |
| launch-readiness-json | SHIPPED | `scripts/launch-readiness.mjs --json` returns checks, owner-only gates, and a readiness summary. |
| closeout-autopilot-help | SHIPPED | `scripts/closeout-autopilot.mjs --help` / `-h` prints usage and exits before doctor/git work. |

---
*Generated by `/audit` v1.0 · Session 67 · ready for `/implement`*
