<!-- generated-by: /arc Codex session 116 -->
<!-- generated-at: 2026-07-03 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie — Session 116

Live-code audit found the primary startup list still dominated by dashboard, device, community, and production-data gates. The highest repo-executable slice remained the S62 deferred MenuScreen → MenuPanels unification. HomeV2 remains the default player surface, so this session took a bounded legacy-path step instead of deleting the v1 fallback without production Lighthouse/funnel evidence.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | FIRE | architecture / front-door resilience | 1h | 6 | 3 | 16.2 | **legacy-menu-shared-panel-routing** — Route legacy MenuScreen modals through shared MenuPanels exports before full v1 retirement. |

## Execution Log

- legacy-menu-shared-panel-routing: shipped — `MenuScreen.jsx` now imports and renders the shared `MenuPanels.jsx` exports for Rules, Controls, Most Wanted, Run History, Loadout Builder, Missions, Upgrades, and What's New. The old inline runtime branches are gated off during this bounded migration slice.
- Added `src/components/MenuScreen.test.jsx` to prove the legacy Command Center opens shared Rules and Controls panel content.

## Validation

- `npx vitest run src/components/MenuScreen.test.jsx` — 1/1.
- `npx eslint src/components/MenuScreen.jsx src/components/MenuScreen.test.jsx --max-warnings 0` — clean.
- `npm run lint` — clean.
- `npm test` — 600/600 across 71 files.
- `npm run replay:state-stepper` — 4/4.
- `npm run replay:edge-fixtures` — 4/4.
- `npm run launch:media-check` — passing.
- `npm run build` — passing.
- `git diff --check` — clean.

## Honest Deferrals

- Analytics/dashboard allowlists remain provider-credential gated.
- Physical PWA install and real gamepad QA remain device-evidence gated.
- HomeV2 Lighthouse/funnel analysis remains production-data gated.
- Full legacy `MenuScreen` deletion remains gated on HomeV2 production LCP/funnel evidence.