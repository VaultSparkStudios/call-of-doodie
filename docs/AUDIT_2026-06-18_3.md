<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-18 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 12
- Combined Priority: 805.7
- Top 3: front-door-clarity-spine · obelisk-verify-guest-migration · launch-screenshot-truth-pack

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | ui / ux / user-experience | 4h | 10 | 8 | 123.8 | **front-door-clarity-spine** — The default front door is strong, but it now carries a DEPLOY dropdown, Command Center, Intel Ticker, mutation banner, measurement status, tabs, Codex subsections, and many modal panels. New players need one spine: deplo **Recipe:** Create a `src/utils/playerJourney.js` model that classifies visitors as first-run, early-run, returning, mastery, or account-curious. Use it in `HomeV2.jsx` to show one primary next action, one secondary learning/action  |
| 2 | 🔥 | security | 4h | 9 | 8 | 111.4 | **obelisk-verify-guest-migration** — Session 100 correctly made `/login` and `/auth/callback` explicit without gating play, but callback still depends on `/api/obelisk-verify`. This is the next trust boundary: account copy should not imply durable recovery  **Recipe:** Implement a Cloudflare Pages Function or Worker endpoint for `/api/obelisk-verify` that verifies the returned Obelisk token server-side, returns a redacted identity receipt, and stores a local `cod-obelisk-identity` rece |
| 3 | 🔥 | ui / ux / user-experience | 3h | 9 | 7 | 102.2 | **launch-screenshot-truth-pack** — The launch media gate proves placeholder exports exist, but the public manifest still advertises stylized SVG stills instead of captured gameplay. For a browser game, real action screenshots sell trust faster than featur **Recipe:** Use Playwright on the strict `53173` dev server to capture desktop combat, boss, builds/debrief, leaderboard, and mobile control screenshots. Update `public/manifest.json` screenshot paths to production PNG captures, upd |
| 4 | 🔥 | gamification / engagement / immersion | 3h | 9 | 8 | 99.7 | **first-run-control-rite** — The game already has input calibration, pointer sweep evidence, controller labels, and an onboarding list, but the player experiences them as advice rather than a ritual. Turn the first aim sweep into a tiny in-world rea **Recipe:** Promote the existing input calibration nudge into an optional `AIM CHECK` pre-run micro-screen for first-run/debug states. Drive four target pips around the player, save the calibration record, show controller/mouse cove |
| 5 | 🔥 | ai / intelligence integration | 4h | 8 | 8 | 99.1 | **deathscreen-next-run-drill-bridge** — The debrief has strong local intelligence: build grade, replay proof, ghost death readout, choke warning, weapon mismatch, run arc, rivalry ladder, and share tools. The missing step is a one-tap drill that converts the d **Recipe:** Create `src/utils/drillDirector.js` that maps run coach outputs, ghost death readout, choke wave, top killer, and weapon mismatch into one `nextDrill` object. Render a single `RUN THE FIX` button on DeathScreen that laun |
| 6 | ⚡ | feedback-loop | 1h | 8 | 5 | 75.8 | **visitor-safe-status-copy** — The front door currently shows operational measurement details such as `PostHog key missing`. That is useful to agents and developers, but it is not visitor-facing product information and can make a polished public game  **Recipe:** Gate the HomeV2 measurement-status banner behind `?debug=ops`, localStorage `cod-debug-ops=1`, or a dev build check. Replace normal visitor copy with no banner unless there is a player-actionable local/offline state, and |
| 7 | ⚡ | speed / organization / efficiency | 6h | 8 | 6 | 63.6 | **app-death-submit-extraction** — `src/App.jsx` is now nearly 4,800 lines even after many successful extractions. The death, submit, event digest, replay proof, rivalry, mission, and share-card transitions are high-change surfaces because every new coach **Recipe:** Create `src/systems/deathFlow.js` and move death summary assembly, mission/career side-effect planning, trace evidence packaging, and post-run prop composition into tested pure builders. Keep App.jsx as the owner of refs |
| 8 | ⚡ | ui / ux / user-experience | 4h | 8 | 6 | 61.9 | **hud-collision-budget** — The HUD has become feature-rich: wave/timer, score, kill feed, mission progress, build archetype, ghost pack, weekly rival, heat, pace, experiment, weapon toolbar, synergy, and mobile controls all compete for fixed canva **Recipe:** Add a HUD layout allocator that reserves top-center, top-left, top-right, bottom-left, bottom-center, and bottom-right slots with priorities and viewport thresholds. Use Playwright screenshots to assert no key HUD region |
| 9 | ⚡ | gamification / engagement / immersion | 6h | 8 | 8 | 59.4 | **persistent-ghost-path-opponents** — The HUD can show Ghost Pack targets and the debrief can replay the player's death path, but players still do not race a visible opponent during the run. Showing a faint leaderboard ghost path or checkpoint ribbon turns a **Recipe:** Extend `loadTopGhosts()` data into compact path checkpoints when available, render a low-alpha ghost ribbon or next checkpoint marker in `drawGame.js`, and expose a setting to disable it. If leaderboard rows lack path da |
| 10 | ⚡ | ai / intelligence integration | 4h | 7 | 7 | 56.6 | **local-balance-lab** — The game already collects local Studio events and run history, but balance insight is spread across Run History, trust recommendations, mission streaks, and debrief cards. A zero-token Balance Lab can act like a local de **Recipe:** Add `src/utils/balanceLab.js` that consumes run history, rivalry history, mission progress, and Studio events to emit ranked local insights. Surface a debug/admin Balance Lab panel under `?debug=ops` and include its top  |
| 11 | ⚡ | security | 2h | 7 | 5 | 53.8 | **release-security-header-gate** — The project has Cloudflare headers and many launch checks, but account routes, external Obelisk script loading, service worker caching, Supabase functions, and canonical redirects make release security a moving target. A **Recipe:** Extend `scripts/launch-readiness.mjs` or add `scripts/security-release-gate.mjs` to fetch local/prod headers, verify CSP includes only intentional origins, assert `/login` and `/auth/callback` are explicit surfaces, chec |
| 12 | 💡 | speed / organization / efficiency | 2h | 6 | 4 | 37.4 | **legacy-home-retirement-gate** — HomeV2 is the default, but legacy MenuScreen remains reachable and duplicates What's New, Command Center, and several menu/panel concepts. Keeping a fallback is valid until evidence says otherwise, yet it increases maint **Recipe:** Add a `legacyHomeRetirement` checklist script/doc that requires HomeV2 launch smoke, mobile/desktop screenshot parity, no unique v1-only flows, and an explicit decision entry. If the gate passes, remove `?home=v1`; if it |

## Innovation reserve

- front-door-clarity-spine
- obelisk-verify-guest-migration
- first-run-control-rite
- deathscreen-next-run-drill-bridge
- persistent-ghost-path-opponents

## Skipped

- llm-powered-run-coach — Rejected on verification: `context/DECISIONS.md` locks Run Brain as zero-token/local-first. The current signals are sufficient for deterministic coaching, and adding paid/token AI would increase privacy and cost without clear quality gain.
- gate-gameplay-behind-account — Rejected on verification: Session 100 explicitly restored guest-first gameplay and route tests require `/`, `/daily`, and unknown paths to render the game. Account work should add recovery/trust, not block free play.
- full-3d-gameplay-rebuild — Rejected on verification: current 2D canvas gameplay has extensive replay/input/trust coverage. The safer visual path is real screenshots, pseudo-3D primitives, and authored sprite assets before any gameplay renderer migration.
- more-new-modes-before-consolidation — Deferred: the verified surface already has seven modes, many panels, and deep post-run systems. Current leverage is clarity, feedback closure, and trust rather than another mode competing for attention.

## Execution Log

- visitor-safe-status-copy: shipped — Gated the HomeV2 measurement-status banner behind `?debug=ops` / `cod-debug-ops=1` so ordinary visitors no longer see operational copy such as `PostHog key missing`. Added focused HomeV2 tests proving default visitor hiding and debug-surface visibility. Validation: `npx vitest run src/components/HomeV2.test.jsx` 7/7 and game medium gate passed.
- release-security-header-gate: shipped — Added `scripts/security-release-gate.mjs`, `security:release`, and `security:release:audit`, then wired the audit-backed gate into `launch:verify`. The gate checks required security headers, Content Security Policy directives, Obelisk route boundaries, service-worker cache versioning, lockfile presence, and `npm audit --json` when requested. Fixed CSP to allow the explicit Obelisk script/connect origin in both `public/_headers` and the Cloudflare security-header worker. Validation: static gate passed, audit-backed gate passed with 0 vulnerabilities, focused Obelisk route tests passed, and game medium gate passed.
- obelisk-verify-guest-migration: shipped — Added `functions/api/obelisk-verify.js` as a Cloudflare Pages Function proxy that verifies returned Obelisk session tokens only when `OBELISK_VERIFY_URL` is configured, optionally uses `OBELISK_VERIFY_SECRET`, returns redacted identity receipts, and reports an honest `verify-not-configured` state otherwise. Updated the SPA callback helper to preserve backend failure reasons/details, documented the server env vars, and extended the security release gate to require the endpoint while preserving guest-first routes. Validation: endpoint/callback/route tests passed 8/8, audit-backed security gate passed with 0 vulnerabilities, and game medium gate passed.
- legacy-home-retirement-gate: shipped — Added `docs/LEGACY_HOME_RETIREMENT.md` and `scripts/legacy-home-retirement-gate.mjs`, exposed as `npm run home:retirement-gate`. The gate confirms HomeV2 is default, legacy `MenuScreen` remains lazy fallback-only, shared MenuPanels cover deep systems, launch smoke covers the default path, and the decision log still requires a data gate before removal. Validation: retirement gate passed, `npm run launch:smoke` passed, and game medium gate passed.
- front-door-clarity-spine: shipped — Added `src/utils/playerJourney.js` plus tests, stopped auto-expanding Command Center for returning players, and wired a compact HomeV2 Journey card that keeps DEPLOY primary while surfacing one next best secondary action such as Aim Check, Daily, Upgrades, Missions, Challenge, or Codex. Validation: playerJourney/HomeV2 focused tests passed 12/12, `npm run launch:smoke` passed, and game medium gate passed.
- first-run-control-rite: shipped — Promoted Aim Check from a passive nudge into a HomeV2 verification panel with four directional targets, a local controls-verified receipt using the existing input calibration record format, and a diagnostics escape hatch. The Journey card and quick chip now route to this single verification surface, and the front door updates to `AIM CHECK VERIFIED` after completion. Validation: HomeV2/inputCalibration/playerJourney focused tests passed 17/17, `npm run launch:smoke` passed, and game medium gate passed.
- deathscreen-next-run-drill-bridge: shipped — Added `src/utils/drillDirector.js` plus tests to synthesize one corrective next-run drill from choke warnings, Enemy Lab, weapon mismatch, ghost death readouts, Run Intelligence, or debrief actions. DeathScreen now renders a `NEXT DRILL` card with a `RUN THE FIX` / `PLAY THE FIX` CTA that reuses replay/play-again paths and records shown/accepted Studio events. Validation: drillDirector/runCoach/launch focused tests passed 21/21, `npm run lint` passed, and game medium gate passed.
- local-balance-lab: shipped — Added zero-token `src/utils/balanceLab.js` plus tests to rank local design signals from run history, Studio events, career recent-death patterns, and weapon usage. HomeV2 now shows the top Balance Lab finding only in `?debug=ops` / ops-debug mode, keeping normal visitors on player-facing copy. Validation: balanceLab/HomeV2 focused tests passed 13/13, `npm run lint` passed, and game medium gate passed.
- hud-collision-budget: shipped — Added `src/utils/hudLayout.js` plus tests and wired a debug-only HUD slot overlay behind `?debug=hud` / `cod-debug-hud=1`. The overlay maps top-left, top-center, top-right, bottom-left, bottom-center, and bottom-right budget zones without affecting normal gameplay rendering. Validation: hudLayout/useGameLoop focused tests passed 7/7, `npm run lint` passed, and game medium gate passed.
- persistent-ghost-path-opponents: shipped — Added `src/utils/rivalPace.js` plus tests and wired a compact in-run `RIVAL PACE` HUD chip that picks the nearest score target from weekly rival and Ghost Pack data. It uses the existing center-stack spacing budget so challenge score, ghost pack, weekly rival, and rival pace banners do not occupy the same top-center slot. Validation: rivalPace/hudLayout focused tests passed 6/6, `npm run lint` passed, and game medium gate passed.
- launch-screenshot-truth-pack: shipped — Added `scripts/capture-launch-screenshots.mjs`, `npm run launch:screenshots`, `docs/LAUNCH_SCREENSHOT_TRUTH_PACK.md`, and two verified Chromium gameplay captures under `public/launch-captures/`. Updated launch docs, asset provenance, and the media gate so authored SVG stills remain fallback/promotional media while literal screenshot requests use verified capture PNGs. Validation: screenshot capture command exited cleanly, `npm run launch:media-check` passed, `npm run lint` passed, static security gate passed, and game medium gate passed.
- app-death-submit-extraction: shipped — Added `src/systems/deathFlow.js` plus tests and moved DeathScreen prop composition out of the App render branch into the pure `buildDeathScreenProps()` helper. App still owns refs/state/handlers, but the high-change debrief prop mapping is now isolated for future death-submit extraction work. Validation: deathFlow/App launch focused tests passed 3/3, `npm run lint` passed, and game medium gate passed.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
