<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-07 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 132.8
- Top 3: enemy-lab-run-coach · launch-readiness-evidence-receipts · protocol-drift-sentinel

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification / engagement / immersion | 1h | 9 | 8 | 41.6 | **enemy-lab-run-coach** — Turn repeated deaths into a named counter-drill. Call-Of-Doodie already tracks recent killer patterns and renders Run Coach advice, but the enemy lesson is still embedded as prose. A compact Enemy Lab profile converts frustration into mastery: killer identity, threat level, counter verb, and one next-run drill. **Recipe:** Extend `buildRunCoach()` with an `enemyLab` object for repeated killers, render it as a compact DeathScreen coaching card, and add regression coverage for repeat-killer and no-pattern states. |
| 2 | ⚡ | user feedback loop | 45m | 8 | 7 | 32.4 | **launch-readiness-evidence-receipts** — Make the human finish line auditable. The launch script correctly separates optional analytics from true owner gates, but it prints the remaining gates as plain text. The founder needs each gate to carry the exact evidence command or artifact that closes it, so manual QA stops becoming vague memory. **Recipe:** Upgrade `scripts/launch-readiness.mjs --json` and text output with structured owner gates, evidence fields, and next commands while keeping analytics explicitly post-launch optional. |
| 3 | ⚡ | speed / organization / efficiency | 45m | 8 | 6 | 27.7 | **protocol-drift-sentinel** — Report missing Studio OS helpers without breaking startup. `/start` found several protocol helpers missing locally (`credential-watch`, `ark`, `check-brief-staleness`, `build-skill-manifest`, `skill-trace-emit`). That is not player-facing, but it erodes the repeatable command loop this repo now relies on for launch hygiene. **Recipe:** Add a repo-local `scripts/protocol-drift-check.mjs` that checks required/optional Studio OS helpers, emits stable JSON/text, and documents the missing helpers as warning-level drift rather than blockers. |
| 4 | ⚡ | security | 30m | 7 | 7 | 31.1 | **trace-proof-next-benchmark** — Turn replay trust status into the next measurable target. The trust panel already classifies weak/basic/rich trace evidence and resim readiness, but the next benchmark is implicit. A named benchmark makes leaderboard trust feel like a visible ladder without falsely claiming deterministic resimulation is done. **Recipe:** Extend Studio event trust summaries with a `nextBenchmark` object and include it in trust recommendations, with tests for no-sample, weak, basic, pilot-ready, and ready states. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- enemy-lab-run-coach

## Skipped (Priority < 1.5)

- deterministic-resim-runner — Still the major trust milestone, but a real runner is a multi-surface design pass. This audit ships the benchmark and coaching layer that makes the later runner measurable.
- playwright-pointer-360 — Still valuable, but adding Playwright requires a package-trust/install pass and should not be mixed into this launch-readiness sprint.
- supabase-auth-obelisk-bridge — High monetization/account value, but current launch posture is free, local-first, and cost-neutral. Account work should trigger when paid tier or traffic justifies it.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

| Slug | Status | Evidence |
|---|---|---|
| enemy-lab-run-coach | SHIPPED | `src/utils/runCoach.js` returns `enemyLab`; `src/components/DeathScreen.jsx` renders the Enemy Lab card; `src/utils/runCoach.test.js` covers repeat-killer and no-pattern states. |
| trace-proof-next-benchmark | SHIPPED | `src/utils/studioEventOps.js` returns `nextBenchmark`; trust recommendations include the named benchmark; `src/utils/studioEventOps.test.js` covers baseline, proof-drill, upgrade-basic, bank-one-more, and ready states. |
| launch-readiness-evidence-receipts | SHIPPED | `scripts/launch-readiness.mjs --json` emits `ownerOnlyGates` and `evidenceReceipts` with evidence, next command, close condition, and optional flags; text output preserves analytics as post-launch optional. |
| protocol-drift-sentinel | SHIPPED | `scripts/protocol-drift-check.mjs` reports required/optional helper drift as stable JSON/text; `package.json` exposes `npm run protocol:drift`; runtime check returns `warning` with `missingRequired=0`. |
