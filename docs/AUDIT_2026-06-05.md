<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-05 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 89.7
- Top 3: runbrain-experiment-guardrail · recent-killer-revenge-drill · remembered-controller-glyphs

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | ⚡ | AI / intelligence integration | 30m | 8 | 6 | 32.0 | **runbrain-experiment-guardrail** — Make RunBrain's experiment matcher impossible to crash on safe-opener advice. This protects the zero-token coach loop right where players are deciding whether the game actually learned from them. **Recipe:** Define a normalized config haystack inside `matchesExperiment()` and add a regression for safe-opener intent with no direct entity match. |
| 2 | 🔥 | gamification | 1h | 8 | 8 | 31.7 | **recent-killer-revenge-drill** — Promote repeated enemy deaths into a front-door Revenge Drill. The game already remembers death pressure and widens warning windows; the missing move is telling the player exactly which threat to study before the next run. **Recipe:** Cluster `career.recentDeathsByEnemy` in `buildFrontDoorActionStack()` and emit a `revenge_drill` action for 3+ deaths to the same enemy. |
| 3 | ⚡ | UI / UX / user-experience | 1h | 7 | 7 | 26.0 | **remembered-controller-glyphs** — Use remembered controller profile metadata to show Xbox or PlayStation-specific labels across tutorial and controls help. This turns the recent input QA substrate into visible player trust, especially before launch device testing. **Recipe:** Add `getControllerLabels()` in `gamepad.js`, thread controller type through tutorial/controls/pause surfaces, and use HomeV2's stored profile as fallback. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- recent-killer-revenge-drill

## Skipped (Priority < 1.5 or larger than this pass)

- `playwright-pointer-360` — still requires adding Playwright test dependency or using an already-approved browser harness; keep for a separate package-trust/install pass.
- `enemy-annotated-deathmap` — worth doing, but it touches death-screen rendering and enemy icon composition; larger than this closeout-safe sprint.
- `deterministic-resim-runner` — high-value trust milestone, but it needs a dedicated replay runner design pass and server contract review.

## Execution Log

- `runbrain-experiment-guardrail` — SHIPPED. Evidence: `src/utils/runBrain.js` normalized config haystack; `src/utils/runBrain.test.js` regression for safe-opener with no direct entity match.
- `recent-killer-revenge-drill` — SHIPPED. Evidence: `src/utils/menuGuidance.js` emits `revenge_drill` for 3+ recent deaths to one enemy; `src/utils/menuGuidance.test.js` verifies killer type and CTA metadata.
- `remembered-controller-glyphs` — SHIPPED. Evidence: `src/utils/gamepad.js` label map; `TutorialOverlay`, `MenuPanels.ControlsPanel`, and `PauseMenu` render device-specific labels; `HomeV2` uses remembered profile fallback; `App.jsx` passes controller type into tutorial.

## Validation

- Focused: 29/29 passing across `gamepad`, `runBrain`, and `menuGuidance`
- Full: 408/408 passing across 46 files
- Lint: 0 errors
- Build: passing, main chunk 769.27 kB / 236.52 kB gzip

---
*Generated by `/audit` v1.0 · ready for `/implement`*
