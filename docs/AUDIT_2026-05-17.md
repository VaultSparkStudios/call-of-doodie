<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 34.75
- Top 3: replay-command-trace-v1 · launch-readiness-json · closeout-autopilot-help

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security / engagement / AI | 2h | 9 | 9 | 18.00 | **replay-command-trace-v1** — The honest blocker for deterministic replay validation is still missing replayable input evidence; `inputHash` alone cannot be reversed into commands. A compact local command trace gives the replay trust path an actual artifact to validate later while keeping the current zero-token/local-first Run Brain posture. **Recipe:** Add a pure `replayCommandTrace` utility with deterministic normalization, encoding/decoding, summary digest, and tests that prove traces are bounded, ordered, and tamper-detectable. |
| 2 | ⚡ | feedback loop / launch ops | 1h | 8 | 6 | 12.00 | **launch-readiness-json** — Launch readiness currently prints human text, but Studio OS surfaces cannot consume it as a reliable signal. Structured output would let future startup briefs and status boards distinguish missing analytics, owner-only launch gates, and asset readiness without scraping prose. **Recipe:** Extend `scripts/launch-readiness.mjs` with `--json` output that returns checks, owner-only gates, and a status summary while preserving the existing text output. |
| 3 | ⚡ | process / token-cost | 30m | 7 | 5 | 14.75 | **closeout-autopilot-help** — Session 65 found that `scripts/closeout-autopilot.mjs --help` starts the autopilot instead of printing usage. That burns context and creates a false affordance for Codex-safe closeouts. **Recipe:** Add a real `--help` / `-h` path documenting `--dry-run`, `--skip-push`, `--yes`, and `--message`, and ensure it exits before doctor/git work. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- replay-command-trace-v1

## Skipped (Priority < 1.5)

- 0 items deferred. Human-only launch actions remain in `context/TASK_BOARD.md`: physical PWA QA, gamepad QA, Itch.io publication, and analytics/dashboard credentials.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| replay-command-trace-v1 | SHIPPED | `src/utils/replayCommandTrace.js` adds bounded normalization, compact encode/decode, digest validation, and trace summaries; `src/utils/replayCommandTrace.test.js` covers ordering, round-trip, tamper detection, and event caps. |
| launch-readiness-json | SHIPPED | `scripts/launch-readiness.mjs --json` returns checks, owner-only gates, and a readiness summary while preserving the default text report. |
| closeout-autopilot-help | SHIPPED | `scripts/closeout-autopilot.mjs --help` / `-h` now prints usage and exits before doctor, git, or prompt work. |

---
*Generated by `/audit` v1.0 · ready for `/implement`*
