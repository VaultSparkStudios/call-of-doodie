<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 33.04
- Top 3: replay-loadout-hydration · replay-bootstrap-regression · audit-handoff-tightening

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | engagement / trust | 30m | 9 | 7 | 31.50 | **replay-loadout-hydration** — Shared replay links encode `starterLoadout`, but HomeV2 does not apply it when a replay URL or pasted replay code is loaded. This weakens the seed-rematch promise because the same link can launch with the viewer's current starter instead of the run's actual opener. **Recipe:** Thread `setStarterLoadout` through HomeV2's replay bootstrap and replay-code load path, applying decoded starter before opening or launching the replay. |
| 2 | ⚡ | QA / regression | 30m | 8 | 5 | 20.00 | **replay-bootstrap-regression** — Replay fidelity needs a direct component-level regression, not only encode/decode unit coverage. The launcher is where query params, mode setters, difficulty, and loadout state meet. **Recipe:** Add a HomeV2 test that renders with `?replay=<code>` and asserts difficulty, mode, starter loadout, and seed hydration all fire. |
| 3 | 💡 | process / token-cost | 30m | 5 | 4 | 10.00 | **audit-handoff-tightening** — This repo is already at a high maturity level, so the implementation pass should prefer compact execution artifacts over broad speculative plans. Smaller audit slices reduce context load and make `/implement` idempotent. **Recipe:** Write `docs/IMPLEMENT_PLAN.md` with the exact execution order and append an execution log to this audit so future agents can skip completed work without re-reading the whole session. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- 0 items in this slice. The highest-leverage work here is a trust/fidelity bug, not a speculative feature.

## Skipped (Priority < 1.5)

- 0 items deferred. Larger ideas remain in `context/TASK_BOARD.md`, especially validate-replay Phase 2B, App.jsx extraction, and HomeV2 retirement gates.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| replay-loadout-hydration | SHIPPED | `src/components/HomeV2.jsx` now destructures `setStarterLoadout` and applies decoded replay starter loadouts in both URL bootstrap and pasted replay-code load paths. |
| replay-bootstrap-regression | SHIPPED | `src/components/HomeV2.test.jsx` adds a replay URL hydration test covering difficulty, daily mode, starter loadout, and seed input state. |
| audit-handoff-tightening | SHIPPED | `docs/IMPLEMENT_PLAN.md` records the execution sequence and this execution log makes re-runs idempotent. |

---
*Generated by `/audit` v1.0 · ready for `/implement`*
