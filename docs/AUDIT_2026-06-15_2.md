<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-15 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> Focused continuation plan for the next verified repo-local launch-confidence slice.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 1
- Combined Priority: 68.4
- Top 3: edge-validate-replay-pressure-parity

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | ⚡ | security / speed / organization / efficiency | 1.5h | 9 | 6 | 68.4 | **edge-validate-replay-pressure-parity** — Browser replay fixtures already pin advisory pressure expectations, but the Supabase Edge validate-replay path still carried parallel pressure math with no fixture parity gate. Extracting the Edge pressure receipt into an importable module and checking it against the same fixtures closes the highest-leverage launch-confidence gap without claiming deterministic resimulation. **Recipe:** Extract Edge pressure/evidence helpers from `supabase/functions/validate-replay/index.ts` into a JS module importable by both Deno and Node. Add `scripts/validate-edge-replay-pressure-fixtures.mjs` and `npm run replay:edge-fixtures` to compare Edge pressureClass, commandCount, finalWave, and finalScore against `replayTraceFixtureTable()` and browser `buildReplayPressureProfile()`. |

## Innovation reserve

- None

## Skipped

- full-deterministic-resim-runner — Still a larger future trust milestone; this slice is the prerequisite that prevents browser/edge pressure-estimate drift.

## Execution Log

- edge-validate-replay-pressure-parity: shipped — Edge validate-replay now consumes `supabase/functions/validate-replay/pressure.js`, and `npm run replay:edge-fixtures` compares Edge `pressureClass`, `commandCount`, `finalWave`, and `finalScore` against the shared browser replay fixtures and `buildReplayPressureProfile()`. Evidence: Edge parity gate passed 4 fixtures, existing browser fixture validator passed 4 fixtures, and focused replay tests passed 17/17.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
