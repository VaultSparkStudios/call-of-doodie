<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-09-08.json; do not hand-edit -->
<!-- generated-at: 2026-09-08 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 85.90
- Top 3: arena-authoritative-spawn-perimeter · extraction-objective-radar · large-sewer-extraction

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | features-depth | 3h | 10 | 6 | 25.8 | ✓ | **arena-authoritative-spawn-perimeter** — Make every enemy and boss spawn respect the live arena. The camera and simulation use arena bounds, but App still sends viewport width/height into ordinary enemy and boss spawning and clamps late-wave clusters to that viewport. BOT ROYALE masks the defect because it creates bots directly; any scaled wave mode would concentrate pressure in the upper-left quadrant. **Recipe:** Add a pure arena-bounds resolver at the spawn seam, pass arena dimensions into enemy and boss creation, clamp clusters to the same bounds, and add deterministic viewport-identity plus scaled-arena regression tests. |
| 2 | 🔥 | gamification-immersion | 6h | 9 | 8 | 24.0 | ✓ | **large-sewer-extraction** — Turn Sewer Extraction into the second scrolling-world mode. Extraction's loot routes, rising alarm, and far-edge evac promise traversal, yet its arena remains exactly one viewport. Scaling without a density rule would make the larger world emptier and weaken pressure. **Recipe:** Opt Sewer Extraction into a 1.5× arena, derive a bounded linear pressure scale from arena scale in the mode runtime, preserve every unscaled mode byte-for-byte, and prove crate placement, evac distance, enemy count, and win/lock paths deterministically. |
| 3 | 🔥 | ui-ux-feedback | 4h | 9 | 7 | 24.4 | ✓ | **extraction-objective-radar** — Plot loot and evac objectives on the whole-arena radar. The large-world radar plots the player, enemies, arena, and royale flood only. On a scaled extraction map, crates and the far-edge evac toilet can be off-screen with no navigation signal, violating readable chaos. **Recipe:** Extract a pure radar-projection model, include bounded loot and exit markers only in whole-arena mode, render distinct gold/cyan glyphs with a pulsing evac state, and test edge/corner clipping plus viewport identity. |
| 4 | ⚡ | speed-organization-efficiency | 6h | 7 | 5 | 11.7 | ✓ | **lazy-secondary-death-analysis** — Move collapsed secondary analysis off the immediate death beat. DeathScreen is 1,611 source lines and its production chunk is 90,498 bytes. A large collapsed secondary-analysis panel is compiled into the same chunk as the one-verdict and run-the-fix controls even when the player never opens it. **Recipe:** Extract the secondary-analysis body into a lazy component loaded on first disclosure, preserve all evidence wording and actions, add pending/error recovery through AsyncPanelBoundary, and verify the DeathScreen chunk shrinks without changing the immediate rematch path. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### arena-authoritative-spawn-perimeter
- **L1 · minimal** _(~1h)_ — Replace the two spawn dimensions with arena dimensions.
- **L2 · solid** _(~3h)_ — Add a shared pure resolver, update enemy/boss/cluster paths, and cover identity plus scaled bounds in unit tests.
- **L3 · genius** _(~6h)_ — Add L2 plus camera-aware off-screen spawn exclusion around the current viewport and deterministic perimeter-sector receipts.

### large-sewer-extraction
- **L1 · minimal** _(~2h)_ — Set a 1.5× arena and retain the current enemy count.
- **L2 · solid** _(~6h)_ — Scale the arena, add a reusable bounded pressure-scale contract, and cover traversal and mode outcomes.
- **L3 · genius** _(~10h)_ — Add L2 plus seeded loot districts, risk-weighted crate values, and route receipts after participant pacing evidence exists.

### extraction-objective-radar
- **L1 · minimal** _(~1h)_ — Paint crate and exit dots directly in the existing radar block.
- **L2 · solid** _(~4h)_ — Create a pure bounded radar-objective model, render distinct accessible markers, and add regression coverage.
- **L3 · genius** _(~7h)_ — Add L2 plus distance/bearing labels and priority collapse after real mobile comprehension evidence.

### lazy-secondary-death-analysis
- **L1 · minimal** _(~2h)_ — Lazy-load the existing community and playtest subpanels.
- **L2 · solid** _(~6h)_ — Extract the complete collapsed secondary-analysis body behind a first-open lazy boundary and preserve behavior with focused tests.
- **L3 · genius** _(~10h)_ — Add L2 plus move secondary-only model builders and imports into the deferred chunk after profiling their shared dependencies.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- seeded-extraction-districts
- camera-aware-spawn-sectors
- radar-bearing-collapse

## Skipped (Priority < 1.5)

- balance-retune — DATA-BLOCKED — no representative participant corpus supports numeric balance changes.
- full-replay-physics — DEFERRED PROGRAM — the current replay passport correctly marks enemy/projectile physics parity unavailable.
- new-account-flow — GATED — Obelisk identity and reply-capable mail evidence are external release dependencies, not permission to invent local auth.
- new-asset-atlas — DATA-BLOCKED — no participant evidence disproves the current readable visual baseline.

## Execution Log

### arena-authoritative-spawn-perimeter
- `function at() { [native code] }` · **?** · 

### large-sewer-extraction
- `function at() { [native code] }` · **?** · 

### extraction-objective-radar
- `function at() { [native code] }` · **?** · 

### lazy-secondary-death-analysis
- `function at() { [native code] }` · **?** · 

