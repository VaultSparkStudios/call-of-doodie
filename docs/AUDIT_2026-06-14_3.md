<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-14 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 10
- Combined Priority: 914.5
- Top 3: beat-precision-vulnerability-window · social-proof-wave-death-aggregator · adaptive-boss-dialogue-templates

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 9 | 9 | 121.5 | **beat-precision-vulnerability-window** — The game already has getMusicBeat() triggering spawn particles AND a precision streak glow ring at streak≥5. These two systems have never intersected: nobody in the browser-game category has implemented beat-synced preci **Recipe:** In drawGame.js, compute beatPhase = frame % framesPerBeat (already computed at line 2279). When beatPhase < 8, draw a thin cyan pulse-ring on each non-boss enemy at radius = enemy.radius * 0.5. In App.jsx bullet-enemy hi |
| 2 | 🔥 | gamification | 1h | 8 | 8 | 121.5 | **social-proof-wave-death-aggregator** — The local leaderboard cache (cod-lb-v5) already stores `wave` for every entry. Nobody in the browser-game genre computes and surfaces 'how many other players died on the same wave you're about to face'. A single aggregat **Recipe:** In storage.js, add getWaveDeathCounts() that reduces cod-lb-v5 entries into a {wave: count} map (one pass, O(n)). In App.jsx WavePreviewCard render (lines ~2300 area), pull deathCounts[nextWave] and append a subtle '💀 N |
| 3 | 🔥 | gamification | 1h | 8 | 8 | 121.5 | **adaptive-boss-dialogue-templates** — BOSS_GRUDGE_QUOTES has beautifully written static arrays, but Karen saying 'I have a Yelp account' hits differently when it becomes 'I have a Yelp account and your wave-{wave} performance? One star.' A lightweight `inter **Recipe:** Add interpolateBossQuote(template, ctx) to a new src/utils/bossDialogue.js: replaces {wave}, {weapon}, {deaths}, {streak}, {act} with gs values. Add 2 context-aware grudge/nemesis templates per boss in BOSS_GRUDGE_QUOTES |
| 4 | ⚡ | gamification | 1h | 7 | 7 | 93 | **precision-streak-audio-ladder** — The precision streak already has two visual milestones (glow ring at ≥5, center-hit window highlight at ≥10) and a time dilation trigger at ≥10. But the only audio feedback is the standard kill sound. Adding a rising pit **Recipe:** In sounds.js, add soundPrecisionClick(streakLevel) that plays a 40ms sine oscillator at 440 + (streakLevel * 40) Hz (rising pitch ladder). Add a soundPrecisionLock() played at streak=5 (the ring activation threshold) wit |
| 5 | ⚡ | gamification | 3h | 8 | 8 | 82.8 | **weapon-evolution-on-legend** — weaponLegendKills[] in storage.js already tracks career kills per weapon and LEGEND_THRESHOLDS at 1000 kills. Currently the only reward for Legend tier is a chip on DeathScreen at threshold crossing. The weapon that carr **Recipe:** In storage.js, add getWeaponEvolutionState(idx) that returns {evolved: bool, name: string, damageMult: float} based on weaponLegendKills[idx]. Add WEAPON_EVOLVED_NAMES constant (12 entries) in constants.js. In App.jsx, a |
| 6 | ⚡ | gamification | 1h | 7 | 6 | 79.7 | **multi-kill-combo-fullscreen-card** — RAMPAGE/GODLIKE/UNSTOPPABLE at combo 5/10/15 are currently just addText() floating lines in the center of the screen — the same presentation as any coin pickup. These are milestone moments that deserve cinematic weight.  **Recipe:** In App.jsx, at combo 5/10/15 set gs._comboCardTimer = 60 and gs._comboCardTier = 'rampage'|'godlike'|'unstoppable'. In drawGame.js, when _comboCardTimer > 0, draw a full-width centered card (700×80px) with tier name, col |
| 7 | ⚡ | gamification | 2h | 7 | 7 | 78.75 | **run-arc-gameplay-amplification** — buildRunNarrative() in runNarrative.js classifies every run into Prologue/Rising/Climax/Epilogue act based on wave, score, kills, and near-death events. But this classification ONLY affects the post-run DeathScreen displ **Recipe:** Import buildRunNarrative (or extract a lightweight getRunAct(wave, score, kills)) into App.jsx as a pure function called once per wave-clear. Store result in gs._runAct. In enemy spawn, apply: gs._runAct === 'prologue' ? |
| 8 | ⚡ | gamification | 4h | 8 | 8 | 74.4 | **weekly-world-theme-event** — getWeeklyGauntlet() has a deterministic PRNG seeded by weekNum — but only uses it for weapon/difficulty/perk selection. The same PRNG can deterministically select one of 5 rotating world themes each week: CORPORATE UPRIS **Recipe:** Add WEEKLY_THEMES array (5 entries: id, label, spawnWeightOverrides, statOverrides, themeDesc) in constants.js. Extend getWeeklyGauntlet() to pick theme via existing PRNG (6th rand() call). In App.jsx, apply theme.spawnW |
| 9 | ⚡ | gamification | 2h | 7 | 7 | 73.5 | **enemy-proximity-cluster-spawning** — The existing formation system (flank/pincer/surge from waveDirector.js + heatBiasedFormation) operates at the wave-plan level. At wave 15+, same-type enemies that appear in the same spawn batch still arrive at random can **Recipe:** In spawnEnemy() in gameHelpers.js, track lastSpawnByType[typeIndex] = {x, y, frame}. If the incoming enemy matches the last typeIndex and the same frame batch (<3 frames), offset spawn position by ±60px from lastSpawn.x/ |
| 10 | ⚡ | ux | 3h | 7 | 7 | 67.9 | **deathscreen-run-dna-share-card** — The run DNA fingerprint bar (proportional weapon kill distribution, shipped S88) lives only inside the DeathScreen component. It's the most shareable visual in the game — a personal run signature — but copying the replay **Recipe:** Create src/workers/shareCard.worker.js using OffscreenCanvas. Receives run state: {weaponKills, buildGrade, wave, score, kills, replayProofTier, runArc, seed}. Draws DNA bar (same proportions as DeathScreen), grade lette |

## Innovation reserve

- beat-precision-vulnerability-window
- social-proof-wave-death-aggregator
- adaptive-boss-dialogue-templates
- weapon-evolution-on-legend
- weekly-world-theme-event

## Skipped

- app-bullet-step-extraction — Speed axis only, priority 7.0 — below threshold. Extraction roadmap already in TASK_BOARD; defer to dedicated refactor session.
- flow-field-partial-recompute — Speed axis, priority 8.3 — marginal; BFS runs acceptably on 24px grid. Low innovation (5). Defer.
- leaderboard-wave-histogram — UX, priority 42 — valid but subordinate to social-proof-wave-death-aggregator which covers the same data at higher impact. Demoted to L3 of that item.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
