<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-07-16 -->
<!-- project: call-of-doodie · type: game-app -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 9
- Combined Priority: 218.1
- Top 3: evidence-backed-aim-calibration · canonical-sil-history-truth · next-run-drill-mastery-loop

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | FIRE | UI / UX / input trust / accessibility | 3h | 10 | 8 | 34.5 | **evidence-backed-aim-calibration** — Live inspection found `completeAimCheck()` writes east/north/south/west unconditionally when the player clicks VERIFY CONTROLS. The panel claims a sweep occurred but captures no pointer, keyboard, touch, or gamepad evide **Recipe:** Extract pure calibration-capture helpers; make the panel collect pointer/touch vector, WASD/arrow-key, and right-stick direction buckets; highlight observed targets; disable verification until all four are observed; pers |
| 2 | FIRE | infrastructure / observability honesty / token efficiency | 2.5h | 10 | 7 | 32.3 | **canonical-sil-history-truth** — The canonical startup brief rendered `Last closeout: 42d` and `Projected: 0/1000` while Session 123 closed today at 1000/1000. `render-startup-brief.mjs` robustly discovers the latest session for some fields but still so **Recipe:** Create a shared pure SIL history parser that handles both supported header/body formats, out-of-order append history, category aliases, dates, totals, and velocity. Reuse it in the startup renderer and forecaster; source |
| 3 | FIRE | feedback loop / AI intelligence / retention | 4h | 9 | 9 | 31.3 | **next-run-drill-mastery-loop** — DeathScreen generates specific next-run drills and measurable contracts, but the standard and replay-seed accept paths call `onStartGame()` without passing the drill. Only REMATCH practice carries `challengeOpts.drill`,  **Recipe:** Normalize an active run-drill contract with baseline wave/score and target/proof copy; pass it through every acceptance path; keep ordinary runs leaderboard-eligible; render a compact LIVE DRILL HUD contract; derive an a |
| 4 | FIRE | security / identity trust / supporter UX | 2.5h | 8 | 7 | 25.8 | **supporter-proof-not-self-attestation** — SupporterModal lets any browser click `I already supported`, stores `cod-supporter-v1=1`, and promises the star will appear on leaderboard entries. The submit-score Edge Function correctly overwrites the client flag from **Recipe:** Replace the self-attestation action with a callsign-scoped Supabase verification refresh; cache only verified status plus verification timestamp/callsign; distinguish verified, pending, unavailable, and local-preview sta |
| 5 | BOLT | speed / runtime organization / efficiency | 3h | 8 | 6 | 20.7 | **zero-garbage-frame-index** — The live game loop allocates five new arrays every frame through `.filter(Boolean)`, allocates sergeant position objects every frame, and rescans every enemy for each Summoner. This sits inside a 4,900-line App callback  **Recipe:** Add tested in-place compaction and a reusable enemy-frame index scratch structure; compact nullable runtime arrays without reallocating; build sergeant references and summon counts in one pass; use the index for aura and |
| 6 | BOLT | identity trust / cache coherence | 1h | 7 | 7 | 18.9 | **supporter-verification-cache-freshness** — The primary fix removed self-attestation, but its verified cache had no expiry, so a revoked supporter grant could remain cosmetically active forever in one browser. **Recipe:** Add a seven-day verification time-to-live, surface stale status honestly, fail closed for cosmetics, and test the exact boundary. |
| 7 | FIRE | feedback loop / retention / observability honesty | 1.5h | 8 | 8 | 20.2 | **repeatable-drill-evidence-ledger** — A single improved run is useful evidence but cannot establish repeatability or causality. The new drill receipt needed a cross-run evidence window. **Recipe:** Deduplicate the last three same-drill receipts, require two observed improvements, render EVIDENCE progress, and retain an explicit non-causality claim. |
| 8 | BOLT | input trust / lifecycle coherence | 1h | 7 | 6 | 17.4 | **input-calibration-evidence-freshness** — Evidence-backed capture fixed minting, but saved proof remained ready indefinitely after devices, mappings, or browser input paths could change. **Recipe:** Add a 30-day freshness boundary, distinguish completeness from freshness, show AIM CHECK EXPIRED / INPUT QA RECHECK, and test the boundary. |
| 9 | BOLT | infrastructure / cross-repo coherence / closeout safety | 1h | 8 | 7 | 17 | **read-only-closeout-doctor-boundary** — Final closeout inspection found the autopilot directly invoked the sibling Studio Ops doctor with --update-json, violating the mission boundary even though a read-only health verdict is sufficient. **Recipe:** Route the autopilot through the project-local doctor command with --json --quiet, remove the cross-repo write flag, and add a source-level regression assertion. |

## Innovation reserve

- evidence-backed-aim-calibration
- next-run-drill-mastery-loop
- repeatable-drill-evidence-ledger

## Skipped

- analytics-dashboard-allowlists — `node scripts/check-secrets.mjs --for analytics` is MISSING; PostHog and project-scoped Sentry configuration remain external dashboard work. Supabase CORS is already green.
- homev2-retirement — The live retirement gate passes only by retaining `?home=v1`; production Lighthouse and funnel evidence do not exist, so deletion would violate the recorded decision.
- supabase-auth-obelisk-bridge — The recorded trigger is paid tier or material traffic; neither premise is evidenced. Implementing auth now would silently reverse the accepted guest-first decision.
- contact-email-forwarding — Inbound delivery requires cross-infrastructure DNS/webhook work and remains explicitly unverified on the public page. No repo-local code change can honestly make delivery pass.
- new-security-framework-or-runtime-ai-service — Security release, headers, Content Security Policy, npm audit, cost-neutrality, and replay trust gates are green. The runtime uses zero paid inference, so a new dependency or API would add cost and attack surface without a verified gap.
- ai-theme-pixel-verdict — The hosted 24-capture matrix passed 192/192, but direct AI image inspection remains unclaimed because both filesystem image loading and in-app browser bootstrap failed at Windows CryptUnprotectData.

## Execution Log

- canonical-sil-history-truth: shipped — Shared mixed-format SIL parser now drives startup session/date/score and forecast truth; fresh brief renders last closeout 1d instead of 42d and forecast 999/1000 instead of 0/1000.
- evidence-backed-aim-calibration: shipped
- zero-garbage-frame-index: shipped
- supporter-proof-not-self-attestation: shipped
- next-run-drill-mastery-loop: shipped
- supporter-verification-cache-freshness: shipped
- repeatable-drill-evidence-ledger: shipped
- input-calibration-evidence-freshness: shipped
- read-only-closeout-doctor-boundary: shipped

---
*Generated by `/audit` v1.0 · ready for `/implement`*
