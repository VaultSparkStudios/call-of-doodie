<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-14 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 97.8
- Top 3: replay-proof-receipt · pressure-profile-parity · trace-fixture-harness

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security / user feedback loop / ui / ux / user-experience | 2h | 9 | 9 | 40.5 | **replay-proof-receipt** — The game already captures replay trace evidence and stores level/count/weakness reasons, but the DeathScreen only shows a rich-run badge instead of a compact receipt that tells the player what made the run trustworthy or **Recipe:** Add `buildReplayProofReceipt(traceEvidence)` in `src/utils/replayCommandTrace.js`, returning label, score, status, color, proofLines, and nextAction. Wire `buildSessionSubmission()` to include `traceReceipt` in submitted |
| 2 | ⚡ | speed / organization / efficiency / security | 1.5h | 8 | 7 | 31.4 | **pressure-profile-parity** — `runResim()` already computes action pressure, movement pressure, duration, finalWave, and finalScore internally, while the edge function has a parallel TypeScript version. Before real deterministic resim can exist, the  **Recipe:** Extract the pressure math in `src/utils/replayResim.js` into exported `buildReplayPressureProfile(seed, trace, submitted)` with `durationSec`, `actionPressure`, `movementPressure`, `pressureClass`, `finalWave`, and `fina |
| 3 | ⚡ | feature depth & refinement / token / api consumption reduction | 1h | 7 | 8 | 25.9 | **trace-fixture-harness** — The repo has many trace tests, but each test hand-builds traces inline, making future trust changes easy to update inconsistently. A small fixture harness gives audit/implement sessions a shared vocabulary for rich, basi **Recipe:** Create `src/utils/replayTraceFixtures.js` with `makeRichTrace()`, `makeBasicTrace()`, `makeWeakTrace()`, and `makeMalformedTrace()`. Update replay trace/resim tests to use the fixtures where they currently inline the sam |

## Innovation reserve

- replay-proof-receipt
- trace-fixture-harness

## Skipped

- run-dna-fingerprint-v2 — Rejected on verification: DeathScreen already renders a RUN DNA weapon distribution bar from Session 88.
- rich-trace-badge — Rejected on verification: DeathScreen already renders a VERIFIED RUN chip for rich trace evidence; the new receipt must cover all evidence levels instead.

## Execution Log

- trace-fixture-harness: shipped — Added reusable replay trace fixtures for rich/basic/weak/malformed cases and updated focused trust tests to consume the fixtures.
- pressure-profile-parity: shipped — Extracted `buildReplayPressureProfile()` from advisory resim math, added pressureClass/pressureScore receipt fields, and included `pressureProfile` in `runResim()` results.
- replay-proof-receipt: shipped — Added `buildReplayProofReceipt()`, attached `traceReceipt` to session submissions, and rendered a standalone DeathScreen REPLAY PROOF card for all trace evidence levels.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
