<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-03 -->
<!-- project: call-of-doodie · type: game · session: 78 -->

# Project Audit — Call-Of-Doodie (Session 78)

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 170.5
- Top 3: `nemesis-boss-mechanic` · `experiment-followthrough-loop` · `aim-flow-state-ring`

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 9 | 9 | 40.5 | **nemesis-boss-mechanic** — Track per-boss kill/death counts; 3 deaths without a kill flags that boss as your Nemesis (🎯 on health bar, badge on cutscene card, 3× kill reward). Kill counts also unlock FIRST ENCOUNTER → VETERAN → EXECUTIONER narrative tiers. Transforms bosses from wave events into recurring antagonists with personal history. **Recipe:** Add `getBossKillRecord()`/`saveBossKillRecord()` to `storage.js` (`cod-boss-kills-v1`), set `gs.nemesisBossType` on wave start, update boss cutscene card + drawGame health bar, add NEMESIS_SLAIN achievement to constants, award 3× coins on nemesis kill. |
| 2 | 🔥 | AI / intelligence | 2h | 8 | 9 | 39.8 | **experiment-followthrough-loop** — Save last RunBrain `nextExperiment` to localStorage on death; compare player's actual first-perk/weapon/mode config against it at run start; show 🧪 HUD chip when matched; surface "Experiment followed/diverged" on DeathScreen RunCoach. Makes the AI feel like it remembers and tracks — zero tokens, zero backend. **Recipe:** Add `saveExperimentIntent()`/`loadExperimentIntent()` to `storage.js`, add `matchesExperiment(config, intent)` pure fn to `runBrain.js`, wire `gs._experimentMatched` in App.jsx on first perk/shop, render HUD chip + DeathScreen result line. |
| 3 | 🔥 | gamification | 2h | 8 | 8 | 32.0 | **aim-flow-state-ring** — Precision streak ≥5 draws a subtle glow ring around the player (alpha scales 0→1 from streak 5→15, color lerps cyan→violet). Streak ≥10 adds faint center-hit zone highlights on nearby enemies. Makes mastery visible; creates a distinct visual moment when flow state breaks. Zero balance changes — reads existing `gs.precisionStreak`. **Recipe:** In `drawGame.js`, after player sprite draw, add `ctx.arc` ring conditional on `gs.precisionStreak >= 5`; add enemy center-ring pass at streak ≥10. |
| 4 | ⚡ | AI / intelligence | 1h | 7 | 7 | 31.0 | **mutation-difficulty-compound-brief** — `getDifficultyBriefing()` already returns per-difficulty stats. Add `getMutationDifficultyBrief(mutation, difficulty, runHistory)`: filters history for matching difficulty + active mutation week, computes avg-wave delta, returns one compound line: "NIGHTMARE × Corrosive Rounds — your avg wave drops 40% at this combo (3 runs)." Renders as italic sub-line in HomeV2 difficulty picker. **Recipe:** Add pure fn to `runBrain.js`, expose mutation week ID from `constants.js`, render below existing difficulty brief in `HomeV2.jsx`. |
| 5 | ⚡ | gamification | 30m | 6 | 6 | 27.3 | **formation-flavor-wave-preview** — Wave director already assigns formation identity (flank/pincer/surge). The wave incoming preview card doesn't use it. Map formations to flavor descriptors: "SURGE — overwhelming force", "PINCER — split attack", "FLANK — pressure from the sides". Add as italic subtitle in drawGame wave preview card. 8 lines of code for meaningfully richer narrative identity. **Recipe:** Add 1-line `gs._lastFormationLabel` write in waveDirector's `resolveFormation` call; add formation descriptor map + render in `drawGame.js` wave card block. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `nemesis-boss-mechanic` (Innov: 9) — first truly persistent antagonist identity in a browser roguelite at this scope
- `experiment-followthrough-loop` (Innov: 9) — local-only zero-token AI experiment tracking loop is novel in any game category
- `aim-flow-state-ring` (Innov: 8) — ambient mastery visualization tied to precision streak depth

## Skipped (Priority < threshold or scoped out)

- 7 items deferred: adaptive-enemy-difficulty-curve (balance risk), playwright-pointer-360 (needs devDep install), enemy-annotated-deathmap (2h+ canvas), beat-synced-health-bars (lower ROI this session), device-control-labels (S77 follow-on), runbrain-archetype-expansion (incremental), runhistory-sparkline (polish)

---
*Generated by `/audit` v1.0 · Session 78 · 2026-06-03 · ready for `/implement`*
