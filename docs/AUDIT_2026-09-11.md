# Audit — 2026-09-11 (Session 176)

**Lens:** the numbers the game states to the player, one layer past S175. S175 bound *mode* facts to their runtime; S176 surveyed every other player-facing number — perk, meta-upgrade, meta-tree, weekly-theme, doctrine, controls, tips, coach — against the code that produces it. Plus one tooling defect found by the arc's own triage.

**Rule applied per item:** where the copy states the design and the code breaks it (a bug), fix the code. Where the design is intentional and the copy is wrong, fix the copy. No balance retune beyond making a stated number true.

| # | Finding (verified against live code) | Class | Fix |
|---|---|---|---|
| 1 | `check-writeback-currency.mjs` counted `97705ae docs: record S175 deploy verification` (WORK_LOG only) as write-back debt → reported "a session ended without running closeout" about S175, which closed out fully. Every session that verifies its deploy after closeout trips it for the next session. Ops' newer probe (S291 attribution) does not match this subject shape either. | tooling false positive | Exempt only when subject is canonical, every path is `logs/WORK_LOG.md`, and `S<n>` equals the newest SIL heading; fails closed without SIL. 5 regression cases. Ark cargo to ops. |
| 2 | S175 recorded the genome-writer `(date, session)` upsert defect as "reported as Ark cargo". No such cargo exists in the Ark ledger. | false record | Shipped the cargo (`01K2912H…`); recorded the correction. |
| 3 | META tree **Hair Trigger** "+10% fire rate" multiplied the shot gap by 1.10 — a paid node that made you fire ~9% *slower*. | code bug | `× 0.90` (lower gap = faster, the convention every other fire-rate modifier uses). |
| 4 | **Magnetism + Hoarder** "5× range" synergy: 9× Hoarder-first, 3.6× Magnetism-first. | code bug (order-dependent) | Pair totals exactly 5× in either order. |
| 5 | **Bullet Hose + Deep Pockets** "+50% more ammo": ×2.25 Deep-Pockets-first (inline + FULL ARMORY synergy), ×1.5 otherwise. | code bug (double-apply) | Synergy owns the bonus alone → ×1.5 both orders. |
| 6 | Iron Tank / Speed Freak loadouts assigned a fixed speed after the meta **Speedster** multiply, silently wiping a purchased upgrade. | code bug | Loadout base speed × Speedster tier multiplier. |
| 7 | **Scavenger** tier 3 "+225%" — runtime 90 vs base 30 = +200%. | copy | "+200%". |
| 8 | **Glass Jaw** "double damage" — ×1.65 Hard, ×1.4 Insane. | copy | States the per-difficulty values. |
| 9 | **Build doctrines** tooltip claimed stats (+15% DR, +20% crit dmg, +25% AOE, combo floor) — forging applies nothing; it records to the Doctrine Archive. | copy (claims unbuilt effects) | Flavor kept, false stats removed, states the real reward (archive). |
| 10 | Weekly themes: Corporate Uprising "Karens spawn twice as often" (no spawn bias exists); Rush Hour "spawn rate doubles" (1.5×, reset after wave 1); Retro Wave "doubled" (1.8×). | copy | Each states its real override. |
| 11 | Controls panel "1/2/3/4 switch, 5/Q/G grenade" + tip "Press 5 for grenade" — key 5 selects weapon 5; weapons are 1–9/0/-/=. | copy | Correct key map. |
| 12 | Run Coach tip for The Algorithm (index 20) described berserker elites "3× fast" (they are 1.7×, and not this enemy). | copy | Real mechanic: viral surge triples spawn rate. |
| 13 | Nuke tip "5% drop rate" (weights 0.04/0.07, unnormalized); DEATH'S DOOR / FRAGILE FURY "at low HP" (applied unconditionally). | copy | Truthful wording. |
| 14 | RulesPanel "Every 5th wave" hand-typed. | DUP | Derived from `STANDARD_BOSS_WAVE_INTERVAL`. |

**Deliberately not done:** the ~30 remaining hand-typed-but-agreeing perk/shop/route numbers (DUPs) are not refactored into shared constants this session — that is a wide surface refactor on a launch-sensitive branch with no player-visible payoff; the regression court below pins the four order/direction bugs behaviorally instead. Boss bonus abilities Clone Decoy / Lifesteal set flags nothing reads — dead code, never shown to players; recorded, not touched.

**Verification:** `src/config/copyTruth.test.js` (behavioral court), full suite, strict lint, static gates, build.
