<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-05 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie Protocol Integrity

> Focused continuation audit after `/start` exposed executable protocol drift.
> Run `/implement` to repair the session automation path before closeout.

## Top-line

- Total items: 4
- Combined Priority: 124.6
- Top 3: startup-helper-compatibility · codex-plan-mode-exemption · sil-invariant-repair

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed / organization / efficiency | 1h | 9 | 6 | 26.8 | **startup-helper-compatibility** — Make the upgraded Studio OS protocol executable in this public game repo. Missing helper imports currently break `compact-handoff`, `render-startup-brief`, and direct blocker preflight execution. **Recipe:** Add deterministic local compatibility helpers for `turn-classifier`, `visual-blocks`, `sil-forecaster`, and `blocker-rules`. |
| 2 | ⚡ | speed / organization / efficiency | 30m | 8 | 5 | 26.7 | **codex-plan-mode-exemption** — Restore the existing Codex exemption for Claude Code plan mode. Codex cannot run `/model opusplan`, so reporting `MISSING` is a false blocker. **Recipe:** Read `context/.session-lock -> agent`, stamp non-`claude-code` sessions as `not_required`, and verify JSON output. |
| 3 | ⚡ | observability / honesty | 15m | 8 | 6 | 30.9 | **sil-invariant-repair** — Align `silScore` with the sum of the ten SIL v3 categories. The helper caught `1000` vs category sum `999`; the score box must tell the truth. **Recipe:** Run `node scripts/lib/write-project-status.mjs --fix`, then `--check`. |
| 4 | ⚡ | security / process quality | 45m | 8 | 5 | 22.2 | **protocol-script-smoke** — Prove the repaired start/closeout path at runtime. Syntax checks miss missing ESM imports. **Recipe:** Run `compact-handoff`, `render-startup-brief`, `validate-brief-format`, `blocker-preflight`, `context-meter`, and targeted test/lint gates. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- None in this repair audit. This is reliability work that protects the already-shipped product loop.

## Skipped

- `playwright-pointer-360` — highest product QA gap, but protocol execution failure has to be fixed first.
- `deterministic-resim-runner` — high-value trust milestone, but too large for this closeout-safe repair sprint.

## Execution Log

- `startup-helper-compatibility` — SHIPPED. Evidence: local compatibility helpers added for `turn-classifier`, `visual-blocks`, `sil-forecaster`, `blocker-rules`, `skill-cost-ledger`, and the missing closeout `scan-secrets` gate.
- `codex-plan-mode-exemption` — SHIPPED. Evidence: `verify-plan-mode` again reads the session-lock agent and returns `not_required` for Codex sessions.
- `sil-invariant-repair` — SHIPPED. Evidence: `write-project-status --fix` corrected the status score to match the SIL category sum; `write-project-status --check` reports clean.
- `protocol-script-smoke` — SHIPPED. Evidence: `compact-handoff`, `render-startup-brief`, `validate-brief-format`, `blocker-preflight`, `context-meter`, `verify-plan-mode`, and `write-project-status` all run successfully; `npm run lint`, `npm test`, and `npm run build` pass.

## Validation

- Protocol: startup/closeout dependency smokes pass.
- Full: 408/408 tests across 46 files.
- Lint: 0 errors.
- Build: passing, main chunk 769.27 kB / 236.52 kB gzip.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
