<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-08 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> Axis weights: Gamification 6× · UX 4× · AI 3× · Feature Depth 3× (game type + overlay)

## Top-line

- **Total items:** 8
- **Combined Priority:** 251.0
- **Top 3:** localized-kill-feed · wave-mutation-accept-callout · ai-difficulty-suggestion
- **SIL at audit:** 1000/1000 · Tests: 412/412

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 1h | 9 | 8 | **45.4** | **localized-kill-feed** — Post-wave kill attribution card: "9× TOXIC · 3× ARMORED · 1 ELITE ROGUE — NEUTRALIZED" shown for 1.5s after each wave clears. Teaches enemy composition without pausing action; deepens the military parody voice with a zero-cost debrief. Data already lives in the game loop; only drawGame.js rendering is new. **Recipe:** Add `gs._waveKillsByType = {}` reset per wave; increment on kill; render a top-3-type overlay card in drawGame.js on wave-clear frame using existing floating-text infrastructure. |
| 2 | 🔥 | gamification | 30m | 7 | 7 | **37.1** | **wave-mutation-accept-callout** — Full-width 1.5s red screen flash ("☠️ DIRTY DEAL SIGNED — CORROSIVE ROUNDS") when a wave mutation challenge is accepted. The risk/reward moment is the best decision in the game; right now it has zero ceremony. Thirty minutes in drawGame.js, no state changes. **Recipe:** Set `gs._mutationAcceptFlash = { label, framesLeft: 90 }` on accept in App.jsx; render banner + camera-shake in drawGame.js while `framesLeft > 0`. |
| 3 | 🔥 | AI + UX | 2h | 8 | 8 | **32.0** | **ai-difficulty-suggestion** — RunBrain computes avg wave and boss kills from run history already; extend it to emit a difficulty nudge ("Clearing wave 14 on RECRUIT — VETERAN awaits") below the HomeV2 difficulty picker. Zero tokens, local-first, directly closes the feedback loop from death data to next-run setup. **Recipe:** Add `suggestDifficulty(runHistory, currentDiff)` in `runBrain.js`; return null when < 3 runs; wire into HomeV2 difficulty picker as a 1-line italic hint. |
| 4 | ⚡ | gamification | 30m | 7 | 6 | **31.8** | **coin-streak-visual-escalation** — `gs.coinMultActive` already fires a multiplier but the visual is a tiny HUD label. Escalating poop-particle rain (3 → 5 → 8 💩 per kill, keyed off `gs.coinStreakKills`) makes the multiplier viscerally legible and rewards sustained aggression with comedic brand payoff. Pure drawGame.js addition. **Recipe:** In the kill-flash path of drawGame.js, spawn `Math.min(3 + floor(streakKills/3), 8)` extra 💩 emoji particles when coinMultActive is true; reuse existing floating-emoji infrastructure. |
| 5 | ⚡ | gamification | 2h | 8 | 7 | **28.0** | **adaptive-spawn-damping** — `recordDeathByEnemy()` already exists; reading that into spawn-weight damping (top-2 killers −10% spawn prob after 3+ consecutive deaths) auto-balances frustration without touching difficulty constants. "Adapted for you" chip at run start closes the feedback loop. Task-board Next item with a cleaner implementation path than previously scoped. **Recipe:** Add `getAdaptiveSpawnMods(career)` in `storage.js`; multiply base enemy-type probability by `(1 − dampFactor)` in `spawnEnemy()` in `gameHelpers.js` before the weighted random draw. |
| 6 | ⚡ | UX + gamification | 2h | 8 | 7 | **28.0** | **enemy-death-heatmap-types** — The existing heatmap shows WHERE you died but not WHY. Extending `gs._deathPositions` entries with the killer type (already tracked in `gs._lastDamageBy` at death) and rendering a small enemy emoji at each cluster centroid turns the spatial map into a genuine tactical debrief. Direct task-board Next item completing the heatmap feature. **Recipe:** Add `type: gs._lastDamageBy` to each `gs._deathPositions` push; in DeathScreen heatmap canvas render, draw enemy type emoji at each death position after the path; group when ≥3 nearby share the same type. |
| 7 | 🔥 | gamification + UX | 4h | 9 | 8 | **27.8** | **run-end-build-report-card** — Post-death BUILD GRADE card grading perk-synergy efficiency, ammo discipline, and survivability-per-level using data already in `statsRef` and `gs`. Comedy tier labels ("BORN SOLDIER / TRIGGER HAPPY / GLASS CANNON") fit the parody military voice and create a shareable hook no comparable mobile-web shooter has shipped. **Recipe:** Create `src/utils/buildReport.js` with `computeBuildGrade({ activeSynergies, weaponKills, weaponAmmos, totalShots, wave, level })` → `{ grade, label, breakdown }`; add 11 focused tests; render BUILD GRADE card in DeathScreen.jsx above the SCORE section with letter grade + 3 sub-bars. |
| 8 | ⚡ | feature depth | 4h | 9 | 6 | **20.9** | **playwright-pointer-360** — `buildPointerAimSweepReport()` exists in `gameStep.js`; the debug HUD tracks quadrant coverage; the missing piece is a Playwright test driving the real canvas and asserting all four buckets fill after a pointer sweep. Deferred 7 consecutive sessions; last automated gate before 'input fairness' launch claim is provable. **Recipe:** `npm i -D @playwright/test`; create `tests/pointer-360.spec.ts` that loads `localhost:5173?debug=input`, enters a run, sweeps pointer in N/E/S/W arcs around canvas center, asserts debug HUD shows all 4 quadrant counters ≥ 1; add `"test:e2e"` to package.json scripts. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

3 items — execute regardless of session pressure:

- `localized-kill-feed` (innovation 8) — nobody in the parody-shooter category does a post-wave kill-attribution card with comedic voice
- `ai-difficulty-suggestion` (innovation 8) — no difficulty picker coaches you from your own play history without an LLM call
- `run-end-build-report-card` (innovation 8) — no comparable web shooter grades your build efficiency post-run

## Execution Log

- **localized-kill-feed** — shipped in current worktree: wave kills are tracked by enemy type and rendered as a post-wave attribution card.
- **wave-mutation-accept-callout** — shipped in current worktree: accepting a mutation sets a timed flash and draw layer renders the dirty-deal banner.
- **ai-difficulty-suggestion** — shipped this pass: `suggestDifficulty()` coaches upgrade/downgrade choices from recent run history and HomeV2 renders the local hint.
- **coin-streak-visual-escalation** — shipped in current worktree: coin multiplier kills escalate emoji particle payoff by streak depth.
- **adaptive-spawn-damping** — shipped in current worktree: repeated killer history feeds spawn dampening and run-start adaptation copy.
- **enemy-death-heatmap-types** — shipped as a bounded first step: final ghost-path death markers now retain/render the killer type; multi-death cluster centroid grouping remains the natural next polish layer.
- **run-end-build-report-card** — shipped this pass: `computeBuildGrade()` grades synergy, ammo discipline, and survival, and DeathScreen renders the BUILD GRADE card.
- **playwright-pointer-360** — shipped this pass: `@playwright/test@1.60.0`, `playwright.config.ts`, `tests/pointer-360.spec.ts`, and the debug HUD sweep assertion prove 4/4 pointer coverage in a real browser.

**Validation:** `npm test` passed 423/423 across 47 files, `npm run build` passed, `npm run test:e2e` passed 1/1 Chromium test, and `npm audit --json` reports 0 vulnerabilities after dev-toolchain remediation.

## Skipped (Priority < 1.5 or blocked)

- `homeV2-retirement-gate` — Priority 9.5; data gate (Lighthouse + funnel) is a human action; not implementable this session
- `validate-replay-phase2b` — Blocked per DECISIONS.md; needs compact input timeline contract before resim runner is viable
- `first-session-calibration-ring` — Priority 24.5; out-ranked; good candidate for next session after heatmap ships
- `anti-idle-enemy-taunt` — Priority 24.0; needs animation design pass before implementation
- `precision-streak-leaderboard` — Priority 22.8; requires Supabase token to deploy schema change (token MISSING)

---
*Generated by `/audit` v1.0 · Session 84 · 2026-06-08 · ready for `/implement`*
