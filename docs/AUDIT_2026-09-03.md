<!-- generated-by: /audit skill v1.0 (founder-directed evolution audit) -->
<!-- generated-at: 2026-09-03 -->
<!-- project: call-of-doodie · type: game -->

# Call of Doodie — Evolution Plan (Session 163 · 2026-09-03)

## Context

**Founder complaint:** "the 3 modes all look the exact same", menu pages jump to a different theme, and the game hasn't evolved toward real modes / teammates / objectives / multiplayer.

**Audit verdict (evidence-backed, 3 explore + 2 plan agents):**
- **Goal drift confirmed.** Sessions 154–162 shipped evidence receipts, corrective-order ledgers, protocol docs, and threat compasses. Self-scored Game Loop Review says 9.0/10 while no gameplay differentiation shipped. Every session was "evidence-gated" against tuning/content, so nothing that changes *what the player does* landed.
- **Modes are cosmetic.** `src/config/modeCatalog.js` is labels only; the only runtime table is `src/systems/modeRules.js:36-67` (timer direction, score mult, boss interval, shop/draft/route booleans, zombie stat mutation). Speedrun = a timer that counts up. No mode changes objective, arena, win condition, or roster. Arena theme is `floor(random()*8)` (`src/systems/arenaEnvironment.js:88`).
- **Operations are labels over the same loop.** 3 ops × 7 verbs (BREACH/HOLD/ESCORT/HUNT/SABOTAGE/ESCAPE/BOSS in `src/systems/operationCampaign.js`) all play as "clear enemies, press E". Interactables in `operationArenaState.js` are inert state machines; the turret doesn't shoot.
- **No allies, no netcode.** Zero teammate entities. No Durable Objects, no WebSocket transport. One Supabase realtime channel (stats ticker).
- **No fixed timestep, partial determinism.** `useGameLoop.js:208-219` is bare rAF. `runRng.js` has 5 seeded streams but `Math.random` remains in App.jsx (2), combatResolution, defeatEconomy (2), objectiveDirector, pickupSpawning, shopOptions (3), waveDirector.
- **App.jsx budget is the hard constraint.** `scripts/contracts/app-architecture-budget.json`: max 5000 lines (now 4760), max loop span 1775 (now ~1768). **7 lines of loop headroom.** Extract before adding.
- **Five unrelated visual languages** ship in prod: `home-arcade.css` (HomeV2 default, CRT), `home-v3.css` (Inter), MenuScreen (86 inline hexes, no CSS), `public/legal.css` (all 17 static pages, Inter 17px doc), `auth.css` (brown radial), plus `runtime-shell.css`, `display-name-screen.css`, `tutorial-overlay.css` (#00ff88), MobileHUD TONE_COLOR. Brand orange exists as 5 hexes. `docs/BRAND_SYSTEM.md` has zero tokens. `src/utils/theme.js` THEMES has the right shape but no CSS consumer. ~900 inline JSX hexes.
- **Redundancy:** modes in 5 places, rules in 3, stats in 5, changelog in 2 arrays, enemies in 2, disclaimer in 2 footers, 18-link flat footer on every page, `/play/` is a one-sentence page, 3 landing pages via `?home=`, `obelisk-passport/` dead duplicates.
- **Stale:** changelog last entry Aug 16; `/status/` hardcodes Aug 23; community snapshot shows 12 runs from Aug 8; README says 7 modes / 57+ achievements (8 / 66); SupporterModal promises "early access" and an unfinished "Week N of 4" track.
- **Infra:** anti-cheat is real (HMAC run token → plausibility envelopes → heuristic replay validator), CSP strong. Gaps: Ko-fi webhook (`ACAO:*`, no rate limit, non-constant-time compare, no idempotency), `style-src unsafe-inline`, App chunk 544 KB (S145 budget was 493), 62 `shadowBlur` sites + uncached gradients in drawGame.js, PostHog/Sentry plumbed but dark. Zero LLM calls anywhere.
- **Token cost:** context set ~900 KB (SELF_IMPROVEMENT_LOOP 315 KB, CURRENT_STATE 206 KB, TRUTH_AUDIT 181 KB, TASK_BOARD 131 KB, DECISIONS 131 KB); 36 AUDIT_* files; STARTUP_BRIEF 8 days past its own 24h validity.

**Founder decisions (this session):** Arcade CRT is the brand · Async multiplayer first, then Durable Object co-op, networked royale only after co-op telemetry · First tranche = gameplay foundations + first two real modes.

**Contract:** this plan replaces the "evidence-gated, hold all content" posture. Content and modes ship; evidence language stays honest but no longer blocks.

---

## Master ordered list (token-efficient order of operations)

Foundations that unblock the most items go first. App.jsx is touched in **one** batched pass. Each CSS/JSX file is touched **once** for tokens.

### Tranche 1 — Gameplay foundations + first two real modes (START HERE)

**1.1 Extract enemy AI to `src/systems/enemyFrame.js`** (L)
`App.jsx:3209-~3700` (aura, movement, ranged fire, boss abilities) → `stepEnemyFrame(gs, ctx)`, pattern of `projectileFrame.js:35`. Ctx carries `W,H,spawnEnemy,spawnBoss,addText,addParticles,rng,difficulty,setBossWaveActive`. Add `pickTarget(e, gs)` (nearest of player + live allies + mode-declared zones/structures) so allies and HUNT flee AI land here later. Verify: seeded 200-frame snapshot of `gs.enemies` identical before/after; `node scripts/check-app-architecture.mjs` green.

**1.2 Fixed timestep** (S)
`src/hooks/useGameLoop.js:208-219`: 1/60s accumulator, step N≤4 per rAF, render once, expose `gs.frameIndex`.

**1.3 Math.random eradication in sim paths** (S)
Route App.jsx(2), combatResolution, defeatEconomy, objectiveDirector:72, pickupSpawning, shopOptions, waveDirector, perkOptions, routeOptions to `getRunRng(gs, stream)`. Add streams `allies`, `director`, `royale` to `RUN_RNG_STREAMS` (additive; spawn stream byte-locked with `validate-replay`). Add a vitest that greps `src/systems/**` for `Math.random` outside runRng.js. Presentation files (drawGame, sounds, roastDirector, DemoCanvas) keep cosmetic random.

**1.4 Headless `stepSim(state, input, ctx)`** (M)
New `src/sim/stepSim.js` composing `gameStep.js` movement/aim, `stepProjectileFrame`, `stepEnemyFrame`, `stepAllies`, objective tick, spawn tick. `src/sim/presentationKeys.js` + `snapshotSimState(gs)` strip list (particles, floatingTexts, screenShake, trail, damageFlash). Node test runs 600 frames without DOM. `replayResim.js` becomes a wrapper. (Unlocks true server resim in `validate-replay` and lockstep later.)

**1.5 Mode definition layer + honest catalog** (M)
New `src/systems/modeDefinition.js`: `{ id, rules, arena{blueprint,themePool}, roster{weights,spawn}, objectives{pool,scripted}, winCondition(gs), waveScript, mechanic{init,step,onEnemyKilled,onWaveStart}, hud{timer,zones,alarm,flashlight,squad,floodRing}, audio.vibe, allies, isChallengeRuleset }`. `modeRules.js` stays as the ruleset layer. `modeCatalog.js` splits into **Modes** (Standard, Hold the Throne, Sewer Extraction, Boss Gauntlet, Infection, Bot Royale; Siege/Duel later) and **Rulesets** (Daily, Weekly Gauntlet, Score Attack, Speedrun, Cursed) that stack on any mode. Catalog collapses 5 name fields → `name, tagline, blurb`. Ghost/leaderboard bucket key becomes `mode+ruleset` (`ghostRecorder.js`, `loadTopGhosts`, `submit-score` enum). Update `REPLAY_MODES` guard + `modeCatalog.test.js`.

**1.6 The one batched App.jsx surgery pass** (M, net-negative lines)
Stub modules first so it compiles. At `gs` build (`App.jsx:586`): `...createModeState(modeDef, seed)` → `allies, zones, structures, flood, alarm`. Arena build: `modeDef.arena` replaces random theme. Wave-start (`~2910-2945`): `beginWaveObjectives(gs, modeDef, ctx)`. Spawn tick (`~2744`): `modeDef.roster.spawn`. After projectiles (`~3181`): `stepAllies; stepModeMechanic; tickModeObjectives`. Win check via `modeDef.winCondition` routed through `runTermination.js` with new cause `"win"`. `drawGame.js` gains `drawAllies/drawZones/drawStructures/drawFlood/drawFlashlightMask` behind `modeDef.hud`. Raise `minSystemBoundaries` in the budget file, never the line caps.

**1.7 Ally / CPU teammate entity** (M)
`src/systems/allyUnit.js`: `spawnAlly(gs, personality, rng)` mirrors enemy shape + `faction:"ally", order: follow|hold|attack|revive|carry, downed`. `stepAllies` behavior tree; shots push to `gs.bullets` with `owner:"ally"` (projectile hit detection reused, ally kills 0.5× score). Enemy bullets hit allies (loop in projectileFrame). Downed → 8s bleed-out, revive by E within 40px. Orders on keys 1/2/3 recorded to `commandTraceRef`. Deterministic via `allies` stream. Personalities (comedy with mechanical difference): **The Intern** (clingy, panics <30% HP, 2× pickup radius), **Plunger Sergeant** (holds, shotgun, shouts real formation hints), **The Roomba** (no gun, vacuums pickups to you, enemies ignore it). `drawAllies` reuses enemy sprite path + green ring. Squad HP strip in HUD/MobileHUD. Gated to modes that declare `allies`.

**1.8 Objective verbs made behavioral** (M)
`src/systems/objectiveHandlers.js`: uniform `{start, tick→active|done|failed, drawHints}` registering the 5 `objectiveDirector` types **and** the 7 Operation verbs. BREACH = door HP shot down, reinforcements at 50%. HOLD = 45s zone timer (player or ally in zone), leaving resets 25%. ESCORT = plunger cart as ally `carry` on waypoints. HUNT = bounty elite with `fleeing:true`. SABOTAGE = 3s continuous E under SURGE. ESCAPE = alarm meter + reach exit before 100. BOSS unchanged. `useOperationMode.js:122 interact()` gates on `handler.tick==="done"`. Add `handlerState` to `operationEncounterContract.js`.

**1.9 Mode: BOSS GAUNTLET** (S) — `src/modes/bossGauntlet.js`: 6 bespoke bosses back-to-back, no trash, phases, 10s perk draft between, par time. Proves the definition layer.

**1.10 Mode: HOLD THE THRONE** (M) — `src/modes/holdTheThrone.js` + `src/systems/zones.js`: 3 rotating zones, ally squad, score = seconds held, lose when control lapses under pressure. Enemies target zones. Forces allies + zone targeting.

**1.11 Tranche-1 verification gate**: `npm run lint:strict`, `npm test`, `node scripts/check-app-architecture.mjs`, `npm run build`, `npm run test:e2e` (chromium + mobile-chrome), play each new mode in `npm run dev` on desktop + mobile emulation, seeded replay snapshot test, `npm run gameplay:contract` regenerates modes contract.

### Tranche 2 — Website unification (one cohesive arcade brand)

**2.1 Design tokens** (M) — Create `src/styles/tokens.css` (`:root` dark, `[data-cod-theme="porcelain-day"]` light, `@font-face` self-hosted IBM Plex Mono in `public/fonts/`, `.fx-scanlines/.fx-vignette/.fx-glow/.fx-grid` opt-in, reduced-motion aware) and `src/styles/base.css`. Canonical: orange `#ff6b22`, gold `#ffd34f`, cyan `#33e6ff`, ink `#f4f0e8`, muted `#b7b2aa`, quiet `#8f8a83`, bg `#070b10`, danger `#ff3b3b`, focus = cyan; display `Impact,"Arial Narrow Bold"`, body mono; type scale 96/32/22/16/14/12; space 4–64; radii 4/8/14. `src/utils/theme.js` exports `THEME_CSS_VARS` + `tokensToCss()`; new `scripts/generate-tokens-css.mjs` writes `public/tokens.css` first in `prebuild`. `index.html:41` focus → `var(--cod-focus)`; import tokens first in `main.jsx`.

**2.2 Static template** (S) — `generate-public-pages.mjs:157` links `tokens.css` + `doc.css` (renamed, hex-stripped `legal.css`, Inter import deleted, mono 16px/1.65/68ch, `.fx-vignette` only). Fixes the founder's complaint on all 17 pages in one commit.

**2.3 Small CSS files, one pass each** (S) — `auth.css` (drop brown radial), `runtime-shell.css` (cap h1), `display-name-screen.css`, `tutorial-overlay.css` (green→gold), `weapon-dock.css`.

**2.4 Delete the alternates** (S) — `HomeV3.jsx`, `home-v3.css`, `MenuScreen.jsx` + test, `src/utils/homeVersion.js` + test, `?home=` branch in App.jsx (`~4225`), `obelisk-passport/` dir. Fold V3's compact mode drawer into HomeV2 as the `#modes` panel.

**2.5 `home-arcade.css` + HomeV2 inline hexes** (M) — alias `--arcade-*` → `--cod-*`, delete `[style*=...]` override hacks, move CRT to utilities, convert 72 inline hexes.

**2.6 In-game panels** (L, scripted) — `src/styles/panels.css` (`.panel .btn .btn--primary .btn--ghost .stat .toast`), then a scripted sed pass over the known hex set in MenuPanels (165), DeathScreen (190), PauseMenu (91), HUD (55), LeaderboardPanel (52), App.jsx (200), MobileHUD TONE_COLOR values → `var(--cod-*)`. Eyeball diff. Replace `docs/BRAND_SYSTEM.md` palette prose with a 20-line token table.

**2.7 Information architecture** (L) — Final sitemap: `/` · `/modes/` (renovated showcase, per-mode hero + rule + objective + best ghost) · `/field-manual/` (new `src/content/fieldManual.js` feeding SPA panel + static; retires `/how-to-play/`, RulesPanel/ControlsPanel bodies, `quickRules.js`) · `/bestiary/` (`src/content/bestiary.js` from ENEMY_TYPES; retires `/enemies/`, feeds MostWanted) · `/arsenal/` (+perks/doctrines) · `/board/` (Daily/All-time/Community/Season tabs; retires `/leaderboard/` + `/stats/`; one component used in-SPA under `#board`) · `/changelog/` (`CHANGELOG_ENTRIES` only; delete `NEW_FEATURES`, What's New = `slice(0,3)`) · `/roadmap/` (new `src/content/roadmap.js` shipped/next/notPlanned; negative-claim lines move here) · `/about/` (absorbs `/press-kit/`, `/ip/`) · `/profile/` (SPA hash) · `/support/ /accessibility/ /status/ /privacy/ /terms/ /contact/` · `/404.html` in theme. Delete `/play/`. `public/_redirects` with 301s. New `src/utils/hashRoute.js` (~40 lines): `#deploy #modes/<id> #board #board/daily #profile #field-manual #changelog #bestiary/<id> #daily`. HomeV2 "ALL PLAYER TOOLS" 12 buttons + 3 tabs → 6 tiles (Deploy · Modes · Board · Profile · Field Manual · What's New); Upgrades/Meta Tree/Loadouts/Missions/Achievements become `#profile` tabs. Footer → 3 columns (Play / Learn / Studio+Legal) + one disclaimer + one agent line, driven by `footer-manifest.json` for both React and static; static pages get the mobile nav via `renderToStaticMarkup` of `PrimaryNavigation`. Update `PrimaryNavigation.test`, `HomeV2.test`, `MenuPanels.replayCoverage.test`, `check-public-claims.mjs`.

**2.8 Freshness automation** (M) — `generate-public-gameplay-contract.mjs` runs first in `prebuild` and emits modes/achievements/enemies/weapons/perks/arenaThemes/buildDate/gitSha/latestChangelog; registry copy templates `{{modes}}`; README `<!-- claims:start/end -->` block synced by `scripts/sync-readme-claims.mjs`; `check-public-claims.mjs` fails on README drift, bare-number drift, `/status/` date ≠ buildDate, and warns when the latest changelog is >30 days old (fails on CI tag builds). `/status/` fetches `/_health` live. Delete `data/community-stats-snapshot.json`; `/board/` shows honest empty state. `scripts/stub-changelog-entry.mjs` from `git log <lasttag>..HEAD` as a predeploy hook.

**2.9 Guest vs passport** (M) — New `ProfilePanel.jsx` (Career, Doctrines, Ghosts, Backup, Passport) assembled from CareerStatsPanel + existing backup/restore. `functions/api/profile.js` PUT/GET localStorage blob → Supabase `profiles(subject pk, blob jsonb, updated_at)` (migration, RLS by subject, rate-limited via `_shared/http-trust.ts`); cross-device sync = last-write-wins + "restore from cloud?" prompt. Leaderboard rows gain optional `subject`. SupporterModal: remove "early access"; cosmetic track hidden behind `COSMETIC_TRACK_ENABLED=false` unless the art exists. Login receipt card folds into Profile→Passport; post-login redirect `/#profile`.

**2.10 Agent + social surfaces** (S–M) — `agents.json`/`llms.txt` gain `contracts{gameplay,routes,modes}` + `api{topScores,communityStats,health}`; `modes-contract.json` generated from the catalog; `/developers/` static page from the same contract; `functions/og/[[slug]].js` SVG cards (`/og/daily/<seed>`, `/og/run/<id>`, `/og/mode/<id>`) injected via the existing `_middleware.js`; `/embed/daily/` widget.

**2.11 Front door + death screen diet** (M) — Deploy above the fold on mobile; mode cards loop `DemoCanvas` previews under IntersectionObserver; ticker fed by `/api/community-stats` with honest empty state; tutorial overlay auto-opens on first Deploy. One `Toast.jsx`. DeathScreen 1,700 → ~600 lines: score/wave/ghost, one share button, "Run again"/"Change mode", 3-line "what killed you", next-unlock bar; link out to `#profile/achievements` and `#board`; extract `src/utils/shareCard.js`.

### Tranche 3 — More modes + async multiplayer + virality

**3.1 SEWER EXTRACTION** (M) — corridors blueprint, loot chests, alarm 0→100, evac toilet at ≥60, die = lose loot, extract = bank to persistent stash (`storage.js`) unlocking starter loadouts. Uses 1.8 SABOTAGE/ESCAPE handlers.
**3.2 Virality quick wins** (S each) — Clip of the Run (auto-capture 6s around top `damageSequence`/death via existing `gifCapture.js`, attach to share card); Run-DNA card + Beat-My-Run on one button with mode/ruleset badge; achievement badges as share images.
**3.3 Async multiplayer** (S/M) — live ghost sprite in Daily at matching `frameIndex`; seed Duel (`challengeLinks.js` + `duels` table, 24h expiry, result card); squad leaderboard (`squadCode` on submit-score, sum of best daily); Beat-My-Run one tap post-death.
**3.4 Nemesis** (M) — most-frequent killer persists in career, spawns as named elite with taunt next run, badge on kill.
**3.5 BOT ROYALE (offline)** (L) — `src/modes/botRoyale.js`: 2.5× arena + camera offset, 15 bots = ally units `faction:"bot"` FFA, loot crates from 12-weapon roster, shrinking flood ring (generalized `lockdown` DoT), kill feed, placement ("Flushed 4th"), shareable lobby seed code. Ships the mode and the ring/loot/camera code before any netcode.
**3.6 INFECTION** (M) — dark map + flashlight `destination-out` mask, ammo scarcity, repairable barricades, `zombieMode.js` roster. **SIEGE** (M) — between-wave turret/barricade build (turret = ally speed 0). **DUEL** (S) — mirror AI with your loadout.
**3.7 Seasonal ladder** (M) — 4-week seasons in `submit-score` + `top-scores.js`, `/board/#season`, ghost reset per season.

### Tranche 4 — Real-time co-op (Durable Objects), then networked royale

**4.1 Lobby Worker** (L) — `cloudflare/lobby-worker/` standalone Worker with `durable_objects.bindings`, `/lobby/:code` WebSocket, hibernation API. DO does seed issue + input ordering with 3-frame delay lockstep; all clients run `stepSim`; periodic `snapshotSimState` hash, mismatch → resync from host. Cost: Workers Paid $5/mo + ~$0.01 per 4-player 10-min session (≈$80–90 at 10k sessions/mo); gate lobby creation by monthly budget via the existing `operationCapacity.js` kill-switch. Bots fill empty slots.
**4.2 Toilet Royale networked** (XL) — 8–16 players replacing bots, 6-frame delay, host-relay. Only after 4.1 telemetry.

### Tranche 5 — Security, performance, intelligence, cost (interleave as small items)

**5.1 Ko-fi webhook hardening** (S) — move to `_shared/http-trust.ts`, constant-time token compare, rate limit, `message_id` idempotency table, drop `ACAO:*`.
**5.2 CSP** (S) — remove `style-src unsafe-inline` once panels.css lands (2.6 makes this possible); narrow `img-src`/`connect-src`; extend `security-release-gate.mjs` with an unsafe-inline regression check + Supabase function presence.
**5.3 Render perf** (S) — gradient cache in drawGame.js, gate `shadowBlur` sites behind `__codPerfStep`; App chunk back under the 493 KB S145 budget (enemyFrame extraction + DeathScreen diet + v1/v3 deletion do most of it); add a bundle-size assertion to CI.
**5.4 Observability on** (S, founder credential) — set `VITE_POSTHOG_KEY` + Sentry DSN in Cloudflare env; this is what finally answers the mobile INP `<select>` hypothesis and the HomeV2 funnel item on the genius list. Replace native `<select>` mode picker with the tile grid (2.7 does this).
**5.5 True server resim** (M) — `validate-replay` imports `src/sim/stepSim.js` (Deno ESM) and replays the command trace instead of heuristics; closes the "plausible synthetic trace" anti-cheat gap.
**5.6 Cheap LLM, offline only** (S) — nightly script batches `agentProjection` death records + `run_anomalies` into one Haiku call for cohort design signal; build-time roast/taunt pool generation. No runtime LLM calls.
**5.7 Token cost of the repo itself** (S) — archive `docs/AUDIT_*` older than last 3 into `docs/archive/`; roll `SELF_IMPROVEMENT_LOOP.md`, `TRUTH_AUDIT.md`, `CURRENT_STATE.md`, `TASK_BOARD.md`, `DECISIONS.md` to a last-10-sessions window with archive tails (~700 KB reclaimed); make `render-startup-brief.mjs` run in `/start` when stale instead of only at closeout; trim `CLOSEOUT_PROMPT.template.md`/`START_PROMPT.template.md`.

---

## Top combined list (what to implement, in order)

1. enemyFrame extraction + fixed timestep + RNG streams (1.1–1.3)
2. Headless stepSim (1.4)
3. Mode definition layer + honest catalog + App.jsx surgery pass (1.5–1.6)
4. Allies + behavioral objective verbs (1.7–1.8)
5. Boss Gauntlet + Hold the Throne (1.9–1.10)
6. Design tokens + static template + small CSS + delete v1/v3 (2.1–2.5)
7. Panels CSS + inline hex sweep (2.6)
8. IA consolidation + hash routes + footer (2.7)
9. Freshness automation (2.8)
10. Profile + cloud backup + copy fixes (2.9)
11. Sewer Extraction + virality quick wins + async multiplayer (3.1–3.3)
12. Bot Royale (3.5)
13. Nemesis, Infection, Siege, Duel, seasons (3.4, 3.6, 3.7)
14. Security/perf/resim/observability/repo-diet (5.x, interleaved when a session has slack)
15. Durable Object co-op lobby → networked royale (4.x)

## Critical files
- `src/App.jsx` (586-608 gs build · ~2744 spawn tick · ~2910-2945 wave-start · ~3181 projectile step · 3209-3700 enemy AI block · ~4225 home switch)
- `src/systems/modeRules.js`, `src/systems/objectiveDirector.js`, `src/systems/runRng.js`, `src/systems/operationCampaign.js`, `src/hooks/useOperationMode.js`, `src/hooks/useGameLoop.js`, `src/systems/projectileFrame.js`, `src/systems/gameStep.js`, `src/drawGame.js`
- `scripts/contracts/app-architecture-budget.json`
- `src/utils/theme.js`, `src/components/home-arcade.css`, `public/legal.css`, `scripts/generate-public-pages.mjs`, `scripts/lib/public-route-registry.mjs`, `scripts/generate-public-gameplay-contract.mjs`, `scripts/check-public-claims.mjs`
- `src/components/HomeV2.jsx`, `MenuPanels.jsx`, `DeathScreen.jsx`, `SiteFooter.jsx`, `PrimaryNavigation.jsx`, `src/config/{modeCatalog,publicNavigation,changelog}.js`

## Reuse points (do not rebuild)
`projectileFrame.js` callback-bag pattern · `objectiveDirector.js` 5 types + weakness weighting · `operationArenaState.js` interactable state machines · `zombieMode.js` · `bossPhases.js`/`bossWaveFlow.js`/`spawnBoss` · `waveDirector.js` formations · `heatMeter.js` · `ghostRecorder.js`/`loadTopGhosts` · `challengeLinks.js` · `gifCapture.js`/`runDnaShareCard.js`/`shareCard.worker.js` · `THEMES` in `theme.js` · `MobileDeployConfig.jsx` palette pattern · `_shared/http-trust.ts` · `operationCapacity.js` kill switch · `commandTraceRef`/`replayResim.js`.

## Verification (every tranche)
`npm run lint:strict` · `npm test` (≥1270 assertions, no deletions without replacement) · `node scripts/check-app-architecture.mjs` · `npm run build` + bundle-size check · `npm run public:contract` · `npm run security:release` · `npm run test:e2e` · manual play of each new mode in `npm run dev` (desktop + Pixel 7 emulation) · seeded snapshot test for determinism · staging preview deploy + `npm run launch:qa` before production.

## Session-protocol notes
- On execution, materialize this as `docs/AUDIT_2026-09-03.md` (+ `.json`) so `/implement` and closeout tooling recognize it, then run the write parts of `/start` (session lock, sync) that plan mode skipped.
- Record in `context/DECISIONS.md`: evidence gating no longer blocks content/modes; arcade CRT is the sole brand; async→DO co-op→networked royale ordering.
