# Implementation Plan — Session 130

Source: docs/AUDIT_2026-07-25.json

Status: **COMPLETE** — all three primary items and three second-order safeguards shipped; no live repo-owned Genius List candidate remains.

## Efficiency order

1. **Source-neutral enemy-defeat pipeline** — establish the exactly-once lethal transition and shared effect boundary first because grenade correctness and every later gameplay proof depend on it.
2. **Live multimodal movement contract** — connect keyboard, touch, and gamepad movement to the pure frame boundary after combat mutation semantics are stable.
3. **Truthful core-logic coverage contract** — expand and ratchet measured logic coverage last so the final scope includes every new system/helper and its focused tests.
4. **Expansion pass** — refresh the Unified Genius List, generate the innovation pack, inspect the newest boundaries, and implement every safe second-order candidate while the context meter remains `CONTINUE`.

## Per-item gate

- Implement the deepest honest L3 recipe and add focused behavioral coverage.
- Preserve deterministic random-number-generator draw timing, damage math, combo/score/loot semantics, invulnerability timing, replay truth, keyboard/touch/controller parity, accessibility, and cost-neutral architecture.
- No player, retention, performance, hardware, visual, or release claim without measured evidence.
- Run focused tests and strict lint after each behavior slice.
- Run `node scripts/context-meter.mjs --json`; continue while the verdict is `CONTINUE`.
- Record exact evidence in the audit execution log and reconcile the Task Board.

## Final matrix

PASS — strict lint · 809/809 tests · core coverage 82/82 at 72.27/67.03/71.14/77.58 · production build · launch/security/public/replay/media gates · browser E2E 2/2 · staging runtime 7/7 · hosted visual automation 255/255 · handoff trim 3/3 focused fixtures. Direct AI pixel inspection remains explicitly unclaimed.
