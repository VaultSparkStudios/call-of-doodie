# Implementation Plan — Session 131

Source: docs/AUDIT_2026-07-26.json

Status: **COMPLETE**

## Efficiency order

1. **Public route truth graph** — establish the shared registry and generated/validated public surfaces first because the staging visual matrix and every later release receipt depend on complete route discovery.
2. **Integer-budgeted enemy atlas pipeline** — normalize and compress the recovered art before runtime/HUD browser verification so the final build measures the real delivery path.
3. **Capability-complete responsive HUD** — wire the pure run-intel model and capability parity after the asset/public foundations are stable, then verify desktop/mobile/controller paths together.
4. **Saturation pass** — refresh the Unified Genius List, generate the innovation pack, inspect the three newest boundaries, and implement every safe second-order candidate while the context meter remains `CONTINUE`.

## Per-item gate

## Completion receipt

- All three ranked L3 audit items are implemented and recorded in the audit sidecar.
- Four verified second-order innovations are implemented in `docs/SECOND_ORDER_INNOVATIONS_2026-07-26.md`.
- The refreshed Unified Genius List contains only external-data, product-choice, dashboard, publication, or founder-gated work; none is silently labeled complete.
- Current engineering verification proceeds through the full suite, staging, visual matrix, doctor, security, and closeout gates below.


- Implement the deepest honest L3 recipe.
- Preserve combat math, deterministic random-number-generator timing, progression, replay/leaderboard trust, guest play, accessibility, themes, proprietary rights, and cost-neutral architecture.
- Use source-derived registries and validators; generated outputs may never certify themselves.
- Add focused behavior and drift coverage before marking an item shipped.
- Run the applicable focused tests, strict lint, and a behavior-specific check after each slice.
- Run `node scripts/context-meter.mjs --json`; continue while the verdict is `CONTINUE`.
- Record exact execution evidence in the audit sidecar and regenerate its Markdown.

## Final gates

- Full unit suite and production build.
- Public contract, visual asset, dependency, security, replay, media, and protocol gates.
- Real Chromium desktop/mobile input smoke.
- Isolated staging deploy for the final exact SHA.
- Registry-derived route × theme × viewport visual automation.
- Direct doctor JSON with `blockingFailing: 0`.
- Subjective AI pixel review remains unclaimed if the Windows `CryptUnprotectData` boundary persists.
