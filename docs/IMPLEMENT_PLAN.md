# Implement Plan — Session 153

Source: `docs/AUDIT_2026-08-13.json`. Default rung: L2.

1. [x] `startup-closeout-age-truth` — shared SIL date resolution now prefers the latest parsed session header and passes stale-header/fallback regression courts.
2. [x] `playtest-signal-contract` — the backwards-compatible receipt now records explicit control trust and danger readability with no free text or upload.
3. [x] `playtest-pulse-command-post` — the accessible local aggregate command post and identifier-free export are wired and rendered-pixel verified.
4. [x] `wave-plan-replay-passport` — bounded wave-plan fingerprints reach death/history/public contracts as an explicitly advisory fourth replay lane.

Per-item gates: focused pure/component tests, strict lint on touched source, architecture budget, and the medium success bar. Final gates: exact full suite, deployable build, public/replay/security/supply-chain/cost checks, isolated staging, and CANON-053 desktop/mobile proof in both themes for touched user-interface states.
