# Implementation Plan — Session 128

Source: `docs/AUDIT_2026-07-23.json`

## Efficiency order

1. **Progression runway contract** — establish the pure account-level and milestone substrate first; Balance Lab consumes it without changing balance numbers.
2. **Pressure arc receipt** — add the bounded accumulator and run-history schema before any post-run UI consumes the evidence.
3. **Playtest flight recorder** — build the opt-in, session-local evidence contract on top of existing command/death/continuation boundaries.
4. **Agent gameplay contract** — generate the public dual-audience artifact from source constants and make drift fail the public contract.
5. **Single next move death flow — DROPPED (false premise)** — live inspection proved RUN THE FIX is already primary and secondary actions already live behind `MORE RUN ACTIONS`; only the valid playtest-choice receipt remains in scope.
6. **Expansion pass** — refresh the Unified Genius List, generate the innovation pack, inspect the newest five boundaries, and ship every safe second-order refinement while the context meter remains `CONTINUE`.

## Expansion wave shipped

- Bound Oracle preverification to the actual project so valid local surfaces cannot be reported missing from the Studio Ops working directory.
- Preserved same-band pressure peaks, cached playtest opt-in outside the input hot path, made clipboard failure explicit, and removed the hard-coded progression tier cap.
- Root-fixed the founder-reported Replay Training no-op by turning it into an immediate guided-run action with clean evidence.
- Reorganized the homepage around Quick Play, Player Tools, an open Player Hub, and actionable plain-language device/save status; removed the internal art-card gallery from the player surface.
- Generated, provenance-recorded, optimized, and wired proprietary player/Karen sprites into live canvas rendering with procedural fallbacks and refreshed browser captures.

## Per-item gate

- Implement the deepest honest L3 recipe and add focused behavioral coverage.
- Preserve deterministic random-number-generator draw timing, replay truth, keyboard/touch/controller parity, accessibility, and cost-neutral architecture.
- Use observed-only language; no playtest, balance, retention, or launch claim without source evidence.
- Run focused tests plus the game medium-quality gate after each behavior slice.
- Run `node scripts/context-meter.mjs --json`; continue while the verdict is `CONTINUE`.
- Record exact evidence in the audit execution log and mark the matching Task Board row done.

## Final matrix

`npm run lint:strict` · `npm test` · `npm run build` · `npm run deps:check` · `npm run public:contract` · `npm run protocol:drift` · `npm run security:release:audit` · replay state/edge fixtures · launch media · typed health/HSTS checks · isolated staging + hosted browser/visual evidence · direct doctor JSON · `git diff --check`.
