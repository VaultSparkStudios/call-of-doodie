# Implementation Plan — Session 129

Source: docs/AUDIT_2026-07-24.json

## Efficiency order

1. **Startup brief source coherence** — establish the pure evidence selectors and validation substrate first so every later receipt is projected truthfully.
2. **Capability-gated browser clients** — move optional remote clients behind asynchronous capability boundaries and preserve local-first behavior before touching player feedback.
3. **Final damage-sequence receipt** — route all player-damage paths through one observed boundary, persist the bounded receipt, and add post-run intelligence.
4. **Upstream release-truth cargo** — ship exact checker/registry drift evidence through Studio Ark after local acceptance commands are final.
5. **Expansion pass** — refresh the Unified Genius List, generate the innovation pack, inspect the newest boundaries, and implement every safe second-order candidate while the context meter remains `CONTINUE`.

## Per-item gate

- Implement the deepest honest L3 recipe and add focused behavioral coverage.
- Preserve deterministic random-number-generator draw timing, damage math, invulnerability timing, replay truth, keyboard/touch/controller parity, accessibility, and cost-neutral architecture.
- No player, retention, performance, hardware, visual, or release claim without measured evidence.
- Run focused tests and strict lint after each behavior slice.
- Run `node scripts/context-meter.mjs --json`; continue while the verdict is `CONTINUE`.
- Record exact evidence in the audit execution log and reconcile the Task Board.

## Final matrix

`npm run lint:strict` · `npm test` · `npm run build` · `npm run deps:check` · `npm run public:contract` · `npm run protocol:drift` · `npm run security:release:audit` · replay state/edge fixtures · launch media · live health/cutover checks · local proof-chain · direct doctor JSON · `git diff --check`.
