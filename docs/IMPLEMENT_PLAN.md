# Implementation Plan — Session 126

Source: `docs/AUDIT_2026-07-21.json`

## Efficiency order

1. **Coherent startup evidence** — make an active session newer than `STARTUP_BRIEF.md` a machine-readable stale condition.
2. **Pause trust contract** — centralize every pause route, release held inputs, auto-pause on visibility loss, and preserve replay evidence.
3. **Protocol reference drift gate** — restore public-safe `/audit` and `/implement` summaries and verify all four local command anchors through a safe oracle proxy.
4. **Adaptive performance flight recorder** — replace unbounded frame samples with fixed-memory percentile evidence, conservative assistance, hysteresis, and an honest local run receipt.
5. **Enemy-defeat economy parity** — extract score, career-boss, and coin-drop planning so railgun and projectile paths share one deterministic contract.
6. **Expansion pass** — refresh the Unified Genius List, generate the innovation pack, inspect the newest runtime boundaries, and ship every safe agent-owned second-order refinement while the context meter remains `CONTINUE`.

## Per-item gate

- Implement the selected L3 recipe and add focused behavioral coverage.
- Preserve deterministic RNG draw timing, replay truth, keyboard/controller parity, accessibility, and the cost-neutral client-only architecture.
- Run the game medium-quality gate after input/gameplay changes.
- Run `node scripts/context-meter.mjs --json`; continue while the verdict is `CONTINUE`.
- Record exact evidence in the audit execution log and mark the matching Task Board row done.

## Final matrix

`npm run lint` · `npm test` · `npm run deps:check` · `npm run public:contract` · `npm run protocol:drift` · `npm run security:release:audit` · replay state/edge fixtures · launch media · production build · isolated staging deploy + browser/visual matrix · direct doctor JSON · `git diff --check`.