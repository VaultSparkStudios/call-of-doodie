<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-07-01 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 7
- Combined Priority: 119.3
- Top 3: rematch-death-wave-drill · seeded-enemy-rng · replay-contact-enemy-parity-slice

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | FIRE | gamification / engagement | 2h | 9 | 7 | 31.5 | **rematch-death-wave-drill** — Death is the game's most motivated moment and the debrief already coaches the player, but the only action is a full restart from wave 1. A REMATCH button that relaunches at the death wave with the same seed converts coac **Recipe:** Add optional startWave to initGame(forceSeed, startWave) (App.jsx:544, currentWave hardcoded 1 at :554); scale starting loadout/level context sanely; add REMATCH AT WAVE N button to DeathScreen next-run drill block (Deat |
| 2 | FIRE | feature depth / replay trust | 4h | 9 | 7 | 24.4 | **seeded-enemy-rng** — The run seed only drives arena layout (App.jsx:811-920); enemy type, spawn side, position, elite rolls and timing all use global Math.random (gameHelpers.js:34,54-59,83-101; App.jsx:1299-1300,2314). So REPLAY #seed and D **Recipe:** Add createWaveRng(seed, wave) (mulberry32/LCG over a mixed hash) in src/gameHelpers.js; thread an rng param through spawnEnemy() and the App.jsx spawn call sites for type roll, side/position, elite/berserker rolls, shoot |
| 3 | FIRE | security / replay trust | 3h | 7 | 8 | 24.1 | **replay-contact-enemy-parity-slice** — Phase 2B's honest next slice per S106 canon: stored traces carry player commands only, so a true enemy replay is impossible today - but a seed-derived single contact enemy stepped with the real chase math (direct-vector  **Recipe:** Add runDeterministicContactEnemySlice() to src/utils/replayResim.js with coverage label trace_movement_one_contact_enemy_derived: derive spawn side/position/frame from seed via local LCG, step the enemy with atan2 chase  |
| 4 | SPARK | ai / intelligence | 1h | 6 | 5 | 18.9 | **balance-lab-player-surface** — HomeV2 computes a balance-lab insight from local telemetry (HomeV2.jsx:213) but renders it only behind the ?debug=ops gate (:123-127, :850-873), so a real intelligence surface is invisible to every player. Promoting the  **Recipe:** Render balanceLab.topInsight as one compact chip/line in the visible HomeV2 intel area when present, keep the full lab table debug-gated, cover with a HomeV2 test asserting the insight renders without the debug flag. |
| 5 | SPARK | process quality / observability | 0.5h | 6 | 2 | 9.1 | **board-truth-stale-blockers** — The top two genius-list blockers are stale: deploy-supabase-function.yml has SUPABASE_ACCESS_TOKEN + SUPABASE_PROJECT_REF as Actions secrets and has deployed sync-studio-events successfully five times since 2026-06-12 (l **Recipe:** Mark the S82 sync-studio-events TASK_BOARD item done with the CI-run evidence; narrow the S61 allowlist items to the genuinely-gated PostHog/Sentry/Ko-fi dashboard halves (analytics capability still MISSING); trigger one |
| 6 | SPARK | speed / efficiency | 2h | 6 | 3 | 9 | **drawgame-hot-loop-perf** — drawGame allocates a filtered copy of the enemies array twice every frame (drawGame.js:467,1123), rebuilds full-screen gradients every frame (:150,351,1148,1168,1397), and allocates per-enemy body-shape array literals pe **Recipe:** Replace the two filter(Boolean).forEach passes with plain for-loops that skip falsy entries; add a size-keyed gradient cache (key W:H) for the static full-screen gradients following the S96 vignette-cache pattern; hoist  |
| 7 | SPARK | speed / organization | 0.5h | 3 | 1 | 2.3 | **dead-sound-exports** — soundHit, soundPickup, soundGrenade and soundEnemyDeath were superseded by spatial ...At variants and have zero non-test callers (~55 dead lines). Dead exports mislead future sessions into wiring the wrong variant. **Recipe:** Delete the four exports from src/sounds.js after re-verifying zero callers; run lint + full suite. |

## Innovation reserve

- replay-contact-enemy-parity-slice

## Skipped

- deathflow-unit-tests — Phantom - src/systems/deathFlow.test.js already exists and covers the S111 builders; scout premise disproved on verification.
- supabase-auth-membership — Founder product decision (docs/AUTH_INTEGRATION_PLAN.md); not agent-ownable.
- posthog-sentry-kofi-dashboard-allowlists — analytics capability still MISSING locally and in scope; dashboard edits are not repo-code work. Narrowed from the stale combined S61 item.
- physical-device-qa — Real devices required (PWA install + gamepad); receipts surfaces already shipped S109/S110.
- homev2-lighthouse-funnel — Requires production browser/data evidence a CLI cannot fabricate.
- itchio-listing-and-screenshots — Publication UI + verified browser captures required; SVG fallbacks stay per S105 decision.

## Execution Log

- board-truth-stale-blockers: shipped — TASK_BOARD S82 + S61 items closed with CI-run and live-OPTIONS evidence; fresh workflow_dispatch run 28555855725 confirmed green.
- seeded-enemy-rng: shipped — createWaveRng()/getWaveSpawnRng() in gameHelpers.js; spawnEnemy/spawnBoss + App.jsx spawn call sites threaded; 15 new determinism tests, all legacy spawn tests still pass.
- rematch-death-wave-drill: shipped — resolveRematchStartWave/buildRematchKit in src/systems/rematchDrill.js; REMATCH button in DeathScreen; leaderboard/achievement/career-record exclusion for practice runs.
- replay-contact-enemy-parity-slice: shipped — runDeterministicContactEnemySlice() in replayResim.js, coverage trace_movement_one_contact_enemy_derived; fixture gate extended with determinism assertion.
- balance-lab-player-surface: shipped — PATTERN SPOTTED session-dismissible card in HomeV2 renders balanceLab.topInsight when status=signals-found, outside the ops debug gate.
- drawgame-hot-loop-perf: shipped — Two filter(Boolean).forEach passes over gs.enemies replaced with for-loops; bg + theme-vignette gradients cached on gs; per-enemy body-shape literals hoisted to module constants.
- dead-sound-exports: shipped — soundHit/soundPickup/soundGrenade/soundEnemyDeath deleted after re-verifying zero non-test callers; spatial ...At variants are exact supersets.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
