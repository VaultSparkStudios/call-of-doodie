<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-18 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 188.6
- Top 3: trace-edge-forwarding-firewall · trace-payload-storage-contract · replay-trust-smoke-script

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security / automation | 1h | 9 | 7 | 58.2 | **trace-edge-forwarding-firewall** — `buildSessionSubmission()` now creates `traceDigest`/`traceLength`, but `saveToLeaderboard()` normalizes the payload before invoking `submit-score`, silently dropping the trace fields. This makes the S68 server contract unreachable from the real online submit path. **Recipe:** Add a pure `buildSubmitScorePayload()` helper in `src/storage.js` that preserves sanitized trace fields after leaderboard normalization, use it in `saveToLeaderboard()`, and add regression coverage. |
| 2 | 🔥 | security / feature depth | 2h | 9 | 8 | 54.0 | **trace-payload-storage-contract** — Digest + length prove a trace contract exists, but deterministic resim still needs an actual bounded trace body. Carry the compact trace body through the client payload, validate its digest/length at the edge, and store it only in member `game_sessions.metadata` so the leaderboard schema stays clean. **Recipe:** Extend `buildSessionSubmission()` with `traceBody`, add edge validation against body/digest/count, and keep body storage metadata-only. |
| 3 | ⚡ | automation / feedback loop | 1h | 8 | 6 | 44.3 | **replay-trust-smoke-script** — The repo has launch health checks, but no terminal-verifiable smoke for valid vs malformed replay trace contracts on deployed `validate-replay`. That leaves future deploys able to regress trace confidence silently. **Recipe:** Add `scripts/replay-trust-smoke.mjs` plus an npm script that posts a valid trace-backed competitive run and a malformed trace run, expecting `trace_contract` and quarantine respectively. |
| 4 | ⚡ | gamification / immersion | 1h | 7 | 5 | 32.1 | **ghost-pack-hud-surface** — `loadTopGhosts()` seeds `gs.topGhosts`, but the player never sees that persistent leaderboard pressure during a run. Surface the top ghost pack as a compact HUD target strip so the existing async ghost load becomes visible motivation. **Recipe:** Pass `topGhosts` into `HUD` and render a small non-blocking “Ghost Pack” score target strip below the challenge tracker. |

## Innovation reserve (Innovation >= 8 — execute these even when busy)

- **trace-payload-storage-contract** (Innovation: 8) — turns replay trust from metadata-only proof into a future resimulation substrate without adding leaderboard schema churn.

## Skipped (Priority < 1.5)

- 0 items deferred. Human-only launch gates remain in `context/TASK_BOARD.md`: physical PWA/gamepad QA, Itch.io publication, analytics credential injection, and Cloudflare token rotation.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| trace-edge-forwarding-firewall | SHIPPED | `src/storage.js` now exports `buildSubmitScorePayload()` and `saveToLeaderboard()` uses it so trace fields survive leaderboard normalization; `src/storage.test.js` covers the stripped-field regression. |
| trace-payload-storage-contract | SHIPPED | `src/utils/runSubmission.js` forwards `traceBody`; `supabase/functions/submit-score/index.ts` validates trace body count and digest before accept, rejects mismatches as replay-trace failures, and stores valid bodies only in member `game_sessions.metadata`. |
| replay-trust-smoke-script | SHIPPED | `scripts/replay-trust-smoke.mjs` and `npm run replay:trust-smoke` exercise valid trace confidence plus malformed-trace quarantine against deployed `validate-replay`; local syntax check passed. Live run was attempted but blocked by sandbox network and escalation was not approved. |
| ghost-pack-hud-surface | SHIPPED | `src/App.jsx` passes `gs.topGhosts` into `HUD`; `src/components/HUD.jsx` renders a compact Ghost Pack target strip with top leaderboard scores for the current mode/difficulty. |

## Validation

- `npx vitest run src/utils/runSubmission.test.js src/storage.test.js` — 24/24 passing.
- `node --check scripts/replay-trust-smoke.mjs` — passing.
- `npm run lint` — passing.
- `npm run build` — passing.
- `npm test` — 350/350 passing.
- `npm run replay:trust-smoke` — attempted; sandbox fetch failed and network escalation was not approved.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
