<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-09-09 -->
<!-- project: call-of-doodie · type: game -->

# Project Audit — Call-Of-Doodie

> One ranked improvement plan covering verified current-state gaps.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 197.4
- Top 3: death-attribution-observed-damage · mode-outcome-receipt · screen-anchored-announcements

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification-immersion | 4h | 10 | 6 | 69.6 | pending | **death-attribution-observed-damage** — handlePlayerDeath reads gs._lastDamageBy, which no module has written since the S163 enemy-frame extraction, then falls back to the nearest enemy's `.type` — a field enemies never carry (they carry `typeIndex`). killerTy **Recipe:** Add a pure resolveDeathAttribution(gs) in src/systems/deathAttribution.js that prefers the last damage-sequence event (sourceType/sourceName/kind), falls back to the nearest live enemy by typeIndex, and reports the evide |
| 2 | 🔥 | gamification-immersion | 3h | 8 | 6 | 62.1 | pending | **mode-outcome-receipt** — The death and victory screens carry only a mode label (plus a royale placement). A SEWER EXTRACTION death never says how much loot went down the drain or how close the evac was; a HOLD THE THRONE death never says how man **Recipe:** Give each mode definition a pure outcome(gs) that returns a bounded {headline, detail, stat} receipt (extraction: loot lost or banked, crates, alarm state; royale: placement, flushed, phase; throne: captured/lost; gauntl |
| 3 | ⚡ | ui-ux-feedback | 4h | 9 | 6 | 41.8 | pending | **screen-anchored-announcements** — Floating texts are drawn in arena coordinates under the camera translate (S165). Twenty-eight App.jsx call sites write level-ups, DOCTRINE READY, objective ACTIVE, MISSION COMPLETE, synergy, and perk roasts at GW()/2, GH **Recipe:** Add addScreenText to transientPresentation (a floating text flagged screen:true), render screen-flagged texts outside the camera translate in drawGame, migrate every viewport-centre App.jsx call site, and hand modes an a |
| 4 | ⚡ | features-depth | 2h | 8 | 4 | 24 | pending | **mode-wave-context-arena-authority** — createModeState and stepMode receive arena dimensions, but onModeWaveStart is called with { W: GW(), H: GH() } — the viewport. SEWER EXTRACTION spawns three crates per wave from that context, so every crate after the fir **Recipe:** Resolve arena bounds once at the wave-start seam and pass them (plus viewW/viewH for screen-space needs) to onModeWaveStart; add a unit test that drives onWaveStart with arena dimensions and asserts crates land beyond th |

## Innovation reserve

- None

## Skipped

- lazy-panel-browser-contract — DEFERRED — the S166 follow-up needs a Playwright path that reaches the death screen; no existing fixture or test hook reaches it, and adding a production test hook is a design decision the founder should make. The jsdom source contract from S166 remains in force.
- extraction-lockdown-ends-run — DEFERRED — the source comment says lockdown ends the run while the code turns it into a last stand; the public roadmap only promises 'lockdown at 100'. Changing the ending is a design call without participant evidence.
- balance-retune — DATA-BLOCKED — no representative participant corpus supports numeric balance changes.
- new-account-flow — GATED — Obelisk identity and reply-capable mail evidence are external release dependencies, not permission to invent local auth.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
